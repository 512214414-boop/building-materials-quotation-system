# 2026-09-01 工作日志

## 元数据取值词汇表 + 一致性校验（纯旁路任务）
- 新建 `data-source/vocabulary.yml`：中文↔英文取值对照表（检索锚点，不参与生成）。
  - A类(6)：scene / fieldClass / confirmStrategy / renderMode / align / fixed（值域来自 `entityRelations.types.ts` 联合类型，权威）。
  - B类(3)：layer / dataType / unique（值域来自 `entity-meta.yml` 已出现值）。
  - 注意：用户原粘贴在 `align` 处被截断，按文件自身 header 规则「A类=TS联合类型全部值、B类=yml已出现值」重建；`fixed` 与 `layer/dataType/unique` 为我推断补入，中文词为用户建立的真值层，需用户复核。
- `tools/gen-entity-meta.mjs` 生成前插入 `validateVocabularyConsistency()`：
  - A类域 values 键集须 === TS 联合类型成员（含未使用值如 global/number 也须登记）；
  - B类域 values 须覆盖 yml 已出现值；
  - vocabulary.yml 缺失→仅 warn 跳过（不改既有行为）；不通过→`process.exit(1)` 中断。
- 验证：`node tools/gen-entity-meta.mjs` 通过（9 域）；负向测试（删 layer 的 snapshot 值）→ 正确报错并中断生成；恢复后通过。
- 关键约束遵守：纯旁路，成功路径 4 份产物字节级不变（entityMeta/entityRelations/后端 generated/可视化 actions）。

## 待办（用户未要求，可后续）
- vocabulary.yml 推断条目（fixed、layer/dataType/unique 中文词）需用户确认/裁剪。
- （遗留自 08-31）entity-meta 迁移历史损坏（v22 shadow 重放失败 / v27 空文件夹），开发库靠 db push 维护。
