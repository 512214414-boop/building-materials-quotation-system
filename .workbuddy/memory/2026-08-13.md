# 2026-08-13 工作日志

## 构建「顶层设计驱动文档治理」Skill（项目级）

- 路径：`.workbuddy/skills/topdown-doc-governance/`
  - `SKILL.md`：主指令（触发词 + 三步流程：梳理/检查/演进 + 铁律）
  - `references/methodology.md`：顶层抽象↔细节 哲学详解
  - `references/templates.md`：溯源映射 / 功能文档骨架 / 架构原则骨架 模板
  - `references/inspection-checklist.md`：四类病灶（膨胀/断链/越权/重复）检查表
  - `scripts/scan_docs.py`：stdlib-only 扫描器，自动初筛四类病灶
- 用户诉求（口述提炼）：开发文档无限膨胀、顶层抽象与实现细节失联；细节应以顶层逻辑为中心组织，演进须自上而下（先改顶层再推演细节）。
- 已在真实文档 `用户项目开发文档/` 跑通扫描，产出 `_治理报告_20260813.md`。

### 扫描真实发现
- 11 份功能文档**几乎都不回溯最顶层**（项目设计哲学/顶层设计规范），只引用兄弟功能与架构原则短名 → 印证"顶层↔细节失联"。
- 断链 4 处：引用《产品数据层》但真实文件是 `数据库新设计·产品数据层.md`（命名不一致）。
- 膨胀：3 份 >500 行（供应商管理 616 / 配货与成本 562 / 系统管理 522）；其余 7 份功能文档 + 1 份 AI协作 >300 行。
- 越权 36 处（主要为目录类文档命名文件，按"放宽"处理；骨架/示例代码块不计入）。
- 重复：未发现高重复对。

### 待办（已全部执行完）✅
- 给 11 份功能文档补"顶层依据"锚点句 → 已完成，每份开头加了 `> 顶层依据：详见《顶层设计规范·理念X》《项目设计哲学·约束Y》。`
- 修复《产品数据层》断链 → 已执行：把 `数据库新设计·产品数据层.md` 重命名为 `产品数据层.md`（与其 H1、README 一致），同步更新 产品管理/供应商管理/产品数据录入转化规范 的 Markdown 链接与 整改任务书 引用。
- 生成 `溯源映射.md` → 已完成，含「顶层←用户约束」「功能文档←顶层」两张映射表，悬空项已清零。
- 扫描器 `scan_docs.py` 同步增强：4.1 解读改为动态（全部锚定则显示✅）、孤儿检测排除 AI协作/技术架构 结构性元文档。
- 复扫验证：11 份功能文档全部 ✅ 锚定最顶层；断链 0；孤儿 0；旧文件名引用 0 残留。报告已回写 `_治理报告_20260813.md`。

## 走查「采购报价视图」达标度 + 定位核心 bug（用户要求"真正用一下"）

- 结论：核心用法达标（行内即存、无保存按钮、快照解耦、弹窗不叠、实时防竞态均到位）。
- **推翻先前"无翻页"判断**：翻页在 `UnifiedTable.tsx`（手写单据式翻页，默认 pageSize=13），非 PurchaseQuote 自身。
- **核心 bug（P-014，已写入技能质量闸门第七节）**：
  - `emptyRowFactory` 用整表行数算 seq → 每页空行 seq 撞车；
  - 空行提交一律 append 到表末尾（非输入位置），对比 `handleInsertBelow` 的 splice(idx+1) 才对 → 不同页/行输入串位；
  - 选品 onSelect 与输入框失焦文本 onCommit 双提交竞态 + seq 撞车 → 点匹配没落位、反而复制出带原文字的空行、无限增。
  - 用户原话"输入通用→两个通用→下一条又通用→重复新建"完全对应。
- 用户心智模型（正确，应落地）：手写单据式翻页（想写哪写哪、想改哪改哪、可无限翻页）→ 工具栏「整理数据」按钮把真实行从前往后紧凑排列、去中间空行、重排 seq（稳定、不浪费性能）。
- 已把 P-014 作为永久规避缺陷写入 `.workbuddy/skills/building-quote-dev-collab/references/quality-gate.md`。
- 待用户确认修复范围：A 只修插入串位/双提交根因；B 仅加「整理数据」按钮；C 两者都做（推荐）。

## 系统性梳理业务架构（档案/订单/工作台三件套）

- 用户诉求：系统性描述整个项目业务部分，搞清档案管理、订单中心、协同工作台的协同关系；推演建材特有环节是否合理；补全基础字典（够用、只分类不分级）；明确数据使用方式与"层"的抽象。
- 产出：`业务架构系统性分析.md`（项目根目录）+ 3 张可视化关系图（业务三件套总览 / 订单九流程与建材特有环节 / 档案层抽象机制）。
- 核心结论：
  1. **档案中心**：独立界面 4 个（产品=多表集合、供应商/客户/仓库=单表）；嵌入式全局字典 5 个（分类/品牌/价格类型/联系方式/客户类型，由 DictRefField 承载）。判定原则已固化为"多表集合或单表→独立；小字典→嵌入式"。
  2. **订单中心**：documents+document_lines，8 档状态机，快照+ID 解耦（档案改名/删除不影响历史）。
  3. **协同工作台九视图**：采购报价→收款对账→配货★→交付→成本核定★→售后→销售汇总→归档。★为建材特有（配货拆分/超额待入库/欠库/成本三段/点位规则），均源自建材门店真实经营，合理。
  4. **只分类不分级核查**：现状达标——产品 categoryId、客户 customer_type(枚举标识)、客户 discount_rate(数值) 都不是分级；供应商/仓库无分类字段也够用。无需扩充。
  5. **层抽象**：`QUICK_CREATE_LAYERS`（product/price/customer 三层）= 字段+兜底值+去重键；使用处声明"用哪层+填了什么"，配置驱动；去重两类（A 全局 name / B 父级 id+name）；DictRefField 统一承载 5 字典。这是"不同视图关系能搞清"的根基。

## 修复采购报价视图 P-014（根因 + 整理数据，用户选"两者都做"）

- 后端：`documentLineService.resequenceLines`（包 normalizeSeq+broadcast，保 ID）/`resequenceLinesHandler`/`POST /staff/documents/:id/lines/resequence`；tsc 通过。
- 前端：`UnifiedTable.emptyRowFactory` 改 `(insertIndex)=>T` 按页传全局索引；`PurchaseQuote` 空行带唯一负占位 seq + `__insertIndex`，`commitCell` 空行升级改 `splice(__insertIndex)` 按位置插入（不再甩末尾）；`PickerCellEditor` 命中匹配后置位 `suppressTextCommitRef` 抑制失焦文本双提交；工具栏加「整理数据」按钮（`handleOrganizeData`：删空白行+重排）。
- 本次改的 5 个前端文件 tsc 0 错；其余报错为历史存量、未扩大处理。
- 文档：`功能文档/采购报价.md` 补"翻页位置化录入""整理数据"两节 + resequence 接口行。

## 升级「文档治理」Skill 为「重组机」（用户反馈 prompt 不够）

- 用户反馈：原技能只会"明确文档互相引用关系"，他想要的是**能重新组织文档内容、向上抽象整体、读代码对齐现实、把散落文档合并成简短清晰描述**的能力（新人一看就懂系统）。
- 重构 `SKILL.md`：**三步 → 四阶段**：梳理 / 检查 / **代码对齐** / **向上抽象与重组综合**。
  - 新增 Phase ③ 代码对齐：`scripts/extract_architecture.py`（stdlib）扫描真实代码 → 路由/页面/面板抽象/设计令牌/后端模块/API/引擎/Prisma 实体，产出《_代码事实_》事实底座，找"文档脱节 / 漏记复用能力 / 命名不一致"。
  - 新增 Phase ④ 向上抽象与重组综合：动作 A 生成《系统全景》（新人 2 分钟版）；动作 B 文档归并/精简方案；动作 C 逐篇重写。铁序：先顶层后细节。
- 扩展 `references/methodology.md`（加第 6 章代码对齐、第 7 章向上抽象与重组综合）、`references/templates.md`（加系统全景骨架 E、归并提议 F）。
- 实战演示（证明能力）：
  - 跑 `extract_architecture.py` → 真实代码事实：前端 20 路由、11 面板抽象、6 令牌；后端 49 模块、162 API、4 引擎；Prisma 47 model / 22 enum。
  - 产出 `系统全景.md`（新人视角，含分层全景/组成/关键抽象用在哪/核心数据流/文档地图/对齐发现）。
  - 产出 `文档归并提议_20260813.md`（基础档案合一 / 单据生命周期合一 / 三超长文档抽公共）。
- 扫描器 `scan_docs.py` 增强：排除导航/合成/生成类文档（`系统全景`/`文档归并提议`/`_代码事实_`/`_治理报告_`）参与断链源与孤儿判定，避免误报；复扫确认断链 0、孤儿 0。
- 已把技能定位更新进 MEMORY.md「已建项目技能」条。

## 补充「交付层组件抽象地图」（用户指出缺交付侧连接）

- 用户诉求：之前三张图讲了数据侧（档案层/状态机/流程），缺交付侧——在使用数据的地方用哪几种统一面板更新维护数据。强调"单位和供应商进价用同一种面板"，抽象统一后调一处全对，否则改来改去补窟窿。
- 产出：`交付层组件抽象指引.md`（项目根目录）+ 1 张「交付层组件抽象地图」可视化。
- 核心组织：交付层三大类——A 引用输入（DictRefField/各Picker）/ B 信息维护面板（MatrixTable列表类基座 + RecordExpandPanel矩阵类外壳）/ C 快速新建确认（QuickCreateConfirmDialog）。
- 关键澄清：**矩阵类不是另一种东西，是列表类的组合外壳**（RecordExpandPanel 内装多个 MatrixTable）。售价/进价/单位/联系信息/单价选择全是 MatrixTable 配置化实例 → 调一处全对。
- 含「数据形态判定流程」（一查即知用哪个）+ MatrixTable 复用清单 + 反模式清单。
- 定位为整合性地图：不重复《表格与交互规范·组件抽象》和《共享组件与公共能力》的组件细节，只组织成判定地图。

## 升级「重组机」补「洞察性推演」能力（用户二次反馈）

- 用户二次反馈：重组不能只机械归并/瘦身，要对**文档内容本身做推演**，从用户口述被 AI 固化的"确定性/限制性"描述中，洞察出"真正应该是什么"——这才是洞察级文档。
- Phase ④ 新增 **动作 D 洞察性推演**（最高价值）：并置片段→识别三类固化信号（字面绝对化/局部照抄违背全局范式/补丁当设计）→上推意图/套用顶层范式/复用通用机制→落地写回文档。好洞察判据=哪里被固化+组合证据+真正应该是什么+落地建议。
- 扩 `methodology.md` 第 8 章、`templates.md` G 章（洞察记录模板）；`scan_docs.py` 排除集加 `文档洞察记录`。
- 实战洞察演示：探子研读 11 功能文档+5 架构原则+哲学/规范，挖 6 个真实"被固化死规则、组合看应更优"实例，写成 `文档洞察记录_20260813.md`：
  1. canTransition 恒 true vs 归档终态冻结；2. 产品全名省略+Tooltip vs 名称列 wrap；
  3. 客户主表禁用空行/Modal vs Excel 末尾空行；4. 源码硬编码 SUPERADMIN vs is_system；
  5. 默认联系人"无论是否指定都写库" vs 缺省值注册表；6. 非标行 28 单位硬编码 vs 单位字典。
  每条含固化原文+信号+证据+推演+落地建议。复扫断链 0、孤儿 0。

## 行为纠偏：技能加「执行纪律」、清噪音、直接落地重组（用户再次反馈）

- 用户再次反馈两点：① 洞察结论不能直接写进文档/沉淀成"记录"文件，必须先**对话提问向用户确认**（对不对/要调整/补充），确认后再落地；② 别再新建"报告/提议/洞察记录/代码事实"类文档代替实际重组——那是新一层膨胀；技能认可后就**直接改现有文档**（合并/瘦身/重写/上调顶层/写回洞察），不要反复问"要不要做"。
- 给 `topdown-doc-governance` 技能铸入「执行纪律」：SKILL.md 新增一节（执行优先于产出文档/洞察必须先确认/结构性重组直接做/清理自身噪音）；methodology.md 加第 9 章；templates.md F/G 章标注"执行后不长期留存"。
- 清理自身噪音：把 `_治理报告_20260813.md`/`_代码事实_20260813.md`/`文档归并提议_20260813.md`/`文档洞察记录_20260813.md` 4 份演示文档移入回收站（`~/.Trash`，macOS 沙盒下 mv 成功、ls 不可见）；文档根仅留 `系统全景.md`+`溯源映射.md`+顶层三件套（用户要的向上抽象产物保留）。
- 直接执行结构性重组（不新建文档）：`功能文档/供应商管理.md` 两处"重复"收敛为引用——① L55 关联说明改指《数据规范·解耦三原则》；②「与其他功能的关联」整段（62 行）删掉对解耦/快照/停用的重述，精简为引用《数据规范》通用规则 + 保留供应商特有引用关系。仍待继续：保存逻辑/列表交互段对架构原则的重复重述。
- 下一步：用对话提问让用户确认此前挖出的洞察（canTransition 终态 / 客户主表 Excel 范式 / 单位字典）后再写回文档。

## 洞察确认翻车（用户纠偏，三条全废）

- 用 AskUserQuestion 呈现 3 条洞察，用户全否定，暴露推演根本错误：
  1. **状态机终态**：用户说落库即归档，但归档≠冻结不可改；数据无百分百不需改的，要改就正常改。错因：把"禁止后台批量改写历史单"和"用户不能修改单据"混为一谈。
  2. **客户/档案末端空行**：末尾空行只适合"使用数据的地方"（单据/订单视图批量录），档案管理不适用——产品关系复杂、档案量增长（末行=翻最后一页才能录，无法统计），故**档案管理统一走弹窗才合理**，与《哲学·约束4》不矛盾（约束4 指单据视图）。错因：没区分"单据视图 vs 档案管理"维度。
  3. **28 单位硬编码**：用户规则=档案类字典一律不预写数据、统一兜底（没填自动补一个；全局字典初始空、从单位字典选唯一；有档案从单位规则表选）。代码的固定枚举是**合法兜底**，非违规硬编码。错因：把"兜底"误读成"硬编码"。
- 教训已固化进技能：`methodology.md` 第 8.5 节"推演翻车反例" + 铁律（洞察被否定立即作废、绝不写回文档/改代码、翻车原因补回本节）。
- 行为层验证：本次"洞察先确认"流程生效——因先问，避免把 3 条错洞察硬落文档/代码，正合用户"确认对不对再调整"。

## 交付层抽象 v2：归一到「唯一列表基座」（用户指出 v1 没抽象到底）

- 用户二次纠偏：v1 分"A引用输入/B信息维护/C快速新建"三类仍不够——Picker(产品/客户/价格)和列表基座其实是一回事；引用输入跟快速新建应绑定（所有实时匹配处天然能快速新建，是一个功能两面）；引用输入用的面板就是列表基座，两种选择语义(插入/切换)只是参数。
- 用户关键洞察：①使用处叫"插入"(把数据送回外部单元格)；②自身处叫"切换"(切换自身显示+面板内直接维护，无插入动作，因信息维护面板本就可维护)；③价格选择也是列表基座，和售价/进价明细只差插入vs切换一个参数。
- 产出：`交付层组件抽象指引.md` 升级为 v2 归一版 + 1 张「唯一列表基座·参数化抽象矩阵」HTML 表格。
- 核心归一：**所有交付场景 = 唯一列表基座(MatrixTable) + 6参数**(输入框/选择语义/数据源/维护/快速新建/挂载)。10场景参数矩阵证明 Picker/DictRefField/MatrixTable实例全同源，差异只是参数取值。RecordExpandPanel=多基座Tab组合外壳。快速新建=插入+检索场景内置能力(走QUICK_CREATE_LAYERS)，不独立成类。

## 采购报价视图·选品"点整行不插入/参数错误"修复（building-quote-dev-collab 技能实战）

- 用户复现：单元格输入→检索列表→点记录→不插入且提示"参数错误"(后端 422)。
- 根因：`ProductPicker.confirmPick` 点整行走 `deriveDefaultUnit(sku)`，检索结果 `defaultUnitName/defaultUnitId` 可 null → `unit:''` 触发 `unit:min(1)` 422，或 `defaultUnitId=null` 静默 return。
- 修复：① `confirmPick` 点整行优先用 `pickBestUnit(row.units)`（已加载真实单位），未加载则 `await loadUnits` 拉取再选，兜底才退回 `deriveDefaultUnit` 且须 `unitId && unitName.trim()` 非空；② `PurchaseQuote.handleProductSelect` 加 `toNullableId` 把 0/''/null 的 brandId/specId/productId/unitId 转 undefined（前端 `?? undefined` 对 0 无效）。改动文件 tsc 0 错。
- 固化：质量闸门新增 **P-015**（选品落库单位须取自已加载真实单位并校验 unitId+unitName 非空；ID 落库前剥离 0/''/null）。
- 用户设计诉求（待确认范围）：选品面板是"选择类面板"→点行即插入；价格/单位展开面板是"编辑类面板"→需显式插入按钮。已请用户确认是否全系统统一此规则。

## 选品交互"全系统统一"对齐结论（用户选"全系统统一"）

- 派 Explore 代理盘点 frontend/src/shared/components 全部选品/列表面板：插入按钮只出现在编辑类面板(ProductPicker售价/进价/单位子面板、UnitManagePanel单位维护、PriceChoicePanel单价)，纯选择类面板(产品整行/客户/字典/枚举/单位简单列表/SuggestInput)全部点行即插入。
- 结论：系统本就符合用户规则(选择类=点行、编辑类=插入按钮)，无需要改代码的"不一致"；唯一真实缺陷是 ProductPicker 参数错误(已修)。未动一致代码(避免扩大影响面/瞎改)。
- 把规则用用户大白话固化为《表格与交互规范·交互统一铁律(选择类面板 vs 编辑类面板)》，理清文档"选择型"与用户"选择类"用词撞车；同步进技能质量闸门第四节"交互统一铁律"永久强制。

## 文档治理·三超长文档重复收敛（用户说"全部"→直接执行）

- 用户回"全部"= 按执行优先把三份超长文档里"重述《数据规范·解耦三原则》通用规则"的段落收敛成引用（不新建文档、不问用户）。
- 派 Explore 探子定位：三文档重复高度集中在"无物理外键+ID留存+名称快照+档案删除不影响历史"被多处原样内嵌。
- 已收敛（保留模块特有表名/字段，纯通用句改引用）：
  - 供应商管理.md：删除流程 bullet(L339)、contact_method 自由维护(L69) → 引《数据规范·解耦三原则》《表格与交互规范·枚举字段》。
  - 系统管理.md：通用设计原则·解耦原则段(L519-525) 整段收敛为引用+保留 audit_logs/authorization_codes/access_requests 表与字段。
  - 配货与成本.md：库存台账(L135)、数据模型设计原则 banner(L191)、追溯归档(L427) → 引《数据规范·解耦三原则》。
- 复扫：断链 0、孤儿 0、高重复对 0。三文档仍长(563/564/521 行)——主体为模块特有领域内容(字段表/算法/接口/权限码清单)，非重复，故不可再靠"收敛"瘦身。
- 结论：通用规则单一信息源已在《数据规范》达成；若想进一步缩短，唯一杠杆是用户拍板"合并模块文档"(原《文档归并提议》已按用户要求删除、不重建)。

## 落地「统一弹窗外壳」+ 配货 MatrixTable 扩展方案（用户要"全改"偏离项）

- 用户诉求：配货编辑弹窗是否也该用矩阵类/统一外壳实现；所有"点击出现面板"的地方外壳要统一(FloatPanel正下方)，内容才自定义；统一后位置/大小自动正确，套用即对。
- 代码核查结论：**FloatPanel(C60) 已是统一外壳**（左对齐锚点/正下方优先/滚动跟随/嵌套管理/smartPopupContainer挂载），**配货编辑面板已用 FloatPanel** ✓。偏离在内容侧+几处裸 Modal。
- 偏离清单 4 处，已改 3 处（tsc 0 错）：
  1. AllocationView 缺口处置 Modal → DsDialog(C07 统一弹窗) ✓
  2. AllocationSourcePicker 快速新建 Modal → DsDialog ✓（仓库/供应商二选一是特例，保留逻辑只换外壳）
  3. PricePicker 补全售价 Modal → DsDialog ✓（选价格类型+写入是特例，保留逻辑只换外壳）
  4. 配货编辑面板**内容**手写 grid+ALLOC_CSS，没用 MatrixTable —— 出扩展方案待确认（见下）
- 判断：AllocationSourcePicker(二选一)/PricePicker(写入非建档)是特例，强套 QuickCreateConfirmDialog(标准建档)要给它加二选一/写入参数污染标准组件，故只换 DsDialog 外壳，QuickCreateConfirmDialog 留给标准建档。
- **配货套 MatrixTable 扩展方案**（待用户确认后重构）：MatrixTable 固定列 `[switch点]名称 midCols 值 默认 操作`，配货需 `三态勾选|出库方|来源标签|数量|删除`。扩展 2 个可选参数：①`rowLeadingCell`(行首自定义列，承载三态勾选，替代固定switch点) ②`hideDefault`(隐藏默认星标列)。不需扩展：自动补齐行(rows调用方动态算传入)、数量失焦保存(onPriceChange+commitRow)、三态逻辑(cycleCheckState保留调用方)。风险中等，需验证三态勾选/自动补齐/缺口联动/超拿不破坏。

## 续做：配货 MatrixTable 扩展（压缩对话后续接，用户选此方向）

- 核查结论：该重构其实**已在代码里落地**（AllocationView.tsx + MatrixTable.tsx 均为未提交改动）。MatrixTable C29 v17.0 已具备 `rowLeadingCell`/`hideDefault`/`hideAddRow` 三参数；配货编辑面板已用 MatrixTable 承载「三态勾选(rowLeadingCell) / 出库方 / 来源标签(midCols) / 数量(priceRender 自定义输入框) / 删除」，ALLOC_CSS 仅保留三态按钮·数量输入框·来源标签等控件样式（非网格布局）。
- 修两处真实问题（源码唯一权威，跑 tsc 验证）：
  1. **tsc 报错**：AllocationView 用 MatrixTable 未传 `addNameCell`/`addPriceCell`（原必填），但设了 `hideAddRow`（新增行已隐藏）。→ 把两 prop 改 optional（仅 `!hideAddRow` 时渲染），向后兼容，tsc 通过。
  2. **索引错位 bug**（计划里"需验证三态勾选/删除不破坏"正中点）：`activeEditRows = editRows.filter(!removed)` 是紧凑下标，但 `cycleCheckState/updateRow/commitRow/removeEditRow` 全部按 `editRows` 原始下标操作；删除带 id 行（软删 `removed:true` 仍占位）后，后续可见行紧凑下标与 `editRows` 错位 → 勾选/数量/删除落到隐藏的软删行，功能坏掉。→ 新增 `activeOrigIdx` 映射（可见行→editRows 原始下标），所有回调用原始下标。tsc 通过。
- 验证：`tsc -b` 对 AllocationView/MatrixTable **0 错**；其余 DsButton/CustomerPicker/InteractionLayer/UnitManagePanel 等为历史存量报错，与本任务无关、未扩大处理。
- 文档：业务功能文档（配货与成本核算推演方案.md）无 `ALLOC_CSS`/手写 grid 等陈旧引用（本就是领域设计文档，不写前端组件内部）；组件用法已在 MatrixTable.tsx §A 注释落地（符合"业务文档写行为、不写组件内部"）。未另起文档。
