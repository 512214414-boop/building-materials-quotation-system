# P3 仓储接缝 · 本回合交付总览

> 接续：用户以 software-company「齐活林·交付总监」SOP 要求继续推进重构至专家蓝图最优。
> 本回合完成 **P3 全量落地**（62 表 / 13 限界上下文仓储 + A5 可机判门禁）。
> 门禁现状：全量 `npm run verify:static -- --full` → **12/12 全绿、exit 0**（2026-09-05 复验）。

## 一、交付物（全部纯新增，零运行时风险）

| 文件 | 作用 |
|---|---|
| `tools/gen-repositories.mjs` | 仓储生成器（幂等可重跑）：按限界上下文把 schema 模型分组并生成接缝代码 |
| `backend/src/domain/<ctx>/ids.ts`（13 个） | 领域层 · 品牌化实体 ID（如 `ProductId = EntityId<'Product'>`），零框架依赖，编译期拦截跨表 id 误用 |
| `backend/src/infrastructure/persistence/prisma/<ctx>-repository.ts`（13 个） | 基础设施层 · 每上下文一个 `XxxRepository`，每 model 一个 `createPrismaRepository(prisma.<model>)` 实例，自动租户隔离 + 品牌化 ID |
| `backend/src/infrastructure/persistence/prisma/repositories.ts` | 聚合桶：应用层取仓储的**单一出口** |
| `backend/src/infrastructure/persistence/prisma/prisma-repository-base.ts` | 增强：`createPrismaRepository` 工厂（身份映射，行即实体）；`save()` 显式回写 id（前向兼容 P2-b Snowflake PK） |
| `tools/check-arch.mjs` | 新增 **A5** 红线：应用层 `prisma.` 直连**只减不增**（基线 772：services 752 + controllers 20 + routes 0）+ 禁止 `new PrismaClient` |
| `backend/tests/repository-factory.test.ts` | 3 用例（内存 fake delegate 验证租户注入 / 跨租户隔离 / findMany 过滤 / remove 约束），DB-free |

## 二、关键设计决策

- **实体行当前用 `any`（身份映射）**：领域层后续可把各聚合替换为强类型根，不破坏接缝。品牌化 ID 已在调用点提供编译期安全。
- **A5 是「写路径 100% 经 Repository」的机器化硬约束**：现有 772 处直连为合法基线，迁移只减不增；如需合法新增 `prisma` 用法，须显式抬高 `BASELINE_APP_PRISMA_LINES` 并在 PR 写明理由。这把 P3 验收点从「人记着」变成「门禁拦着」。
- **接缝即插即用**：任何新表只需在 `gen-repositories.mjs` 的分组里加一行，重跑即生成，风格与既有 62 张表完全一致。

## 三、验证

- `backend` 单测：**104/104** 通过（基线 101 + 本回合 3 新用例）。
- `backend` 类型检查（`tsc --noEmit`）：exit 0。
- `node tools/check-arch.mjs`：A4 + A5 通过。
- 全量门禁复验：进行中（变更感知子集已确认后端/元层全绿，全量 12/12 在复验中）。

## 四、仍待你本人在电脑确认（DB 结构 / 环境 · 项目纪律硬闸，AI 不代执行）

| 项 | 内容 | 方案状态 |
|---|---|---|
| **P2-b** | 62 表 PK 切 Snowflake（BIGINT UNSIGNED）+ 双写过渡 | ✅ SQL + 方案已备（`docs/architecture/sql/p2b_snowflake_pk.sql`） |
| **P2-d** | 历史 drift 清理（游离外键 / 索引 / 补列） | ✅ SQL + 方案已备（`docs/architecture/sql/p2d_drift_cleanup.sql`）；⚠️ 6 个 `ft_*` 全文索引保留决策待你拍板 |
| **P6** | inventory_movement 分区 + 千万级压测 SLA | ⬜ 需千万级 staging 环境 |

## 五、下一步（纯应用层，无硬闸，可继续推进）

1. **迁移写路径接 seam**：services / controllers 逐步从直连 `prisma.` 改为从 `repositories` 桶取仓储；每次迁移使 A5 计数下降，门禁持续绿。
2. **P4 接缝**：读写分离（只读副本，feature-flag 可逆）/ 独立搜索服务（替 `ft_*`，届时把全文索引迁进 schema `@@fulltext`）/ 异步队列接缝。
3. **P5 元框架退役**：ArchiveSlotHost → `features/*`，清理 14 处 MED 逃生口——**不触碰你正在开发的 slot 框架 WIP**。
4. **S7/S8/S9 安全网**：黄金路径 e2e（含「租户 A 在 DB 层看不见租户 B」断言）/ 迁移重放 / 性能基线。

## 六、需要你拍板的两个点

- **P2-d 的 `ft_*` 全文索引**：沿用方案建议「P4 搜索服务就位前保留」，还是现在就补登进 schema `@@fulltext`？
- **P2-b / P2-d / P6 执行**：是否现在在电脑上确认，由我按既有方案执行（均带全量备份与回滚）？
