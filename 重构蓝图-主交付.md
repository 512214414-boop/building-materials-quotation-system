# 建材进销存平台 · 标准重构蓝图（主交付 · 决策版）

> 编制：齐活林（交付总监）/ SoftwareCompany 团队 — 高见远（架构师）出目标设计、严过关（QA）出重构安全网
> 基线：2026-09-05 仓库实测现状
> 配套全文：
> - 架构设计详版：`docs/architecture/目标架构设计.md` + `docs/architecture/diagrams/*.mermaid`
> - 质量安全网详版：`重构期质量安全网方案-Edward.md`

---

## 0. 一句话定位

你要的不是「再堆功能的表工具」，而是一套**建材行业进销存一体化平台**（采购→库存→销售开单→配货→收付款→售后全链路），远期要扛住**千万级 SKU** 的生产运行；业务用户用中文业务字段和主外键关系直接在表格里编辑数据。

**现在的问题不是"业务想不清"，而是"工程房子没按正常方式盖"**：正确性那层（解耦、快照、加权均价、审计、千万行索引）其实做对了，但**分层、目录结构、物理数据模型、领域模型、生产基建、质量门禁**这几样"正常项目该有的地基"长期缺失或后补。下面这份蓝图就是"按正常软件项目的标准方式"该有的样子。

---

## 1. 你现在 vs 正常项目：差距清单（六类）

| 维度 | 现在（实测） | 正常项目应有的目标态 |
|---|---|---|
| **分层** | 后端扁平 `controllers/services`，无领域分层；前端 ArchiveSlotHost 统一渲染、骨架与业务强耦合 | 后端 DDD 四层 + 横切（依赖单向、编译期可校验）；前端 monorepo + `features/*` 业务模块 |
| **目录** | 21000 文件堆一起；`*.generated.ts` 与手写字混放 `shared/config` | 生成物物理隔离（`@platform/meta/generated`，codegen 独占）；模块边界 arch-lint 强制 |
| **物理模型** | `P+时间戳+随机` 业务码 + `autoincrement` 主键；30+ 非线性迁移、长期 `db push`；SQLite `dev.db` 残留 | Snowflake 64 位聚簇主键 + 独立业务单号；v31 起线性版本化迁移、禁 `db push`；纯 MySQL 8 |
| **领域模型** | 元框架驱动、聚合边界模糊；快照半采纳 | 显式聚合根/实体/值对象 + 明确一致性边界；逻辑 ID 引用 + 快照值对象（固化你的 v11.0） |
| **基建** | 单笔记本 + cpolar；无 Docker/CI/staging/备份DR/搜索/队列/可观测 | Docker Compose → k8s；CI 卡合入；staging；每日备份+DR；Redis/ES/MQ 接缝预留 |
| **质量** | verify 9 门 8 过 1 红（S1 类型检查）；14 处 MED 信号；曾"假绿"事故 | 9 门全绿；MED=0；契约/e2e/迁移回放/性能 4 道新增门禁；退出码即真理 |

---

## 2. 目标架构：分层 + 目录结构

### 2.1 后端标准分层（DDD 四层 + 横切）

铁律：**领域层零框架依赖**；基础设施层实现领域层定义的仓储接口（依赖倒置）。依赖方向单向：`interface → application → domain ← infrastructure`，编译期可校验。

```
backend/src/
├── interface/      # 接口层：controllers / routes / ws / middleware(auth,tenant,tracing)
├── application/    # 应用层：purchase / sales / inventory / pricing / ledger（用例编排+事务边界）
├── domain/         # 领域层：product / inventory / order / pricing / ledger / partner / shared / tenant（零框架依赖，可独立单测）
├── infrastructure/ # 基础设施层：persistence(Prisma仓储) / search(ES缝) / messaging(队列缝) / datasource(主从/分片缝)
├── crosscutting/   # 横切：config / audit / observability
└── generated/      # 代码生成物（OpenAPI client 等），codegen 独占
```

### 2.2 前端标准分层（pnpm monorepo）

```
frontend/
├── apps/                 # 可部署应用壳：auth / staff / customer（仅路由装配+全局布局）
├── packages/@platform/   # 平台库
│   ├── meta/             # 元框架，生成物隔离在 generated/（codegen 独占）
│   ├── ui/               # antd 封装 + 业务控件（ArchiveSlotHost 降级为可选渲染器）
│   ├── domain/           # 类型 / DTO / API 客户端
│   ├── engines/          # 定价引擎等纯逻辑（Vitest）
│   └── shared/           # hooks / stores / utils / styles
└── features/             # 业务特性模块（product/purchase/sales/inventory/fulfillment/ledger）——元框架退役后的落点
```

**解决"21000 文件堆砌 + 生成物混放"的三招**：① 生成物物理隔离、codegen 独占写；② pnpm workspace + 强化 `arch-lint` 强制依赖方向；③ 元框架降级为兼容层，特性模块逐步用标准 React 组件替代。

---

## 3. 物理数据模型（生产级 · 千万级 SKU）

### 3.1 主键：Snowflake 取代反范式方案
- 现状坑：`P+时间戳+随机` 业务码（非单调→B+树页分裂/随机IO/不可分布式）和 `autoincrement`（单点计数器/插入热点/分库后不全局唯一）都反范式。
- 目标：所有实体用 **Snowflake `BIGINT UNSIGNED`** 作聚簇主键（41bit 时间戳 + 10bit 节点 + 12bit 序列），趋势递增、全局唯一、可分布式、紧凑 8 字节。
- 人类可读业务单号（如 `CG-20260905-000123`）降为**独立唯一列**（非 PK），可排序、可审计，不参与关联。
- 备选 UUIDv7（无时钟依赖、跨语言），但 128bit 占空间大、聚簇略慢，仅在强多语言/无中心 worker 时切换。

### 3.2 核心表（中文业务字段，逻辑引用、无物理外键）
- **主数据**：品牌、供应商、客户、商品(SPU)、SKU、仓库、字典（单位/品牌等）
- **业务单据**：采购订单/行、销售订单/行（行内嵌**名称/品牌/单价快照**）、配货单、售后单
- **库存**：`inventory`（余额+加权均价）、`inventory_movement`（流水，十亿级）
- **价格**：价格表、价格行
- **往来账**：应收、应付、收款单、付款单
- **审计/事件**：`audit_log`、`change_log`、`outbox`（领域事件可靠投递）

### 3.3 分区 / 分片 / 索引 / 接缝
- `inventory_movement` 按 `created_at` **RANGE 月/季分区**（十亿级流水核心）；`inventory` 按 `tenant_id` LIST 或 `warehouse_id` HASH 打散热点。
- 复合索引以 `tenant_id` 为最左前缀（多租户过滤），如 `inventory: UNIQUE(tenant_id,sku_id,warehouse_id)`、`inventory_movement: INDEX(tenant_id,sku_id,created_at)` 等。
- **四个接入缝（Seam）**：读写分离（`Repository`+`Router`，远期写主读从）；多租户（`TenantContext` 自动 append `tenant_id`）；独立搜索（`SearchService` 接口，先 DB 后 ES 可回退）；异步队列（`DomainEventBus`+`outbox`，先 in-proc 后 MQ）。

---

## 4. 领域模型（聚合与边界）

- **核心聚合根**：Brand/Supplier/Customer（主数据）、Product(SPU)、**SKU（独立聚合，库存/价格/订单都围绕它）**、Inventory（含加权均价领域服务）、PriceList（含 PriceLine 值对象）、PurchaseOrder/SalesOrder（含行 + 业务快照值对象）、Delivery、AccountsReceivable/Payable、AfterSale。
- **一致性边界内（同事务）**：PO↔POLine、SO↔SOLine、PriceList↔PriceLine、Inventory 余额变更↔Movement 记录。
- **仅 ID 引用 + 快照（无物理 FK）**：订单引用 供应商/客户/SKU（行内嵌业务快照值对象，主档变不影响历史单据）——这正是你 v11.0 全域解耦的资产，提升到领域层固化。
- **正确性资产迁移映射**：加权均价→`Inventory.MovingAverageService`（原样迁入+单测）；单据快照→`OrderLine.snapshot` 一等公民；审计→`crosscutting/audit` 切面；千万行索引→直接纳入 baseline 迁移。

---

## 5. 增量重构路线图（不中断线上 · 每阶段机器验收）

> 严禁大爆炸重写。阶段可独立上线、部分并行；每阶段带**可机器判定**验收点，未达标不得进下一阶段。

| 阶段 | 目标 | 周期(示意) | 机器验收点 |
|---|---|---|---|
| **P0 地基** | Docker Compose、退役 SQLite、staging、每日备份+DR、CI 卡合入、**修 S1 假绿（当前为红）** | 1–2 周 | `docker compose up` 起 MySQL；CI 在 PR 触发；**S1 fe-typecheck 修绿（`tsc --noEmit --incremental false`）**；`dev.db` 删除、`.env` 指向 MySQL；最小安全网 8 项全绿 |
| **P1 脚手架** | 后端按 §2.1 重组；前端 monorepo 强化；生成物隔离；引入 `SnowflakeGenerator` | 2–3 周 | `pnpm -r build` 通过；arch-lint 通过；Snowflake 100 万次零冲突且单调递增 |
| **P2 物理模型** | v31 起线性迁移；全表加 `tenant_id` + Snowflake PK（双写 `legacy_code` 过渡）；退役 `db push` | 3–4 周 | `prisma migrate status` 全绿；部署脚本无 `db push`；新 PK 为 `BIGINT UNSIGNED` |
| **P3 领域层** | 抽聚合/实体到 `domain/`；定义 `Repository` 接口；infra 实现 + 读写分离缝；`outbox` + 事件 | 4–6 周 | 写路径 100% 经 Repository 接口；`outbox` 有事件产出；S4 单测绿；domain 不依赖 infra |
| **P4 接缝落地** | `TenantContext` 全链路；`SearchService` 接 ES（可回退 DB）；事件总线换 Redis Stream/RabbitMQ（与 P3 并行） | 4–6 周 | 多租户查询自动带 `tenant_id`；搜索可切回 DB；队列消费 `outbox` 事件 |
| **P5 元框架退役** | 档案页拆为 `features/*`；meta 降只读兼容层；修 S1 红；清 14 处 MED | 3–4 周 | `ArchiveSlotHost` 引用数=0；verify 9 门全过；MED=0 |
| **P6 规模压测** | `inventory_movement/inventory` 分区上线；读副本接入；1000 万 SKU SLA 压测（与 P5 并行） | 2–4 周 | 分区裁剪 `EXPLAIN` 通过；写入 P99 达标；读副本承载 >70% 读 |

> **排期一致性注记**：当前 S1 fe-typecheck **实测为红**（与历史"假绿"事故同源），属现有阻断项，必须由 P0 止血门禁**先修**，不能等到 P5 才首次要求绿。路线图已据此把"S1 修绿"显式纳入 P0 出口标准。

---

## 6. 重构期质量安全带（不把系统改坏）

四大支柱 + 10 道门禁（在原 9 门基础上新增 S6~S10），红线：**真实、可机判、非 0 即失败、禁止 AI 自评算通过**。

- **S6 接口契约**：后端 `openapi.json` 入库，PR diff 阻断 breaking 变更（字段删除/重命名/类型收窄/必填新增）。
- **S7 黄金路径 e2e**：优先 API 层（开单→配货→收款），浏览器层只跑 3 条核心流、断言真实 DOM，不复用"15 个过期 URL 永远 exit 0"的旧脚本。
- **S8 数据迁移回放**：空库 `migrate deploy` → 加载子集 → 一致性断言（行数/checksum/业务不变量）；**无物理 FK 模型以"孤儿行扫描=0"为安全底线**；Schema 哈希门禁；禁 SQLite 残留。
- **S9 性能基线**：staging 千万级 SKU 压测存基线，回归阻断 `p95 > 基线×1.2` 或错误率>0。
- **S10 信号清零**：14 处 MED 清零或带 owner+截止的白名单。

**重构前最小安全网 8 项（齐备才可动核心代码）**：① verify 去假绿（S1 全量 `--incremental false` + `ranIds==wantedIds` 强制 + 禁 `|| true`）② 契约 diff S6 ③ 迁移回放骨架 S8 ④ 黄金路径 API e2e S7 ⑤ 14 MED 门禁化 S10 ⑥ signal-scan 进 CI 阻断 ⑦ 性能基线 S9 ⑧ SQLite 残留门禁。

---

## 7. 现在就能拍板的下一步（建议优先级）

1. **先拍板 P0 的最小安全网**（8 项，投入小、ROI 最高）——尤其修 S1 假绿 + 立契约 diff + 迁移回放骨架，这三项直接拦住"静默改坏"和"数据丢失"两类最致命风险。
2. **确认主键与多租户基线决策**（见详版 Trade-off T1/T2）：Snowflake PK、shared-DB shared-schema 多租户起步 + seam 预留升级路径。
3. **DB 迁移需你本人确认**：P2 起涉及真实 schema 演进（加 `tenant_id`、PK 迁移、退役 `db push`），按项目规矩需你在电脑上确认，手机端无法审批——届时我会先给可回滚方案再等拍板。
4. **元框架去留**：建议"降级为兼容层、逐步退役"（兼顾不中断业务与标准化），而非全保留或全重写。

> 详细设计、Mermaid 分层图/ER 图/路线图、门禁脚本口径见两份配套全文。本文件为决策版摘要。
