node_modules/
dist/
.env
.env.local
*.db
uploads/
.uploads/
screenshots/
e2e_browser/screenshots/
.DS_Store
backup_*/
.trae_*

# 项目掌控台：由 tools/gen-boss-view.mjs 生成（含实时探测状态），不入库
项目掌控台.html
项目掌控台.md

# 门禁运行产物：每次 npm run verify 都重写一次，入库只会污染 diff
# 判定依据是退出码，不是这个文件——掌控台消费它，但不进版本库
verify-report.json
