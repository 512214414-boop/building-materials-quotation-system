# 架构原则·元模型运行时（元模型驱动全栈行为）

> **职责**：定义"元模型即运行时"这一架构主线的唯一权威——所有表业务的行为差异落在登记表上，前后端统一解读，自动处理渲染 / 确认 / 校验 / 检索 / 门禁 / 引用 / 快照 / 权限 / 审计 / 统计。改行为 = 改登记表 = 改数据，不是改代码。
> 上游方法论：L0「表功能元模型」（九视角）→ 文档落地「文档可视化 · 集合体架子」→ 本文（代码落地主线）。
> 现状盘点：2026-09-01。

## 一、愿景与四条原则

**一句话**：一份登记表，前后端统一解读；维度是行为的切片；新增维度 = 全站获得新能力。

1. **行为也是数据**：每个行为问题（这格能不能点、点开怎么确认、谁有权改、是否快照、空值补什么、记不记审计）都是一个"维度"，登记表逐字段声明。业务差异落在登记表上，不落在代码分支里。
2. **一份真相源**：前端渲染、后端校验/保存/引用/权限/审计/统计，从同一份声明解读。出现两套真相源（前端一套后端一套）＝违规。代码里出现"如果表是 X 就这样"的分支 = 该进登记表了。
3. **解读器按维度驱动**：新增维度 = 登记表加一列 + 解读器加一个行为处理。禁止散落改各视图各服务。
4. **先文档后代码**：本文是主线；每补一个维度，先在文档侧「表功能元模型」补齐视角，再动登记表与解读器。

## 二、已证明的样本：权限

权限已经是声明式双端登记：

| 端 | 登记在哪 | 派生什么 |
|---|---|---|
| 后端 | `backend/src/types/index.ts` → `VIEW_PERMISSION_MATRIX`（角色×视图） | 路由守卫 `requireViewPermission` 自动校验 ro/rw |
| 前端 | `apps/staff/menu.config.ts`（navPath + permissionKey） | 权限树、菜单、按钮显隐自动派生 |

**结论**：双端登记表可行、登记表驱动的"行为"可工作。剩下五项（门禁 / 快照 / 审计 / 统计 / 检索层级）照同一思路推广即可——不是创新，是把已验证的模式复制到没做的行为上。

## 三、行为切面清单（现状盘点 2026-09-01）

| 切面 | 行为问题 | 现状落点 | 声明式？ |
|---|---|---|---|
| 渲染 | 这格显示成什么 | 前端 `entityRelations.ts` + `deriveTableColumns` | 半（E 呈现全，G 部分） |
| 确认 | 点开怎么确认、确认才写 | 前端 `confirmStrategy` 三档（direct/dialog/global） | 半 |
| 检索 | 怎么被找到、搜哪层 | 前端 `suggestField`；检索层级（名称/执行标准/渠道）散落 | 半 |
| 门禁 | 前置未满足点开给什么提示 | 字段级 `gate` → `resolveGate`（格子能不能点） | **✅ 是** |
| 操作守卫 | 动作前不满足拦不拦、给什么提示 | 动作级 `actions.guard` → `resolveGuard`（36 动作，8 类判定） | **✅ 是** |
| 引用 / 建档 | 名字怎么幂等解析、空补什么 | 后端 `registry.ts`：`quickAdd / ensureByName / ensureByParent / resolveRef` + `RegistryDef` | **✅ 是** |
| 关系读写 | 子表怎么挂、视图怎么映射 | 后端 `*Relations.ts`（customer / supplier / warehouse） | 半 |
| 快照 | 单据行存 ID+当时值 | **每个 service 手写 `resolveLineSnapshots`** | **否** |
| 权限 | 谁能看、谁能改 | 双端登记表派生 | **✅ 是** |
| 审计 | 谁做了什么要记 | ~10 个 controller 手动 `req.audit(...)` | **否** |
| 统计 | 报表口径是什么 | `opsReportService` 硬编码聚合/SQL | **否** |

**结论**：10 个切面，2 个全声明式（权限、引用建档），5 个半声明式，3 个全手写（门禁 / 审计 / 统计）+ 快照手写。全站每个表功能动一次，这 7 个半/否的切面各改各的——这就是"改功能要动五处"的来源。

## 四、九视角 × 代码现状（缺口清单）

| 视角 | 前端 entityRelations | 后端 registry / *Relations | 缺口 |
|---|---|---|---|
| A 身份（层 / 父 / 去重键） | `fieldClass` A/B | `uniqueKey` global/parent | 两套命名，未统一 |
| B 语义（说明 / 必填 / 空补） | 无 | `defaults` + `resolveRef` 默认名 | 前端无；后端有 |
| C 来源（值来源 / 字典） | `dictKind` | registry 引用解析 | **值来源分类**（own/dict/dictVia/config/derived）两侧都缺 |
| D 关系（方向 / 基数 / 删除） | `relations`（field/to/type 弱） | `include` 映射 | 删除行为、携带字段缺 |
| E 呈现（列 / 顺序 / 场景） | **✅ 全** | 无 | 后端无 |
| F 行为（确认 / 门禁 / 快建） | `confirmStrategy` | `quickAdd` | **门禁维度（已补，见落地进度）** |
| G 检索（层级 / 筛选） | `suggestField` | 无 | **检索层级缺** |
| H 历史（快照） | 无 | 无 | **全缺（两侧都无）** |
| I 权限 | 无（menu.config 承担） | `VIEW_PERMISSION_MATRIX` | 双端两处登记，未同源 |

## 五、统一真相源设计

目标：一份 `data-source/entity-meta.yml`，生成前后端两份登记表，从根上消除"改一处要动五处"。

```
entity-meta.yml（唯一真相源：实体 × 字段 × 九视角维度）
   ├─→ 生成 frontend/src/shared/config/entityMeta.generated.ts   （渲染/确认/检索/门禁）
   └─→ 生成 backend/src/services/generated/entityMeta.generated.ts      （引用/建档/默认值/快照/校验）
```

设计原则：

1. **生成区与手写区物理隔离**：`*.generated.ts` 禁止手改；业务特有的 override 写在旁边的 `*.override.ts`，生成器不覆盖。
2. **一个实体先做通（最小闭环）再推广**：category 已跑通（阶段 A）；推广到 product / supplier / order 前先对拍。
3. **对拍后切换**：生成的登记表先与现有手写版并存、跑同一批用例比对输出；一致才切流量，禁止直接替换。
4. **快照与审计与统计逐步进**：阶段 C/D 起，快照字段映射、审计 action、统计口径也进同一份源。

## 六、执行引擎设计

解读器 = 读登记表，按维度分派行为（这是"统一解读"缺的那块核心，现在只有 deriveTableColumns、registry 函数、resolveGate 三小块）：

**前端渲染器**（读字段维度）：
```
值来源(valueFrom) → 单元格形态（字典检索/确认层/纯输入/只读/配置面板）
              → 确认策略(confirmStrategy) → 门禁提示(gate→resolveGate) → 场景截断(scenes)
              → 操作守卫(actions.guard→resolveGuard)：动作前拦截，前置不满足 → 提示不执行
```

**后端处理器**（读字段维度）：
```
引用解析(resolveRef) → 空值补全(defaults) → 快照取值(snapshotFrom) → 校验(required/unique)
                                    → 审计(action) → 统计口径(indicator)
```

## 七、落地路线图

| 阶段 | 做什么 | 价值 | 风险 |
|---|---|---|---|
| **A 统一真相源最小闭环** | 挑 category：`entity-meta.yml` → 生成前端 + 后端登记表，对拍行为一致 | 证明"一份源两处解读"可行 | 中 |
| **B 补门禁维度** | F 门禁（`gate`）进登记表，解读器 `resolveGate` 出提示 | 门禁一处改全局生效 | 中 |
| **C 快照声明** | H 快照字段映射进登记表（`snapshotFrom`），替代 `resolveLineSnapshots` 多版本 | 单据行加字段不用各 service 找一遍 | 中 |
| **D 审计 / 统计声明** | 审计 action 登记 + 指标口径登记 | "记什么 / 算什么"一处定义 | 中 |
| **E 全量推广** | 所有实体迁到生成版，删手写版 | 全站统一真相源，改一处五面自动跟 | 高（最后做） |

**验收判据（每阶段完成时）**：改一个字段只动 `entity-meta.yml` 一处，前端列表、后端校验、宽表同步、文档侧集合体架子四处自动跟；`node tools/check-docs.mjs` 与前后端构建零报错。

## 操作守卫 DSL · 八类判定（resolveGuard）

声明在 `data-source/entity-meta.yml` 顶层 `actions` 段；解读器 `resolveGuard`。与格子门禁（gate）同族——**前置不满足 → 拦截 + 给原因**，区别只在作用域：gate 拦「格子」（一行一列，数据状态决定），guard 拦「动作」（表单/行/状态，动作上下文决定）。

**判定顺序（先命中先拦）**：`requires → requiresAny → minSelected → numbers → rowNumerics → formats → states → rowUnique`

| 类 | 拦什么 | 声明形态 | 实例 |
|---|---|---|---|
| `requires` | 表单字段必填（组内缺任一） | `requires: [{ fields: [...], reason }]` | `请选择供应商` |
| `requiresAny` | 至少填一个（全空才拦） | `requiresAny: [{ fields: [...], reason }]` | `姓名与联系方式至少填一个` |
| `minSelected` | 有效行数下限 | `minSelected: { n, reason }` | `请先选品，再确认入库` |
| `numbers` | 表单数值校验 | `numbers: [{ field, op: gt/ge/lt/le, ref, reason }]` | `金额必须为正数` |
| `rowNumerics` | 每行数值校验（refField 引用行内字段） | `rowNumerics: [{ field, op, ref\|refField, reason(支持{占位}) }]` | `「{productRef}」数量必须大于 0`、`不能超过剩余可退量 {remaining}` |
| `formats` | 字段格式（正则） | `formats: [{ field, pattern, reason }]` | `请输入登录账号（电话或微信）` |
| `states` | 状态机（字符串/布尔） | `states: { allow, forbid, reason }` | `请先完成 V9 店长汇总确认` |
| `rowUnique` | 集合内组合唯一（查重） | `rowUnique: [{ in, keys: [{field, against}], except?, reason }]` | `供应商「{newName}」已存在，不可重复` |

**边界**：判定数据能描述为「字段 / 行数 / 状态 / 角色」→ 声明化（含集合查重 `rowUnique`）。依赖**多请求时序**（await 后校验响应）、**派生计算 / 聚合布尔**、**条件分支 / 条件文案**、**交互约束**（picker 禁手输）→ 例外，登记 `data-source/guard-exceptions.md`，**禁止静默新增散落守卫**。

**接入范式**（与 gate 相同）：页面按钮 / 提交函数开头一行 `resolveGuard('action_key', ctx)`，`ctx = { form, rows, state, collections }`；返回 `null` 放行，否则弹提示返回。判定结构 + 提示语全在 yml，页面只提供值。

### 落地进度

| 阶段 | 状态 | 产出 |
|---|---|---|
| A 统一真相源最小闭环 | ✅ | `data-source/entity-meta.yml`（category）+ `tools/gen-entity-meta.mjs` → 前后端双登记表生成，后端对拍与手写 `CATEGORY_REGISTRY` 语义一致 |
| B 门禁维度 | ✅ 通用层已接 | `gate` 声明进 yml（product 规格备注真实用例：requires specModel）+ `resolveGate` 解读器 + `gateReadonlyWhen` 辅助（4421fb2，登记表 gate → `readonlyWhen`）。各页面门禁从手写字符串换 `gateReadonlyWhen` 即收尾 |
| C 快照声明 | ✅ 已接入 | `snapshotFrom` 进 yml（document_line 5 字段，含 `via: product.categoryId` 级联反查）+ 生成 `SNAPSHOT_MAP` + 解读器 `resolveSnapshots.ts`；**documentLineService.resolveLineSnapshots 已改调解读器**（快照映射唯一来源 yml，加快照字段只改 yml），backend 全量 tsc 零错误 |
| D 审计 / 统计声明 | ✅ 已接线 | 审计：82 个 action 登记进 yml，`auditLogger` 中间件校验未登记 action 警告仍记录（留痕优于审批）；统计：`INDICATORS` 5 口径 + 一致性测试（`tests/indicators.test.ts`）|
| E 全量推广 | ✅ 已完成 | 后端字典（CATEGORY/BRAND/PRICE_TYPE）从 `REGISTRY_GENERATED` 取（927826b）；前端界面列迁移（6e62cf9）：35 列 + 8 关系进 yml，`entityRelations.ts` 改 re-export 生成物（类型抽到 `entityRelations.types.ts`）。对拍 4 实体一致 + 全量 62 测试零失败 + 前端 build 通过。**改界面列 / 建档语义只改 `entity-meta.yml`，框架外手写残留清零** |
| F 操作守卫 | ✅ 已接线 | `actions.guard` 进 yml（动作级，8 类判定）+ 解读器 `resolveGuard`（`frontend/src/shared/config/resolveGuard.ts`）。**36 个动作全声明，页面引用与 yml 一一对上（无缺失无孤儿）**；散落 `message.warning` 守卫从 70+ 处收到 25 处，**剩余全为已登记例外**（`data-source/guard-exceptions.md`）。DSL 八类见下节 |
