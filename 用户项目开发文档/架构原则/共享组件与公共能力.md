# 共享组件与公共能力（SSOT 索引）

> **定位**：前端共享层（`frontend/src/shared/`）的**能力清单**。任何需要复用组件、函数、配置、工具的场景，**先查本清单**再动手——同一种能力只有一种实现（见《代码与工程规范》），禁止重复造轮子、禁止各页面另写等价实现。
> **权威出口**：组件统一从 `shared/components/index.ts` 导出；API 统一从 `shared/services/api/index.ts` 导出；业务页面禁止裸用 Ant Design 基础组件。
> **更新规则**：新增/修改共享能力时，同步更新本清单（代码与文档必须同步）。

### 如何查看共享组件（标识模式）

每个共享组件分配**稳定编号（C01+）**，开启**标识模式**后**全量直接显示，无需拾取**：

- **开启方式**：任意页面右下角「c」圆形按钮（点击变黄「C」即开启，再点关闭；开关状态本地记忆）
- **显示形式**：每个共享组件实例被绿色虚线框框住（body 最顶层浮层，弹窗/抽屉内组件也完整标识、不被遮罩压暗、不被容器裁切），并直接显示编号——
  - **叶子组件**（内部未嵌套共享组件，如按钮/输入框）：编号以**居中水印**显示在元素上——**红底白字粗体**（#F65A5A 红底 + 白字 + 加粗 + 白描边），辨识度最高，页面背景再深再花也清晰可读；不占布局、不遮内容
  - **容器组件**（内部嵌套了共享组件，如骨架/表格/抽屉）：编号显示在**框内左上角小标签**（同红底白字），与内部子组件的居中水印天然错位 → 大组件套小组件不会互相遮盖
- **同编号 = 同一共享组件**：界面多处出现 `C01` 水印 = 它们用同一个共享组件；右下角**聚合清单面板**还列出 `编号 · 名称 × 实例数`（按实例数降序）汇总
- **定位具体实例**：点击清单某行 → 该编号全部标识**琥珀黄呼吸高亮**，再点取消（非必需，仅辅助定位）
- **聚焦同类**：点击页面上的某个编号标识 → **只显示该编号的全部实例（同类都留），其余编号全部隐藏**（琥珀黄呼吸高亮），一眼看清这个共享组件被复用在哪些地方；再点同一标识或按 **ESC** 恢复全部显示
- **悬浮任一元素** → 显示 `编号 · 中文名` + 分组
- **判断标准**：界面元素带框和编号 = 已抽象共享组件实例（哪怕只是一个小图标按钮）；不带 = 未抽象，可考虑抽象后复用
- **沟通方式**：说「C05 标签」/「C20 价格展开面板」即可精确定位，无需贴代码路径
- 纯逻辑层（无 DOM 根，如 `InteractionLayer`/`RecordFieldColumn`/`SkuPriceColumns`/`PermissionGuard`/`SaveStatusProvider`）无法在界面标记，表中以「无独立 DOM」注明

---

## 一、设计系统（styles/）

| 文件 | 职责 | 规则 |
|------|------|------|
| `tokens.css` | 设计 token（radius/spacer/font/bg/text/border/status/shell 变量） | 颜色/间距/字号/半径**一律走 token**，禁止硬编码像素/色值 |
| `elements.css` | 元素级 `.ds-*` CSS 类 | 处理 antd 内部 DOM 无法通过 props 控制的样式 |
| `antd-theme.ts` | antd 主题（defaultAlgorithm，纸面/墨色，品牌色 #1f4d3a） | 全局唯一主题配置；hex 与 tokens 对齐，禁止 `var(--*)` 进 algorithm |
| `shell-constants.ts` | 骨架行盒子常量（DS_SHELL_INLINE_BTN 规范、行高 24px） | 骨架统一走 shell 变量 |
| `print-documents.css` | 单据打印样式（@media print 隐藏应用 UI） | — |

**内联边框**：`style` 里只用 `borderWidth` / `borderStyle` / `borderColor`，禁止 `border` 简写与 `borderColor` 混用（React 重绘会报警，边框可能闪错）。

---

## 二、基础组件库 Ds*（components/）

| 编号 | 组件 | 用途 | 何时用 |
|------|------|------|--------|
| C01 | `DsButton` | 按钮（primary/secondary/ghost/danger + sm/md/lg）。sm 与输入框同一行高 20 | 所有按钮 |
| C02 | `DsInput` | 输入框（plain/name/price/embedded 变体 + clickToEdit）。sm = 20px / body-sm，与检索框同一套。清除 = 有值且悬停/聚焦时纯文本 × | 所有文本输入 |
| C03 | `DsNumberInput` | 数字输入（text+inputMode=decimal+等宽右对齐，扣减类红色） | 数字/金额/扣减字段 |
| C04 | `DsSelect` | 下拉选择 | 普通下拉 |
| C05 | `DsTag` | 标签（default/brand/success/warning/danger 配色） | 状态/分类标签 |
| C70 | `ValueChangePair` | 原值/新值对比卡片（原=弱灰，新=品牌绿，中间 →）。`ValueChip` 单枚。禁止用「」包一对值。纯 CSS 无状态 | 确认层变化提示、批量匹配前后对比、新建项关键词 |
| C06 | `DsSegmented` | 分段控件 | 视图/维度切换 |
| C07 | `DsDialog` | 弹窗（挂进画布 modal 叠加层，相对 1200px 画布居中） | 范式 B 弹窗 |
| C08 | `DsDrawer` | 抽屉（层级递减宽度，多层递进） | 一对多「多」侧子明细 |
| C09 / C10 | `DsNavButton` / `DsNavLink` | 导航按钮/链接（3 级导航规范） | 导航区 |
| C11 | `DsShellRow` | 通用行盒子（两层嵌套+隐藏滚动条+拖动） | 骨架行布局 |

---

## 三、表格体系（UnifiedTable 三层架构，components/table/）

| 编号 | 模块 | 职责 |
|------|------|------|
| C32 | `UnifiedTable` | 统一表格组合体：数据显示 + 交互 + 单元格编辑 + 数据列按内容撑宽 + 行/表头扩展菜单（行=当前行，表头=已勾选或全部） |
| C33 | `DataViewLayer` | 第一层·纯数据渲染（零编辑态） |
| C34 | `InteractionLayer` | 第二层·交互增强（编辑态管理/键盘导航/失焦自动保存/CellEditor 注册表）·**无独立 DOM** |
| — | `cell-editors/` | 单元格编辑器（Text/Picker/Static + CellEditorRegistry） |
| — | `colWidths.ts` | 列宽预设 `COL_WIDTHS`（内容决定宽度/换行优于截断/尾部弹性填充） |

**规则**：所有表格列宽必须从 `COL_WIDTHS` 取；禁止通过 `!important` 打补丁覆盖表格样式。

---

## 四、浮动面板（FloatPanel + smartPopupContainer）

| 编号 | 模块 | 用途 |
|------|------|------|
| C60 | `FloatPanel` | 浮动面板唯一基座（portal 到画布 float 叠加层，舞台布局坐标；title 顶栏 / children 可滚 / footer 底栏）。层级与缩放规范见 [文档可视化/canvas-ui-hierarchy](../../文档可视化/index.html) |
| — | `smartPopupContainer` | 统一挂载：弹窗/抽屉内→modal 层，其他→float 层 |

**规则**：下拉面板/悬浮面板统一以 FloatPanel 为底层，禁止手写第三种挂载策略。高度有上限、超出只纵向滚动；横向按列完整铺开，禁止面板内横滚。水平左对齐锚点，不做视口避让；左右夹在 1200px 画布内。顶栏 title、底栏 footer 钉住不滚（确认/取消放 footer）；中间才滚。二级展开贴在按钮旁，父面板不得横滚把展开错位。点一下空白才关；移动画布或手指滑动不关。

---

## 五、录入/选择组件（components/）

| 编号 | 组件 | 用途 |
|------|------|------|
| C12 | `SuggestInput` | 检索+新建+回填（AutoComplete + SuggestList 唯一列表实现）。外观与 DsInput sm 同一套（20px / body-sm，无框内下拉箭头） |
| C13 | `SuggestList` | 匹配列表唯一实现（容器/行高/新建项/loading/无匹配统一）。新建项是「新建」+ 关键词卡片，不要写成新建「某某」 |
| C14 | `DictRefCell` | 档案引用单元格（输入→匹配复用/快捷新建/失焦解析，带 id 回调） |
| C15 | `DictRefField` | 档案引用输入+管理面板一体化（brand/supplier/category/priceType 共用）。输入框旁 ▾ 与输入同高 |
| C16 | `DictFieldInput` | 供应商联系方式「方式」列专用（SuggestInput + DictListPanel）。**不是** C15 的替代件，禁止改成 DictRefField 换皮，也禁止当死代码删除 |
| C17 | `DictListPanel` | 字典列表管理面板（配置驱动：搜索/选择/行内编辑/删除/新增）。搜索/新增/改名输入与外面检索框同一套小号 |
| C18 | `UnitDropdown` | 单位切换下拉（仅显示有换算率的单位，口语化换算） |
| C19 | `UnitManagePanel` | 单位管理面板。勾选默认与选品同一套（空行也占位）；行点击切换当前单位；名称 / 换算点值打开确认层，不挂常驻输入框 |
| C20 | `UnitPriceExpandPanel` | 单单位价格展开面板（售价/进价双 Tab）。类型或渠道 \| 面价 \| 点位 \| 实际价。点值打开确认层；确认修改=当前这一条规格，改全局=品牌+分类这一批（已经单独改过的不跟着变）。列表不另开点位列 |
| C21 | `PricePicker` | 价格字段「+ 新建补全价格」入口 |
| C22 | `ProductPicker` | 选用检索面板。点格进确认层后，输入+下拉挂在弹层输入框（默认展开，可收起）。顶栏按 `pickerTree.ts` views 切树上哪一层、主行粒度跟着变。同一格子同一串字，切档不改字；匹配是包含与被包含。切档是换看法。渠道档问这货谁可能供（已进价 ∪ 经营范围），不是搜供应商档案当产品名。到那一层有什么就搜什么，不按词禁结果。切档后入口层最左、其余层各占一列，禁止叠成一行灰字；说明默收起。名称列=`search`；单位列=`unit`（单位 \| 换算率 \| 售价 \| 进价）；单价列=`price`（单位已锁定，不显示换算，也不出现入口按钮）。写入单据只走插入按钮；点名称/换算率/面价/点位打开输入浮层（看全文 + 影响范围）。默认确认修改=当前；单位/品牌/分类/售价类型/供应商可改全局（已有同名则并档，列出影响数据；字典格打开即检索下拉）。点位也可改全局（品牌+分类这一批，已经单独改过的规格不跟着变）。格子划过要看得出能点；确认层不得关掉选品。末尾空行新增同样走这层，单位/价格空行左边也占勾选位。禁止上下铺完整价表，禁止换成维护矩阵。规则见《档案管理·选用检索》；槽位以 `文档可视化/` 为准；现场为什么这样探货见指导思想 · 销售开单 |
| — | `pickerTree.ts` | 选用检索树模型 `layers[]` → `pickerViewsFromModel` 派生宽松+精准。可选 `dateFilter`（确认层按日翻）。产品/客户/供应商/单据头/已卖行只配层；配货来源两枝不套切层。禁止另写选品面板。规则见《档案管理·选用检索》 |
| C23 | `CustomerPicker` | 客户信息格。点格进确认层。顶栏由客户树派生。私有：点中后挑一条联系，姓名+联系填进去。快速新建仍在面板里。规则见《档案管理·选用检索》 |
| C24 | `EnumPicker` | 枚举选择器（只读触发 + 浮动面板） |
| C25 | `UnitPicker` | 单位选择器（FloatPanel + 检索） |
| C26 | `AllocationSourcePicker` | 配货来源选择器。仓和渠道不是一份名单：一框检索，两列分区（内部仓 \| 供应商），各选各的。检索本地过滤已加载名单，不要因父级传入 sources 就关掉匹配。无匹配时列内快速新建，禁止在配货编辑浮层上再叠 Modal。不要套宽松+精准 |
| C67 | `SupplierPicker` | 供应商选用检索。顶栏由供应商树派生（默认宽松）。点行回填一家。规则见《档案管理·选用检索》 |
| C68 | `SoldLinePicker` | 售后已卖行选用。点格进确认层。数据源来自旁边单据格。对着当时名称打字，勾选后插入：复用当时名和价，数量另填 |
| C69 | `DocumentSourcePicker` | 售后单据格。点格进确认层。`dateFilter` 开：空词按日翻（默认当天，上一天下一天 / 上月下月）。打字不锁日期。行上可预览。勾上的单当已卖行数据源 |
| C27 | `QuickOptionsBar` | 预置快速选项条（常用单位等点选填入）。行高走骨架行，横向滚，不把面板撑宽 |
| C28 | `RecordExpandPanel` | 多记录展开面板外壳（Tab + 维度切换 + 常驻 DOM）。无 Tab/切换时贴边吃内容，不垫灰底 |
| C29 | `MatrixTable` | 多记录字段矩阵面板（表头+数据行+末尾常驻空行）；**EntityPanel 适配器**。默认列=名称+可选 mid+值+默认+操作。单字段 N 关 `showPrice`/`showDefault`，只留名称+删除 |
| C61 | `DsInputDropdown` | **输入+下拉组合**。工作台选用的展开/收起挂在确认层输入框里，不要挂在表格格上（表格格只展示）。档案列表 `lockInput` 点开确认层。确认层里仍是真输入，未编辑不得 readOnly 挡键盘（lockInput 除外） |
| C65 | `EntityPanel` | 浮层表网格基座（`.ds-grid-header` / `.ds-grid-row`）；C19/C29 是适配器，禁止用它替换 ProductPicker/UnitPicker |
| — | `FocusBus` | 焦点总线（单元格激活的单一焦点系统）·无 UI |
| — | `PanelTree` | 面板层级树（同级互斥）·无 UI |

---

## 六、快速新建 + 自动补充确认（v15.x 核心能力）

> 这是「缺省值兜底 + 按值去重 + 保存前确认」的统一机制，**所有涉及自动补充的保存路径必须接入**。

| 编号 | 模块 | 用途 |
|------|------|------|
| C30 | `QuickCreateConfirmDialog` | 快速建档弹窗（字段分开编辑 + 缺省确认 + 相似档案候选决策） |
| C31 | `DefaultFillsPreview` | 保存前「完整档案字段清单」预览（分层展示字段 + 自动补充标记） |
| — | `confirmFillsBeforeSave` | **统一自动补充确认入口**：有补充项 → 弹确认；无 → 直接放行·无 UI |
| — | `quickCreateConfig` | 配置 SSOT：数据层注册表（产品信息层/价格信息层）+ 字段兜底值 + 去重键 |

### 去重键规则（A/B 两类，前后端同构）

- **A 类全局字典**（分类/品牌/价格类型/供应商/**单位**）：`name`/`unitName` 全局唯一 → 纯名称去重
- **B 类父级从属**（产品/规格）：父级 id + 名称唯一 → 必须携带父级上下文，不能纯名称去重
- **规格引用单位**（v16.5 `spec_unit`）：全局单位字典上的「这个规格用哪些单位 + 基准/显示」；解除引用不删字典行

### 如何接入「自动补充确认流程」（标准步骤）

> 场景：某保存路径涉及缺省值自动补充（如"没选价格类型就补零售价"），要求保存前提示并确认。

```tsx
import { confirmFillsBeforeSave } from '...shared/components/index.js';
import { QUICK_CREATE_LAYERS } from '...shared/config/quickCreateConfig.js';

// 1. 从配置层取兜底值（禁止硬编码"零售价/面价渠道"等文案）
const priceLayer = QUICK_CREATE_LAYERS.price;
const priceTypeCfg = priceLayer.fields.find((f) => f.key === 'priceType')!;

// 2. 检测本次保存是否有自动补充项
const saleNoTypeCount = salePrices.filter((p) => p.price.trim() && !p.priceTypeId).length;
const notes: string[] = [];
if (saleNoTypeCount > 0) notes.push(`售价 ${saleNoTypeCount} 行自动补充价格类型「${priceTypeCfg.fallback}」`);

// 3. 保存执行前统一确认（无补充项会自动放行，不打扰）
const confirmed = await confirmFillsBeforeSave(modal, { groups: [], notes });
if (!confirmed) return; // 取消 → 不保存

// 4. 确认后执行真正保存
await saveProduct(input);
```

**规则**：任何保存路径（手动/失焦/静默触发）只要涉及自动补充，保存执行前必须走 `confirmFillsBeforeSave`；补充文案的兜底值一律来自 `QUICK_CREATE_LAYERS`，禁止硬编码。

---

## 七、表格列字段组件（cells/）

| 编号 | 组件 | 用途 |
|------|------|------|
| C35 | `NameLinkCell` | 主名称弹窗列（档案列表产品名单独一列点开编辑；选品仍可分段拼品牌+规格） |
| C36 | `ImageThumbCell` | 图片缩略图列（点击看大图，无图进编辑补图） |
| C37 / C38 | `TextCell` / `LongTextCell` | 短文本列 / 长文本弹窗列 |
| C39 | `DateTimeCell` | 日期时间列（等宽字体） |
| C40 | `StatusTagCell` | 状态标签列（色标区分，statusMap 注入） |
| C41 | `EnumInlineEditCell` | 枚举行内编辑列（枚举值 + ▾ + 枚举管理面板） |
| C42 | `RecordFieldColumn` | 多记录字段列统一抽象（单元格+切换+展开面板三件套）。`wrap` 给 N 项集合列：复用 ArchiveFilterChip 换行，禁止 invent 分组 UI。·**无独立 DOM** |
| C43 | `SkuPriceColumns` | 单位▾ 售价▾ 进价▾ 三列工厂（禁止各列手写样板）·**无独立 DOM** |

---

## 八、页面骨架与动作条（components/）

| 编号 | 组件 | 用途 |
|------|------|------|
| C44 | `AppShell` | 全局骨架（1200 舞台 + 叠加层 + 底部上下文操作栏） |
| C45 | `ViewFrame` | 工作台视图统一骨架（根div + StageActionBar + StageBizStrip + 内容区） |
| C45a | `ArchiveListPage` | 管理列表页统一壳（ViewFrame + UnifiedTable + 跨页勾选 + **filters 检索槽**）。新档案走 `filters`（防抖关键词 + 条件词 + 右侧状态），不要手写查询按钮 / 检索条 SuggestInput。规范见《档案管理》与 [文档可视化](../../文档可视化/index.html)「档案管理 · 全局规则」 |
| C45b | `ArchiveSlotHost` | **档案运行时宿主**。各页交 `slots[]` + API，宿主渲染名称列/完整弹窗/N 矩阵/检索。供应商、库房、客户已接入。产品宽表暂不迁。不要再为每个管理功能抄一份同构页面。 |
| C45b | `HeaderCascadeFilter` | 表头列筛（DsInputDropdown + FloatPanel + SuggestList）。档案页选项=当前结果 facets；订单中心选项=当前单据所有行 facets。条件升到关键词/业务条旁 chip。 |
| C46 | `PageActionBar` | 页面级底部操作栏（步骤回退/前进、撤销重做、保存状态） |
| C47 / C48 | `StageActionBar` / `StageBizStrip` | 视图操作条 / 环节业务条（L2/L3 层） |
| C49 | `StageDocumentHeader` | 单据抬头（单行紧凑横排，clickToEdit 可编辑字段） |
| C50 | `DocumentContextBar` | 单据上下文栏（编号/日期/单据标题/客户信息/收货地址/状态，权限分层只读） |
| C56 / C57 | `common/PermissionDenied` / `PermissionGuard` | 无权限提示块 / 权限守卫（业务话术，不暴露权限码；C57 **无独立 DOM**） |
| C58 | `common/SaveStatusProvider` | 即时保存反馈（行级状态点 + message）·**无独立 DOM** |
| C59 | `common/StatusBadge` | 单据 8 档状态徽章 |

---

## 九、单据视图（components/ + document-form/）

| 编号 | 模块 | 用途 |
|------|------|------|
| C51 | `DocumentView` | 单据视图（打印预览，A4 竖版/A5 横版/A5 竖版三模板） |
| C52 | `DocumentPaperView` | 单据即工作台（所见即所得 + 点击单元格直接编辑） |
| — | `DocumentFormView` + A4/A5 模板 | 单据渲染引擎 + 三种模板（容量 25-28 / 10-12 / 16-18 行） |
| — | `moneyToChinese` | 金额转大写中文 |
| — | `useFormPagination` | 单据分页 Hook |
| C53 | `CreateDocumentModal` | 新建单据弹窗（编号自动生成 + CustomerPicker） |
| C54 | `ReimbursementBillPanel` | 报销单面板 |
| C55 | `BatchStandardizeDialog` | 批量补全产品档案弹窗。匹配后用 C70 展示原文字→档案名 |

---

## 十、通用 Hooks（hooks/）

| Hook | 用途 |
|------|------|
| `useDebounce` / `useDebouncedCallback` | 值防抖 / 连续输入防抖落库（flush/cancel） |
| `useSafeAsyncEffect` | 安全异步 effect（卸载后跳过回调） |
| `usePermission` | 当前用户视图权限（none/ro/rw） |
| `useSkuPriceState` | 行级单位/售价/进价状态管理（SSOT） |
| `useSuggest` | 检索辅助录入逻辑（防抖 250ms + suggest API） |
| `useMatrixRecords` | 多记录矩阵面板状态机（末尾空行 + 互斥设默认） |
| `useWsAutoRefresh` / `useDocumentSubscription` | WebSocket 跨视图联动刷新 |

---

## 十一、工具函数（utils/）

| 工具 | 用途 |
|------|------|
| `smartPopupContainer` | 浮动面板统一挂载策略（见第四节） |
| `buildSaveProductInput` | ProductView → SaveProductInput 全量组装（SSOT） |
| `format` | formatMoney / formatQty 等格式化 |
| `resolveImageUrl` | 图片 URL 解析（双部署模式） |
| `imageCompress` | 图片预压缩（Canvas，<200KB 直通） |
| `canvasStage` | 画布舞台：叠加层挂载、布局坐标、antd 下拉校正；弹窗 CSS 居中 |
| `shellZoom` | 远程桌面式整幅画面 `--shell-zoom`（只打舞台）；WebKit 字号补偿 |
| `defaultRecord` | 多记录字段「默认记录」统一规则 |
| `permissionTips` | 权限提示文案（业务话术） |
| `search-scoring` | 前端版搜索打分纯函数（双端 SSOT） |
| `unitRateText` | 单位换算排序 + 口语化换算文本 |

---

## 十二、配置注册表（config/）

| 配置 | 用途 |
|------|------|
| `quickCreateConfig` | 快速新建缺省值注册表 + 数据层字段配置（层/字段/兜底值/去重键 SSOT） |
| `recordDicts` | 档案引用字典配置（brand/supplier/category/priceType 的增删改查 + 引用计数） |

**规则**：调整任何兜底值/字典行为，改配置一处全局生效，禁止在业务代码硬编码。

---

## 十三、引擎（engines/）

| 引擎 | 用途 |
|------|------|
| `pricing-engine` | 算价引擎（前端镜像，与后端 `backend/src/engines/pricing-engine.ts` 双端一致）：round2 / roundPrice / applyMargin |

---

## 十四、请求与状态（services/ + stores/）

| 模块 | 用途 |
|------|------|
| `services/request.ts` | HTTP 封装（axios，员工/客户 token 分存，401 跳登录） |
| `services/websocket.ts` | WebSocket 客户端（鉴权/订阅/自动重连/心跳/事件分发） |
| `services/api/index.ts` | 13 个 API 模块统一出口（单据/报价/收款/配货/交付/成本/退款/汇总/定档/客户/基础数据/系统管理/库存） |
| `stores/auth.ts` / `customer-auth.ts` | 员工/客户鉴权 store（token + 权限图） |
| `stores/document.ts` / `purchase-list.ts` | 活动单据 / 采购清单 store（WS 增量同步） |
| `stores/workbench.ts` | 工作台多标签页 store |

---

## 十五、共享类型（types/）

| 文件 | 内容 |
|------|------|
| `shared/types/index.ts` | 与后端 Prisma + types 对齐的共享类型（枚举/ViewCode/WsEvent/BigInt→string 约定） |

---

## 附：新需求接入流程（防重复造轮子自查）

1. **查本清单**：需求能力是否已有共享实现？有 → 直接复用，禁止另写。
2. **无共享实现** → 先在 `shared/` 落地为共享能力，**同步更新本清单**，再在业务页面引用。
3. **不确定归属** → 按清单目录归类（组件→components/、配置→config/、工具→utils/、状态逻辑→hooks/、算价→engines/）。
