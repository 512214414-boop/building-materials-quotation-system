# 开发规划（spec → 任务拆解）

> 生成：2026-09-05 凌晨。来源：掌控台开发管道 3 项 + 台账遗留项，**逐条对代码核实后**规划（不是照抄台账）。
> 用法：对 AI 说「按开发规划做 Task N」即可开工；每做完一个 Task 回写 `docs-coverage.md` + 重跑 `node tools/gen-boss-view.mjs`。

---

## 一、现状核实（规划的事实基础，全部代码取证）

| 管道项 | 台账/掌控台说法 | 核实结果（2026-09-05 凌晨） | 真实状态 |
|---|---|---|---|
| P1 角色权限/审计日志对齐 | 并行会话占用中，等提交 | 那个会话的活**已干完**：46 个文件改动全在工作区（9 页配置驱动迁移、掌控台核实、向导后端、backorder 快照迁移），**只是没提交**。`opsReportService.ts` 已不在改动列表里（不再是移动靶） | 卡点 = 没落账，不是没干完 |
| P1 经营报表选品/需要维度 | 同上，等提交 | 同上。素材源 `backend/src/services/opsReportService.ts` 已稳定 | 提交后立即可做 |
| P2 Meta Studio 新增实体向导 | 没有新增实体的写入口，须手写 yml | **已过时**：工作区 `tools/meta-studio.mjs` +271 行，`POST /api/entity` 已实现（预览→确认→备份 .bak→跑生成器→失败回滚，四步保护齐全）；**只差界面入口**——`MetaStudio/index.html`（9-03 版）没有新增实体的按钮和表单 | 后端就绪，前端缺入口 |
| 搭车项：cellSpec 三件套死代码 | 1 行清理可单独立项 | 已核实**零消费方**（全站 import 扫描无引用） | 可安全删 |
| 搭车项：actionMeta 死数据 + specBrandId 旧名提醒 | 等并行会话提交后处理 | 同样卡在「没提交」 | 提交后可做 |

**结论：整条管道的真实堵点只有一个——把已完成的工作提交落账。** 提交之后，剩余工作全是小任务。

---

## 二、Spec（每项：干什么 / 为什么 / 成功标准 / 边界）

### 假设（开工前请纠错）

1. 这批 46 个文件的改动是**已完成、待提交**的状态，不需要再补代码（台账 §六 各条均写了「build:check 全过」）。
2. 提交 = 本地 git commit，**不推送**（推送时机由你定，手机端会自动拒绝）。
3. 规划范围只含开发管道 3 项 + 2 个搭车小项；⏳ 长期目标态（v23 两条）不排期不进本规划。

### Task 0 对应的 Spec：提交落账 ✅ 已完成（2026-09-05）

- **干什么**：把工作区 46 个文件按归属分 5 个提交落账（配置驱动迁移收口 / backorder 快照迁移 / 架构蓝图+门禁体系 / Meta Studio 向导后端 / 掌控台+规划+台账）。
- **为什么**：两条 P1 的前置条件就是「等提交」；不提交，任何文档对齐都在移动靶上写。
- **怎么做的**：提交前先跑 `npm run verify:static` 全量门禁——**6/6 全过**（前后端类型检查 / 后端单测 / lint / yml↔生成物对拍），确认这批改动是绿的才入库；**未推送**。
- **边界**：只提交不改代码；临时文件（tmp-*、.workbuddy、.codebuddy、截图证据、孤儿文件 访问地址.md）不入库。
- **落账后发现**：工作区里还有一批台账没记的「架构蓝图 v2 + G1 门禁体系」（verify.mjs / gen-routes.mjs / 冒烟脚本 / tsconfig 全量 strict），所以实际拆了 5 批而不是原定 4 批。

### Task 1 对应的 Spec：系统管理两篇对齐（P1a · 文档任务）

- **干什么**：`sys-role` / `sys-audit` 两篇的规格数据（权限码 18 个、审计 action 目录）与生成物逐项核对——数字和清单必须能在 `entity-meta.yml`（auditActions 段，617 行起）或导航注册表里找到出处，找不到的改文档。
- **为什么**：台账规定「权限码数以实测为准、禁止文档手写死数字」，当前两篇仍有手写口径需要对账。
- **成功标准**：两篇每个数字/清单项都有出处；`check-docs` 通过；掌控台该待办消失（回写台账 §7 销项）。
- **边界**：文档跟代码（裁决链）；若发现代码真错了，报给你裁决，不擅自动代码。

### Task 2 对应的 Spec：经营报表七类补维度（P1b · 文档任务）

- **干什么**：从 `backend/src/services/opsReportService.ts` 逐类实测提取口径（取数条件 / 上限 / 舍入 / 过滤状态），给 `ops-report` 篇补齐——现状 facts 只写了 3 类（区间汇总/库存周转/应收账龄），**缺 分类毛利 / 业务员绩效 / 采购汇总 / 退款统计 4 类**；同时每类标注「选品」维度（只读表 + 货币列，走 `report_*` cellSpec 装配）。
- **为什么**：七类是并列关系，缺一类的口径=那类数字没约束；素材源已稳定，现在写不会再漂。
- **成功标准**：七类各有口径行+选品行；`check-docs` 通过；回写台账销项。
- **边界**：口径照抄代码现状（「现状从代码提取」纪律），不做「应该怎样」的建议性改写。

### Task 3 对应的 Spec：Meta Studio 新增实体向导收尾（P2 · 代码任务）✅ 已完成（2026-09-05）

- **干什么**：Meta Studio 加「新增实体」向导——表单 → 预览将写入的 yml 片段 → 确认落盘（自动备份 + 跑生成器 + 失败回滚）。
- **成功标准**：已达成。非法实体在预览即拒且提示清楚；合法实体预览→确认→落盘→生成器全通；失败自动回滚实测有效；测试实体已清除、工作区还原。
- **完成方式**：后端 + 界面由凌晨的并行会话实现（本规划成文时已在工作区）；本会话补了端到端验证并修了两处缺陷。
- **本会话修的两处**：① 落盘失败的报错原来三件套全空（无法诊断），补上 exit 码 + stderr/stdout；② 预检没查 vocabulary.yml 词表，非法 layer 要到落盘才炸——现在 preview 阶段就拒并列出合法值（globalDict / subject / row / snapshot）。

### Task 4 对应的 Spec：死代码/死数据清理（搭车 · 代码任务）

- **干什么**：① 删 `cellSpec.ts` / `cellSpecAdapter.tsx` / `CellSpecRenderer.tsx`（已核实零消费方）；② `tools/gen-entity-meta.mjs` 停产 actionMeta 死数据（095 渲染器删除后无消费方）+ 清 specBrandId 旧名提醒。
- **成功标准**：前端 `build:check` 全过；`check-docs` 无 specBrandId 提醒；文档站页面无空格子。
- **边界**：删前再跑一次全站 import 扫描复核；`cellSpecsWithEditorsToColumns`（editorRegistry 里的活函数）**不在删除范围**。

---

## 三、任务拆解（依赖序）

| # | 任务 | 类型 | 规模 | 依赖 | 验证方式 |
|---|---|---|---|---|---|
| 0 | ✅ 提交落账（5 个提交，不推送） | git 操作 | S | 已确认 | verify:static 6/6 先行验证；`git log` 5 条各说清一件事 |
| 1 | 系统管理两篇对齐 | 文档 | S | Task 0 | `check-docs` 通过；掌控台待办消失 |
| 2 | 经营报表七类补维度 | 文档 | M | Task 0 | `check-docs` 通过；文档站经营报表页七类齐全 |
| 3 | ✅ Meta Studio 向导（后端+界面+验证） | 代码 | M | Task 0 | 冒烟全过：预检拒非法值、确认落盘、失败回滚；修两处缺陷 |
| 4 | 死代码/死数据清理 | 代码 | S | Task 0 | `build:check` 过；`check-docs` 无旧名提醒 |

**并行关系**：1 / 2 / 3 / 4 互相独立，Task 0 之后可任意顺序或并行。
**检查点**：Task 0 完成后先跑一遍台账 §九 全套自检（确认那批工作区改动提交后依然绿）；每个 Task 完成后回写台账 + `node tools/gen-boss-view.mjs` 刷新掌控台。

## 四、风险与对策

| 风险 | 影响 | 对策 |
|---|---|---|
| 46 文件分批拆错、混提交 | 中 | 按文件归属分 4 批，每批提交前 `git status` 复核清单 |
| 向导写坏 entity-meta.yml | 高 | 后端已有 .bak 备份+生成器失败回滚；实测必须包含「失败路径」（故意填非法 key 看它拒收） |
| 文档对齐时发现文档与代码真矛盾 | 中 | 按裁决链报你确认，不擅自改代码 |
| 删 cellSpec 后遗漏隐藏引用 | 低 | 删前全站 import 扫描 + `build:check` 兜底 |

## 五、做完这轮之后

本轮（2026-09-05 上午）已完成 Task 0（落账）+ Task 3（向导，含端到端验证）。剩余：
- **Task 1 / Task 2**：提交落账后即解除卡点，素材源（entity-meta 生成物、opsReportService）已稳定，可并行做。
- **Task 4**：搭车清理，随时可做。
全部做完：完成度 35/38 → 38/38（⏳ 两条不计分母）。剩余仅长期目标态 v23 两条（触发条件已写死在台账，不主动启动）。
