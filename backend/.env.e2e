# ===== e2e 端到端测试专用环境（S12 golden-e2e · 真库租户隔离）=====
# 消费方：backend/tests/e2e/tenant-isolation.e2e.test.ts（npm run test:e2e）
#
# ⚠ 为什么 DATABASE_URL 仍指向 dev 库 bm_quotation：
#   独立测试库需要 CREATE DATABASE —— 属 DB 结构变更，按硬约束须人工确认，本轮不擅自创建。
#   因此改用「独立租户 + 严格清理」三重防污染：
#     ① 只写 E2E_TENANT_A / E2E_TENANT_B 两个远离真实租户（1）的专用租户；
#     ② 每一行都带一次性 marker 前缀（__e2eti<随机>__），before 预清理（防上次崩溃残留）、
#        after 强制清理并断言行数归零；
#     ③ 所有 DELETE 均带 marker 或 tenant 过滤，绝不无条件删表/删租户 1 的数据。
#   有了独立测试库后，只需改本文件的 DATABASE_URL，测试代码不用动。

DATABASE_URL="mysql://root:rootpass@127.0.0.1:3306/bm_quotation"

# 两个互不相同的专用租户号（BigInt）
E2E_TENANT_A=990000001
E2E_TENANT_B=990000002

# 显式确认「允许在 dev 库上以严格清理模式跑 e2e」。
# 置为非 true 时测试直接失败退出 —— 不允许在没有独立测试库的环境里被静默跳过（假绿）。
E2E_ALLOW_DEV_DB=true
