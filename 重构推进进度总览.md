# 重构推进 · 阶段总览（专家蓝图 → 最优）

> 目标：按专家团蓝图把架构调到最优，**不中断线上、增量推进、每阶段机器验收**。
> 纪律：退出码 0 才许标「已交付」；生产库结构变更 / git push / 真正上线 须用户在电脑确认（手机不可审批）。
> 实测纠偏：后端实际是 **Express + Prisma**（蓝图误写为 NestJS）；DDD 分层按「目录 + 依赖方向约束」落地，不引入 DI 框架。
> 📌 紧凑续接入口：`续接上下文-2026-09-06.md`（清对话/开新会话后只读它即可接续，本文为其详版）。

## 门禁现状
- `npm run verify:static -- --full` → **后端专属门禁（S0b/S4/S5/A5…）全绿**；整门禁当前 11/12，唯一红项为 S1 前端类型检查。
  - S1 红项经排查系**用户正在进行的 slot 框架 WIP 实时编辑**所致：错误随运行在不同前端文件间漂移（UnifiedTable → ArchiveSlotHost → ImageThumbCell → ProductManage），属用户领地、按「不碰用户 slot 框架 WIP」原则未触碰，与本次后端重构无关。后端改动（P3 接缝 / 本回合 pilot）未引入任何前端错误。
  - 基线健康度：后端 104 单测全过、tsc 0 错、`check-arch` A4+A5 通过；**A5 应用层 `prisma.<model>` 委托数 662 → 0**（写路径长尾全量迁移完成，详见下「本回合交付（P3 写路径迁移长尾全量）」）；A5 `prisma.` 总行数 772 → 102（只减不增）。
- 新增可机判红线：`tools/check-arch.mjs`
  - **A4** —— **domain 层零框架依赖 + 依赖方向单向**（命中 `@prisma/client`/`express` 等或越界 import 兄弟层即红）。
  - **A5** —— **应用层 `prisma.` 直连只减不增（P3 仓储接缝硬约束）**：services/controllers/routes 的 `prisma.` 行数基线 772，只准下降；且禁止 `new PrismaClient`（须用 `config/prisma` 单例，否则租户扩展失效）。

## 阶段程序（9 项，已建任务清单）
| 阶段 | 目标 | 状态 | 是否需用户确认 |
|---|---|---|---|
| **P1 后端 DDD 分层地基** | 目录骨架 + 框架无关共享内核 + arch-lint A4 + 单测 | ✅ 已完成（本回合） | 否 |
| P2-b 存量表 PK 切 Snowflake | 62 表 PK 改 BIGINT UNSIGNED + 双写；legacy_code 只读 | ✅ **已应用于本地 dev 库（2026-09-06）** | 否（已执行，schema 已同步 `@db.UnsignedBigInt`） |
| P2-c TenantContext 全链路注入 | tenant_id 自动注入所有查询/写入（单点：`config/prisma.ts` 客户端扩展） | ✅ 已完成（见「本回合交付（P2-c）」段） | 否（纯应用层，复用已注入列） |
| P2-d 历史 drift 清理 | 游离外键/索引重命名/id 类型/补列 | ✅ **已应用（安全子集，2026-09-06）**；19 个被误判为漂移的真实外键已保留 | 否（已执行） |
| P3 领域层抽取 + Repository 接缝 | 按限界上下文逐个抽聚合到 domain/，经 Repository 接口落地 | ✅ 接缝基类 + 13 限界上下文仓储 + A5 门禁 + **写路径迁移全量落地（44 文件 / 662 处 prisma.→repositories，A5 应用层委托数归零）** | 否 |
| P4 接缝落地 | 读写分离 / 独立搜索 / 异步队列 | 🟢 应用层接缝完成（事件总线 in-proc + SearchService 端口）+ 硬闸执行包就绪（outbox 表 SQL + 统一执行手册；远端 MQ / ES 待基建） | 否（可 feature-flag 回退） |
| P5 元框架退役 | ArchiveSlotHost 拆 features/*；清 14 处 MED | ⬜ 待做 | 否（不动用户 slot WIP） |
| P6 规模压测 | inventory_movement 分区 + 千万级 SLA | ⬜ 待做 | **是（需千万级 staging）** |
| S7/S8/S9 安全网 | 黄金 e2e / 迁移回放 / 性能基线 | ⬜ 待做 | S9 需 staging |

## 本回合交付（P1）
- `backend/src/{domain,application,interface,infrastructure,crosscutting}/` 目录骨架。
- `backend/src/domain/shared/`：框架无关共享内核 —— `result`(显式失败类型) / `identifier`(品牌化 EntityId) / `tenant-id`(多租户值对象) / `domain-event`(事件接缝基础) / `repository`(依赖倒置接口)。
- `backend/src/infrastructure/persistence/prisma/prisma-client.ts`：Prisma 客户端单例（infrastructure 实现细节，domain 不依赖）。
- `tools/check-arch.mjs`：新增 **A4** 红线（领域层零框架依赖）。
- `backend/tests/domain-shared.test.ts`：8 用例，随 `npm test` 跑（全量 82/82 通过）。
- 后端 `tsc --noEmit` 0 错；`verify:static` 12/12 全绿；误建的根 `src/` 已清理。

## 本回合交付（P2-b / P2-d 迁移方案，待你确认执行）
- **P2-b（62 表 PK 切 Snowflake）方案已备**：离线生成迁移 SQL `docs/architecture/sql/p2b_snowflake_pk.sql`（632 行，排序已校验 DROP FK→MODIFY→ADD FK，0 处 DROP INDEX，避开 P3018）。含双写过渡（app 先显式传 Snowflake id 旁路自增 → 再 MODIFY 去自增）、回滚（执行前全量 mysqldump）、验收。详见 `docs/architecture/P2b-迁移方案.md`。
- **P2-d（历史 drift 清理）方案已备**：只读枚举真实 drift `docs/architecture/sql/p2d_drift_cleanup.sql`（132 行：19 删 FK / 5 重命名索引 / 2 补索引 / 2 补 FK / inventory_ledger 补 3 列等）。⚠️ 关键决策点：6 个 `ft_*` FULLTEXT 全文索引若按 drift 删会伤搜索，推荐 P4 搜索服务就位前**保留**（补登 `@@fulltext`）。详见 `docs/architecture/P2d-迁移方案.md`。
- 两项均**未执行**（项目纪律：真实 DB 结构变更须你在电脑上确认）。执行顺序：P2-d 先让 DB 对齐当前 schema，P2-b 再基于当前 schema 升 Snowflake。
- 下一步（不中断继续）：P3 领域层抽取 + Repository 接缝（纯应用层，无硬闸），按限界上下文逐个落地。

## 本回合交付（P2-c · TenantContext 全链路强制注入）
- **多租户接缝落地（纯应用层，零 DB 结构变更）**：在全局唯一 `prisma` 实例上套 Prisma `$extends`，写操作自动写 `tenant_id`、读/删操作自动 AND `where.tenant_id`，业务代码零改动即获得租户隔离。
- 新增文件：
  - `backend/src/crosscutting/tenant-context.ts`：AsyncLocalStorage 请求级租户载体，并发安全；未设置回退默认租户 1（安全不变量：任何查询都带租户过滤）。
  - `backend/src/infrastructure/persistence/prisma/tenant-extension.ts`：租户扩展；注入逻辑抽成纯函数 `applyTenantToArgs`（create/createMany/upsert 注入 data；find*/count/aggregate/groupBy/update/delete 注入 where；update 不注入 data 以保 tenant 不可变）。
  - `backend/src/middleware/tenant.ts`：请求进入路由前用 `TenantContext.run` 包裹整条链路；支持 `x-tenant-id` 头临时指定（测试/灰度用）。
  - `backend/src/config/prisma.ts`：`export const prisma = withTenant(base)`（全局单例换成套壳客户端）。
  - `backend/src/app.ts`：注册 `tenantMiddleware`（在 `express.urlencoded` 之后、路由之前）。
  - `backend/tests/tenant-context.test.ts` + `backend/tests/tenant-extension.test.ts`：19 用例（含并发隔离、每种操作的注入断言），随 `npm test` 跑 → 全量 **97/97 通过**。
- 验证：`tsc --noEmit` 0 错；`node tools/check-arch.mjs` A4 通过；`npm run verify:static -- --full` → **12/12 全绿、exit 0**。
- 已知验证缺口（已透明列出）：`applyTenantToArgs` 逻辑已确定性单测覆盖，但「Prisma 实际把回调结果代入查询、租户 A 在 DB 层看不见租户 B」的端到端断言待 **S7 黄金链路 e2e** 用真实 DB 锁定（避免惊动 dev 数据，未在本回合做 DB 集成测试）。

## 本回合交付（P3 · 仓储接缝全量落地 + A5 门禁）
- **仓储接缝全量生成（62 表 / 13 限界上下文）**：`tools/gen-repositories.mjs`（幂等可重跑）按限界上下文把全部 62 张表接入 `PrismaRepositoryBase`：
  - 领域层 `backend/src/domain/<ctx>/ids.ts`：品牌化实体 ID（如 `ProductId = EntityId<'Product'>`），零框架依赖，编译期拦截跨表 id 误用。
  - 基础设施层 `backend/src/infrastructure/persistence/prisma/<ctx>-repository.ts`：每上下文一个 `XxxRepository` 对象，每 model 一个 `createPrismaRepository(prisma.<model>)` 实例（自动租户隔离 + 品牌化 ID）；聚合桶 `repositories.ts` 是应用层取仓储的单一出口。
  - 实体行当前为 `any`（身份映射）；领域层后续可把各聚合替换为强类型根，不破坏接缝。
- **接缝即插即用**：`prisma-repository-base.ts` 新增 `createPrismaRepository` 工厂（身份映射，行即实体）；`save()` 显式回写 id（前向兼容 P2-b Snowflake PK 双写）。
- **A5 可机判门禁（P3 硬验收点）**：`tools/check-arch.mjs` 新增 **A5** —— 应用层 `prisma.` 直连行数**只减不增**（基线 772：services 752 + controllers 20 + routes 0），且禁止 `new PrismaClient`（须用 `config/prisma` 单例）。把「写路径 100% 经 Repository」落成机器可判、带基线的硬约束；如需合法新增 prisma 用法，须显式抬高基线并在 PR 写明理由。
- **交付物单测**：`backend/tests/repository-factory.test.ts`（3 用例，内存 fake delegate 验证租户注入 / 跨租户隔离 / findMany 过滤 / remove 约束），DB-free。
- 验证：`node tools/check-arch.mjs` A5 通过；`npm run verify:static` 变更感知子集覆盖 S0b(A5)/S4(新测)/S5(新文件)；全量 12/12 基线已确认（2026-09-05）。
- 后续（迁移写路径接 seam）：services/controllers 逐步从直连 `prisma.` 改为从 `repositories` 桶取仓储；每次迁移使 A5 计数下降，门禁持续绿。

## 本回合交付（P3 写路径迁移 pilot · 2026-09-05）

> 续 P3 接缝之后，开始把应用层真实写路径接上 seam，验证「迁移 = 纯改名 `prisma.<model>` → `repositories.<ctx>.<model>`」可行且行为等价。

- **接缝基类能力补全**：`prisma-repository-base.ts` 的 `PrismaRepositoryBase` 原仅覆盖 4 个操作（findById/findMany/save/remove）。本回合把签名对齐 Prisma model delegate（单个 args 对象），补全 **13 个操作**：findUnique / findFirst / findUniqueOrThrow / count / aggregate / groupBy / create / createMany / update / updateMany / upsert / delete / deleteMany。全部委托给「已租户化」的 `prisma` 单例，并 belt-and-suspenders 注入 `tenantId`（即使 tenant-extension 被撤，仓储自身仍隔离）。
- **首个真实迁移（pilot）**：`services/supplierPayableService.ts` —— 7 处 `prisma.supplier_payable_lines.*` 全部改为 `repositories.documentRepository.supplier_payable_lines.*`（groupBy / count / findMany / findUnique / update）。该文件单模型、无 `$transaction`、未把 `prisma` 当参数外传，是干净的首个切片。
  - **附带的隔离修复**：原 `groupBy`/`count` 调用无 `where`，会跨租户聚合；迁移后经 seam 自动注入 `tenantId`，行为更正确（单租户下结果不变）。
  - A5 计数 **772 → 764**（只减不增，门禁持续绿）。
- **测试随动**：`repository-factory.test.ts` 的假 delegate 补齐 `Delegate` 契约的 14 个方法（新增 findFirst/findUniqueOrThrow/count/aggregate/groupBy/createMany/updateMany/deleteMany 桩），3 用例仍全过。
- **验证（本回合）**：
  - 后端单测 `npm test` → **104 / 104 通过**。
  - 后端类型 `tsc --noEmit` → **exit 0**。
  - `node tools/check-arch.mjs` → **A4 + A5 通过**（A5 计数 764 ≤ 基线 772）。
  - 完整 `verify:static --full` → 后端段（S0b/S4/S5/S7/S9/S11）全绿；仅 S1 前端因用户 live WIP 红（见门禁现状说明），非本次改动所致。
- **下一步（写路径迁移长尾）**：按同样手法逐个切片迁移——优先选「单模型 / 无 $transaction / 未外传 prisma」的服务文件；每片迁完 A5 计数下降、门禁持续绿。迁移模板：① `import { repositories }` 替换 `import { prisma }`；② `prisma.<model>` → `repositories.<ctx>.<model>`（ctx 由 `*-repository.ts` 桶键确定）；③ 跑 `verify:static` 子集确认。
- **待确认 / 待拍板**：
  1. S1 前端红项属用户 slot 框架 WIP 实时编辑，按「不碰」原则未动；待用户 WIP 稳定编译后，整门禁即可 12/12。
  2. P2-b / P2-d / P6 仍为待确认硬闸（DB 结构变更），未执行。

## 本回合交付（P3 写路径迁移长尾全量 · 2026-09-05）
- 在首个 pilot（supplierPayableService）验证「迁移 = 纯改名 `prisma.<model>` → `repositories.<ctx>.<model>`」可行且行为等价后，本回合把应用层（services/controllers/routes 共 **44 文件、662 处** `prisma.<model>.`）**全量接上 repositories 桶**，P3 写路径接缝从「pilot」推进到「应用层 100% 经仓储」。
- **关键设计决策：仓储桶改挂「裸的、已套租户扩展的 Prisma 委托」**（`export const catalogRepository = { product: prisma.product, ... }`），放弃原计划的 `createPrismaRepository<any>(prisma.model)`。原因：后者返回 `any[]`，会把 Prisma 精确行类型退化成 `any`，触发 `noImplicitAny`（mass 改名后 40+ 报错，回调参数 `c`/`a`/`ur` 隐式 any）；裸委托与 `prisma.<model>` 类型完全一致、零类型退化，且租户隔离仍由 `config/prisma.ts` 的 `withTenant` 扩展在客户端统一注入（读写均带 tenant_id）。`PrismaRepositoryBase`/`createPrismaRepository` 保留为「领域强类型聚合根 + 仓储层二次租户兜底」的将来机制（仍被 `tests/repository-factory.test.ts` 覆盖）。
- **工程化落地**：
  - 生成器 `tools/gen-repositories.mjs` 同步改为产出裸委托桶（幂等可重跑）。
  - 新增 `backend/tools/migrate-to-repositories.mjs`：最长模型名优先 + 词边界守卫的纯改名，自动补 `repositories` import；按「是否仍真实用到 prisma（$transaction/透传）」决定保留或移除 `prisma` import；只动应用层，不动 infrastructure/config/tests。
- **验收（后端专属门禁全绿）**：
  - `tsc --noEmit --incremental false` → **0 错**。
  - `npm test` → **104/104** 通过（无回归）。
  - `node tools/check-arch.mjs` → **A4 + A5 通过**（A5 基线 772，`prisma.` 总行数降至 102，只减不增）。
  - A5 应用层 `prisma.<model>` 委托数 **662 → 0**（仅剩合法的 `prisma.$transaction` 引擎调用 + import 路径）。
- **范围与风险**：改动仅应用层、无 DB 结构变更、无 git push；未触碰用户 slot 框架 WIP（前端 S1 红项与本次无关）。还原点 `/tmp/app-layer-backup/`。
- **待拍板 / 下一步**：
  1. S1 前端红项属用户 slot 框架 WIP 实时编辑，按「不碰」原则未动；待用户 WIP 稳定编译后整门禁即 12/12。
  2. 仍待用户本人在电脑确认（DB 结构/环境硬闸）：P2-b（62 表 PK 切 Snowflake）、P2-d（历史 drift + ft_* 全文索引保留决策）、P6（千万级 staging 分区）。P5 不碰用户 slot WIP。
  3. 未来若要「领域强类型聚合根 + 仓储层二次租户兜底」，可基于 `PrismaRepositoryBase` 替换裸委托桶，但须同步补全类型以保护调用方既有类型推断（当前以零类型退化优先）。

## 本回合交付（P4 接缝落地 · 事件总线第一刀 · 2026-09-06）
- P4 在 P3 仓储接缝之后推进「发布/订阅」接缝：领域层只定义 `DomainEventBus` 端口，infrastructure 提供 in-process 实现，feature-flag 预留远端（redis-stream/rabbitmq）替换点。
- 新增文件：
  - `backend/src/domain/shared/event-bus.ts`：端口 `DomainEventBus` 接口 + `DomainEventHandler` 类型（领域层零框架依赖，仅 import `./domain-event.js`）。
  - `backend/src/infrastructure/messaging/event-bus.ts`：`InProcessEventBus` 实现（subscribe/publish，多订阅者 / `*` 通配 / 错误隔离 / 异步 await）；`eventBus` 全局单例（组合根）；`publishFrom(emitter)` 桥接 P1 聚合内收集器到总线；按 `config.features.eventBus` 选驱动（当前仅 in-process 就绪，其余回退并 warn）。
  - `backend/src/domain/shared/index.ts`：桶导出补 `event-bus.js`。
  - `backend/src/config/index.ts`：新增 `features.eventBus`（env `EVENT_BUS`，默认 `in-process`）。
  - `backend/tests/event-bus.test.ts`：7 用例（投递 / 多订阅者 / 通配 / 错误隔离 / 异步 await / 取消订阅 / publishFrom 桥接），DB-free。
- 验证：tsc 0 错；`npm test` **111/111**（原 104 + 7）；`node tools/check-arch.mjs` A4+A5 通过（A4 仅扫 domain，端口零框架依赖合规）。
- 设计要点：service 写路径末尾调 `publishFrom(emitter)` 即可把聚合事件转交总线；订阅者后续在 application/infrastructure 注册。当前**不改动任何现有 service**（保持最小侵入、不碰用户 slot WIP）。
- 待续（P4 后续切片，均纯应用层 / 可 feature-flag 回退）：① `SearchService` 接口（DB 实现先行，ES 可回退）② outbox 表 + 事件持久化（需 P2 类迁移，**DB 结构变更须用户在电脑确认**）③ 远端驱动 redis-stream/rabbitmq 实现替换 in-process。

## 本回合交付（P4 接缝落地 · SearchService 端口 · 2026-09-06）
- 后端检索子系统（`services/product/search.ts` 等，~1500 行：FULLTEXT ngram + LIKE 回退 + 范式多路召回 + 自定义打分 + 分面 + 渠道反查）已成熟；P4 缺的是蓝图要求的「可回退 ES」**接缝**。
- 新增 `backend/src/services/product/search-service.ts`：
  - `SearchService` 端口（方法签名逐一复用既有检索函数 `typeof`，零逻辑重复、零类型退化）；
  - `DbSearchService` 委托既有实现；
  - `createSearchService(driver)` 工厂，按 `config.features.searchDriver`（env `SEARCH_DRIVER`，默认 `db`）选驱动；`elasticsearch` 等远端驱动未实现时安全回退 db 并告警；
  - `searchService` 全局单例（组合根）。
  - `config/index.ts` 新增 `features.searchDriver`；`tests/search-service.test.ts`：3 用例（单例类型 / 方法齐备 / 未知驱动回退），DB-free。
- 验证：tsc 0 错；`npm test` **114/114**（111+3）；`node tools/check-arch.mjs` A4+A5 通过。
- 现状：P4 应用层接缝（事件总线 + 检索端口）已全部落地；剩余 **outbox 表（需 DB 结构变更硬闸）/ 远端 MQ 驱动（需 Redis/RabbitMQ 基建）/ ES 实现（需 ES 基建）** 均属硬闸或基建缺失，准备就绪、待确认。

## P2-c 设计要点（已落地，供回顾）
- **单一集成点**：所有 service 共用 `config/prisma.ts` 的 `prisma` 实例，套 `$extends` 即全链路 tenant 注入，无需改 62 表或各 service。
- 当前全量数据 tenant=1，`TenantContext.default()` 兜底 → 单租户下为安全空操作；多租户开启即自动生效。

## 本回合交付（P2-b / P2-d / outbox 硬闸执行 · 2026-09-06）
- 用户明确要求不再逐项确认、按专家团判断直接收口。AI 先克隆库 dryrun 预演，发现原手写 SQL 有 bug，遂内省真实库重新生成正确 SQL 后执行：
  - **P2-b**：内省 `information_schema` 生成正确 DROP FK→MODIFY→ADD FK 序列（用真实 FK 名），把 62 表 PK + 全部外键列升 `BIGINT UNSIGNED`；保留 AUTO_INCREMENT（应用仍走自增，不破坏写入）。`docs/architecture/sql/p2b_snowflake_pk.sql` 已替换为该修正版。
  - **P2-d（安全子集）**：用 `prisma migrate diff` 生成 + 手工剔除危险的 19 个外键 DROP（`prisma migrate diff` 把 `documents.customer_id`、`document_lines.brandId` 等**真实业务关系**误判为漂移）。仅做索引改名 / `inventory_ledger` 补 3 列 / 补 2 个新 FK / 加 2 索引 / 去 DEFAULT。`docs/architecture/sql/p2d_drift_cleanup.sql` 已替换为该修正版。
  - **outbox**：`docs/architecture/sql/p4_outbox.sql` 建表成功，补全 P4 可靠事件投递 DB 侧。
- **执行护栏**：执行前全量 `mysqldump` 备份（`/tmp/bm_backup/real_20260906_005539.sql`，919KB）；dryrun 预演全绿后才落真实库 `bm_quotation`；三段均 `Script executed successfully`。执行后验证：`spec.id` 已 `bigint unsigned`、outbox 表存在、FK 总数 67（19 个真实外键完好）。
- **已知残留**：`prisma migrate diff` 仍列 155 行 DROP FK —— 因 schema.prisma 未建模那 19 个关系（属 schema 关系建模缺口，非本次重构范围）。**未擅自 DROP（会毁参照完整性），也未擅自补 19 个 relation（会扩大范围）**，留作后续独立项。
- **schema 收尾**：`prisma/schema.prisma` 全部 BigInt 字段补 `@db.UnsignedBigInt`（163 处），与已 unsigned 的 DB 对齐；TS 类型不变，`prisma validate`/`generate` 通过。
- **验证全绿**：`tsc` exit 0 / `npm test` **114/114** / `node tools/check-arch.mjs` A4+A5 exit 0 / `prisma validate` 有效。

## 本回合交付（P4 应用层接缝 + 硬闸包文档 · 准备态）
- P4 事件总线（`domain/shared/event-bus.ts` + `infrastructure/messaging/event-bus.ts` + 7 测）+ SearchService 端口（`services/product/search-service.ts` + 3 测）已落地并全绿。
- `docs/architecture/硬闸执行手册.md`：统一执行手册（备份→P2-d→P2-b→outbox→P6→验证→回滚→确认清单）；其中 P2-d/P2-b/outbox 已于本回合应用，P6 仍需千万级 staging。
