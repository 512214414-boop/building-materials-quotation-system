---
name: 表格体系二轮收敛-删旧组融入新五层
overview: 删除可视化站点旧「表格功能框架模型」组（9 页）与「实体关系槽位」组，独有裁决按新五层体系融入真相源与矩阵页（产品管理下新增「编辑弹窗剖面」节）；「档案管理·全局规则」迁出旧组保留并对齐 v31 口径；回写台账、分开提交。
todos:
  - id: verify-renderer-wiring
    content: 用 [subagent:code-explorer] 核验 096 渲染器节类型、index.html/05-nav-groups/38 引用关系，产出删组安全清单
    status: completed
  - id: truth-source-absorb
    content: 修 methodology.yml 三条目（know-table 边界节与硬规则/know-metaschema 机制与自检/ui-layer-model 开弹窗剖面维度）融入独有裁决，跑 node tools/gen-docs.mjs
    status: completed
  - id: matrix-page-dialog
    content: 52-ui-layer-tables.js 产品管理下增「编辑弹窗剖面」节（块→组件→特征→登记表出处，内容重组自旧 44 章），096 渲染器按需最小扩展
    status: completed
    dependencies:
      - verify-renderer-wiring
      - truth-source-absorb
  - id: site-group-removal
    content: git rm 删 11 个文件，05-nav-groups 删两组并迁出 archive-framework 为独立「档案管理」组，index.html 清装配清单并改 CSS 名，38 删注册
    status: completed
    dependencies:
      - matrix-page-dialog
  - id: archive-framework-align
    content: 07-archive-framework/02-pointEdit.js「对槽」步对齐 v31（列顺序由登记表 pages.slots 驱动，页面只装配）
    status: completed
    dependencies:
      - site-group-removal
  - id: ledger-and-commits
    content: 回写 docs-coverage.md（§四/§六/§七行动项），跑 gen-docs+check-docs 无阻断，按 feat/chore 分三笔提交不 push，报验收清单
    status: completed
    dependencies:
      - archive-framework-align
---

## 需求概要

可视化文档站点收敛到唯一表格体系：删除旧「表格功能框架模型」组（9 页，数据视角）与「实体关系槽位」组，其独有内容按新「表格 UI 分层」五层体系（使用角度的层级描述）重新理解组织后融入，解决文档冗余重复交叉、唯一真相源未落地的问题。

## 已确认裁决（用户点选）

- **弹窗剖面：开维度·矩阵页承接**——52 矩阵页产品管理下增「编辑弹窗剖面」节（块→组件→特征→登记表出处），五层模型开新维度承接
- **删除范围：删两组成套**——「表格功能框架模型」组（9 页 + 50 架子 + 095 渲染器）+「实体关系槽位」组（37，机制上移真相源）；39/旧档案模型组（30-34）下轮再裁
- **特征台账：台账删·分析留**——63 行逐表特征清单直接删（手维护高漂移，表清单权威已 = entity-meta.yml），14 种特征集合→槽位映射分析吸收进 know-table

## 边界

- 不动业务代码（L4 迁移、cellSpec inline 残留删除仍为下轮代码任务）
- 不动 30-34 旧档案模型组、39、10-product-model、14-tables（下轮裁）
- 删除均 git rm 可回滚，删除理由记录台账；不 push（推送时机用户定）

## 技术方案

### 真相源机制（既有，直接复用）

唯一规范源 = `文档可视化/data-source/methodology.yml`；只改 yml 一处 → `node tools/gen-docs.mjs` 自动重生成 `js/data/gen/*`、AGENTS.md GEN 节、index.html 装配清单；站点手维护内容层（js/data/ 非 gen 文件）与真相源分层明确。

### 一、真相源融入（methodology.yml 三条目）

- **know-table**：① 第 5 步「对照槽位」增边界节——槽位缺口登记（5 建议补槽 expand/snapshotMatrix/annotation/scope/internal + custom=逃逸口体温计）、「该独立则独立」三例（只读台账/流水只追加/授权码无编辑态）、14 种特征集合→槽位映射与缺口统计、「两判据定框架边界」（有无树/主操作是编辑档案还是执行动作）、例外登记（产品=唯一绕开框架的深树集合体、点位四表/行内三表可消除）、防过度收敛反例（单据侧整体槽位化被证伪、合并判据=行为层一致+字段可配置）；② 第二步硬规则补「开单不必先有人档」「单据形似档案神不似（档案格改引用全局生效 vs 单据格快照抄死）」；③ 特征分支「维度数」行补第三分支（≤2 行内格 / ≥3 可编辑展开 / ≥3 且只读→独立台账页）+「记录基数」行补「基数决定槽位不看业务名词」实例
- **know-metaschema**：mergeColumns/slot 占位（`__skuPriceSlot__`）合并机制与 A+B 混合槽（unit 字典/标准双轨）实例；自检表补「默认地址回写主档=有意识冗余，不写会被当 bug 修掉」
- **ui-layer-model**：开「弹窗剖面」维度（facts 补一条：五层管表格主体，编辑弹窗看剖面——块→组件→特征→登记表出处，实例见矩阵页产品管理）

### 二、矩阵页增节

`文档可视化/js/data/52-ui-layer-tables.js`（手维护数据文件）产品管理（ui-layer-product）下增「编辑弹窗剖面」节，内容从旧 44-table-aggregate-product.js uiEditDialog 搬运重组（截图 + 块→组件→特征→登记表出处四层对照）；`096-render-ui-layer.js` 执行时确认节渲染支持，不支持则最小扩展，禁止另起渲染体系。

### 三、站点删组与导航重构

- **删除 11 个文件**（git rm）：42/43/44/45/46/47/48/49/50-aggregate-rack.js/37-entity-slot-model.js/095-render-table-framework.js
- **05-nav-groups.js**：删 table-framework 组 9 项 + entity-slot 组；archive-framework 迁出为新独立组「档案管理」（defaultOpen:false，hint：五段怎么走 · 槽位定律 · 交互范式）
- **index.html**：删对应 script 标签；`50-aggregate-rack.css` 改名 `ui-layer-rack.css`（.agg-rack 类名不动——096-render-ui-layer 复用，同步改 index.html 引用）
- **38-get-module-meta.js**：删 104-111 行 table-framework 注册
- **07-archive-framework/02-pointEdit.js**：「对槽」步对齐 v31 一句话——列顺序由 entity-meta.yml pages.slots 驱动（v31 pageAssembler，SupplierManage.tsx:456 已实证），页面只装配

### 四、台账与提交

- docs-coverage.md：§四 表格UI分层行补弹窗剖面维度；§六 记本轮落地（删组+融入明细）；§七 待补加「users/roles 形态符合框架却自写（605/512 行）待收」行动项
- 提交分笔：feat(docs 真相源融入+矩阵页增节) / chore(删组+导航重构+CSS 改名) / chore(07 对齐 v31)；收尾 `node tools/gen-docs.mjs` + `node tools/check-docs.mjs` 无阻断

### 性能与可靠性

- 删除前先完成融入与导航重构，删后跑 check-docs 验证无断链（侧栏完整性 35 条→核对剩余条数）
- 39-get-module-tables.js 保留：唯一调用方 010-render-why-beads 属旧档案模型组，随下轮裁
- CSS 改名仅改文件名与引用，类名零改动，渲染零风险

## Agent Extensions

### SubAgent

- **code-explorer**
- Purpose：执行前核验 096-render-ui-layer.js 的节渲染结构（52 矩阵页增节是否需扩展渲染器）与 index.html/05-nav-groups/38 的引用关系，防止删组断链
- Expected outcome：产出渲染器节类型清单与引用关系确认结论，指导最小改动