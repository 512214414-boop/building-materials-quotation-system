# 2026-09-07

## 配置层预览：显示元数据下沉到配置（@col 注释）
- 用户痛点：预览面板虽读真源，但每次改配置、HTML 要改很多（列中文名/列宽/渲染写死在 `配置预览面板.html` 的 `COL_META`/`cellOf`），与"只改数据源"原则冲突。
- 方案（用户确认）：注释注解形式 `# @col <键>: <中文表头>,<像素宽>`；范围含物理层字段列 + 规则表列（连同规则表一起）。
- 已落地：
  - 预览框架：`COL_RE`+`collectColMeta()`（扫描 4 个真源文本）→`COL_META_OVERRIDE`；`colMeta(k)` 注释优先、无则 `FALLBACK_LABEL/EXTRA_W` 兜底；`headAndCols`/`ruleHeadAndCols`/`diffSection` 改用 `colMeta`；规则表 `RULE_COLS/RULE_HEAD` 硬编码改为 `RULE_COL_ORDER`+`ruleCols()` 动态；`boot()` 中赋值 `COL_META_OVERRIDE`。
  - 回写注解：physical-layer.yml、derivation-rules.yml、difference-layer.yml 顶部各加 `@col` 块，像素值与旧值一致 → 显示零变化。
  - DDL/契约工具不受影响（本就忽略注释）。
- 边界（留 HTML，不进配置）：教学性说明文字、渲染引擎、跨表推导视图。
- 注：本环境命令审批面板弹不出，`npm run layer` 起服务/语法校验命令无法执行，靠人工读文件核验；改动逻辑自洽。

## 删除 gen-layer-map + 修复推导规则导航重复（2026-09-07）
- 用户确认：配置总览=冗余（面板已整合全部/分组），直接删。`tools/gen-layer-map.mjs` + 根 `配置分层总览.html` 已删；面板与 gen-layer-preview.mjs 的工具清单行(分层体检器)已移除。
- gen-layer-map 未被任何 verify/门禁脚本引用（仅文档+面板工具清单引用），删除安全；但其兼「分层体检器(0红门禁)」能力随之消失，待用户定是否折进面板或另立 check。
- 预览面板推导规则区导航重复修复：原 `rulesSection` 同时渲染 `navHtml`(类别chips导航) 与 `countsHtml`(.rulecnt 同列类别带计数)——后者是重复导航，已删（含其定义，否则未定义变量渲染即报错）。
- 遗留：分层契约.md / 概念纲领.md / product-relation-body-preview.html 仍引用已删的 gen-layer-map / 配置分层总览（"跑 gen-layer-map.mjs → 配置分层总览.html" 等），待清理（属 config-layer 文档，需用户确认再动）。

## 体检折入实时面板 + 文档清理 + 面板损坏修复（2026-09-07 续）
- 体检（原 gen-layer-map 的 0 红门禁）已折入实时预览面板：新增 `healthSection()`（机器语法禁写键扫描 + 标记体检 + 差异层引用存在性校验，try/catch 包裹绝不崩面板）+ 导航卡「体检」(data-nav=check) + 标签页 `tab-check`。配置改动零生成、随真源刷新即体检，可见性恢复。
- 三处文档失效引用已清理：分层契约.md（第6/7/142/149 行改指实时面板；P6–P13「四件套」→「三件套」、gen-layer-map 标注退役）、概念纲领.md（第85行）、product-relation-body-preview.html（工具清单删行）。全仓已无"跑 gen-layer-map → 配置分层总览"失效命令。
- ⚠️ 事故：一条消息里对**同一文件**（配置预览面板.html）并发多个 replace_in_file，导致文件尾部（startNav 哈希路由收尾 + `boot()` 调用 + `</script>`）被截断损坏；且面板文件 untracked，git 无法恢复。修复：据 gen-layer-preview.mjs 模板里的 `apply()`/哈希路由逻辑精确重建尾部（并补 `check` 层哈希路由），`node --check` 通过 SYNTAX_OK。
- 教训（重要）：**同一文件的多处编辑必须串行（逐个 replace_in_file），绝不在一条消息里并发改同一文件**——并发会导致内容丢失/截断，且本仓库配置预览面板.html 未纳入 git，损坏后无兜底。

## P17 表级配置数组化（2026-09-07 晚，用户定稿·一次做完）
- 用户提案并拍板：字段级 `独立去重/联合去重/全文检索/索引` 四标记 → 表级 `表级配置` 数组（独立去重: [字段…]/联合去重: [[…]]/全文检索/索引，缺省行若归此桶）；字段级只留 `检索.级别`、`缺省值`。动机：联合唯一本是跨字段事实，字段级+组号是编码妥协；一处收纳一处读取；"两键同标互斥"错误写不出来；预览字段表瘦 4 列。
- 落地：迁移脚本一次性改 physical-layer.yml 26 表（14 单列唯一/9 组合/9 索引表/全文 0）+ 头部语法说明 + @col 删 4 行；gen-db-ddl/gen-entity-meta 改读表级配置（字段旗标 uniq/fulltext/index 由表级集合推导，defaultRow 兼容）；面板 uniqueKeysOf/uniqFieldsOf/fulltextKeysOf/indexKeysOf 切换 + tcOf，表卡片头部展示表级配置，cellOf/FALLBACK_LABEL 瘦身，healthSection 数组化（字段级退役标记出现即红+表级数组引用存在性）；推导规则 A1–A6 及缺省值规则措辞同步；契约登记 P17。
- **验证：DDL + entityMeta(fe/be) + relations/fieldDefs/layers 全部生成物与迁移前基线逐字节一致（零漂移）**；面板 script node --check 通过；基线法（先 tar 备份生成物→改→重跑→diff）是结构迁移的可靠验证法，可复用。
- 发现既有问题（未动，待用户定）：gen-entity-meta 找 v3 三件套用 `配置预览/config-layer/*.yml` 平铺路径，实际在 `物理层/` 等子目录 → entityLayerV3.generated.ts 一直被跳过未再生。

## P17 补充：固定结构定律（2026-09-07 晚，用户定稿）
- 表级配置四键（独立去重/联合去重/全文检索/索引）**每表必列、无值写 `[]`**，禁止按有无增减键。yml 34 表全部固定（8 表补空块：供应商联系人/供应商地址/客户地址/客户开票信息/仓库/库区/库房联系人/待入库明细），脚本带字段引用自校验全过；头注释旧口径（"有就有、没有就没有"）已改为固定结构口径；契约已登记。
- **为什么能扩展（用户要求想清楚）**：① 显式空=决策、缺省=未决策——配置升级为每张表的完整决策清单；② 消费端零探测、按位取值，体检按形状校验；③ diff 即业务。适用一切「被机器按结构消费的声明」（推导规则固定字段已是；未来呈现层槽位、单据必填集同法）。
- 预览同日多次显示微调（都是只碰面板一个文件）：表级配置大列移到表名后首列、每行一个配置项且固定四项（无值 `[]`）、表名单元格"单位 unit"同行、删掉表名下重复的"唯一: …（全局唯一）"旧描述。教训三次：const 解构后累加会渲染报错；删变量必须 grep 确认零残留；**列头前插后模板里又写一个列头=重复列→全表错位**（phyTableHtml 双重 `<th>表级配置</th>` 事故）。
- 验证方法论：显示结构改动后不要靠肉眼，写脚本**真实执行渲染函数**（截取 `<script>` 到 `async function boot` 前的代码段 + new Function + stub 数据），数 `<th>`/`<td>` 对齐（须感知 rowspan：每表首行=列头数，续行=列头数-合并列数）。本次两视图验证对齐。

## 版式定稿（2026-09-07 晚，用户确认 + 固定结构定律再扩展）
- 用户经选项确认的物理层版式：**域导航 → 表卡片切换**（总览=全部表铺开、各域=卡片按表切换，卡片=中文表名+灰色小标识）；**每表 = 属性独立区（表名一行 + 表级配置固定四行）+ 纯字段表**（无跨行大格子，表级/字段属性彻底分层）；**字段一格合并**「单位名 unitName」（业务名独立列删除）。
- **字段列同律扩展**：`字段/类型/检索.级别/缺省值` 四列每表必列、无值显示 `—`（病根同表级配置：colsOf 曾按"数据见过"过滤固定列集，致分类表比产品表多一列）。已修，34 表审计列头逐表强断言通过。
- 面板代码变化：fieldRowOf 标识格合并渲染、headAndCols 去 BIZ_W 注入、colsOf 固定列集、fieldTableOf 属性独立区版式、phyRowsOf/phyTableHtml 删除、startNav 增加 .tblnav 卡片监听（域内互斥）、yml @col 标识改 `字段,260`。

## 版式定稿 v2 + 真浏览器验证（2026-09-07 深夜）
- 用户截图抓出真 bug：跨列只盖住前两行、字段行间有空行。**根因：`<tr>` 双重包裹**——fieldRowOf 已返回 `<tr>...</tr>`，fieldTableOf 外层又包 `<tr>${lead}${fieldRowOf(...)}</tr>`，非法嵌套被浏览器拆成交错空行，跨行格被空行稀释（视觉=只盖 2 行）。修复：fieldRowOf 只返回单元格序列（不含 `<tr>`），调用方统一包裹。
- 另一真 bug：`.tsec` 显隐 CSS 从未编写（全部常显）→ 表卡片点击"无效果"。已补 `.tsec{display:none}.tsec.show{display:block}`。
- **最终版式（真实浏览器验证通过）**：域导航（总览34/产品11/供应商8/客户5/库存10）→ 表卡片横排（中文表名+灰色标识，紧凑）；每张表 = 字段表内**前两列跨行**（表名列"分类<br>category" + 表级配置列固定四行，rowspan=字段数）+ 字段格「中文+标识」合并 + 四列固定（字段/类型/检索.级别/缺省值）。
- **验证方法论升级（重要）**：字符串正则审计生成的 HTML **查不出非法嵌套/解析期问题**（本次三轮字符串审计全绿但浏览器渲染错位）。显示类改动必须用 playwright-cli（真浏览器）验证：`playwright-cli open --browser=chromium` + eval 检查 `tb.rows`/`cell.rowSpan`/`cellIndex` + 程序化 click 测交互 + screenshot 亲眼看。已实测：103 张字段表结构全过、卡片切换互斥正确、截图肉眼确认。注意：本机无 Chrome 分发，须用 `--browser=chromium`；截图落在启动 cwd（仓库根）。

## 刚性规则入渲染契约（2026-09-07 深夜，用户定稿）
- 两条刚性规则（不可跳过）：① 配置项集合=固定集合，顺序/条目固定，全局所有表共用一套，**永不自动删减**（新增配置项=yml 真源/预览 TC_KEYS/生成器三处同步，禁部分表缺席）；② 值为空→条目保留（yml `[]`、预览 `[]`），空就空着、只填值、不动结构。
- 执行机制：预览体检新增**结构完整性强制**——表级配置缺键/多键/顺序错即红（按形状校验）；字段列同律（四列必列，无值 —）。已入契约 P17 补充「刚性规则」专段。
- 真浏览器复核：103 张表级配置格键序 100% 一致（独立去重→联合去重→全文检索→索引）。

## 渲染层职责铁律·三不（2026-09-07 深夜，用户方向纠偏·最重要）
- 用户纠偏：**源头结构完整性永远优先于渲染层兼容**。此前病根 = 真源 key 缺失，却反复改预览样板去兼容——方向完全反了。正确顺序：**先修真源结构完整性，预览只忠实读取现有结构做渲染**。
- 预览层三不（已入契约 P17 刚性规则段）：① 绝不主动补配置项（画面条目必须全部来自真源；真源缺了→回源头补，预览最多空占位+体检标红）；② 绝不改动源数据结构（零回写、零派生写回；读容错仅限不崩画面）；③ 绝不因源缺陷改渲染样板兼容。
- 复核结论：源头 34 表四键零缺失；预览层唯一网络调用=只读 fetch(no-store)，无任何写回路径——架构已合规，规则入约防回潮。
