# 2026-09-05 工作纪要

## P1 元模型覆盖率（已收口，非本会话动手）
- 台账 `~~P1~~ ✅`：档案类+单据类主体登记进 resources → **resources 19/27（引擎天花板）**；pages 5/27（supplier + 4 字典类）。
- 引擎天花板原因：剩 8 个（7 个 `report_*`（ops-report 只读聚合缓冲，Prisma 无 model）+ `supplier_payable`（表头虚拟、由应付服务承载））本就不该进通用资源引擎，属如实例外，已在 entities 登记字段/列/审计/统计，但不进 resources 段（避免生成 500 端点）。
- 加了 readOnly 引擎锁 + 第三道锁对拍。
- `npm run verify:static` 9/9 绿；掌控台完成度 100%、无排定开发。

## 本会话「继续」：custom 列收敛迁移（信号雷达指向）
- 信号雷达（signal-report.json）实测 liveCustom=85，20 文件待迁（MED「待补」）：页面不得再在 `renderMode:'custom'` 内手写单元格交互，须改写 L4 三维参数（display×editEntry×valueState）接入唯一链路（editorRegistry / compositeColumns）。
- 本批收敛 3 个只读展示页（纯 B 类标签列，低风险）：
  - `shared/components/table/compositeColumns.tsx` 新增 `multiTagColumn`（数组→多 DsTag，如角色）。
  - `AuditLogs.tsx` 操作类型着色标签 → `tagColumn`；`AuthCodes.tsx` 状态着色标签 → `tagColumn`；`AdminUsers.tsx` 角色多标签 → `multiTagColumn`。
  - 三页移除内联 DsTag `custom` render + 无用 DsTag 导入。
- 验证：前端 `typecheck` + `build` 全绿；重跑 signal-scan → **liveCustom 85→66**。
- 剩余待迁（下一刀候选）：`AccessRequests`（授权码列=点击复制交互，需新增 `copyColumn` 原语）/ `AllocationView`(1) `CostVerify`(1) `Delivery`(9) `PaymentReconcile`(5) `PurchaseQuote`(1) / `ArchiveSlotHost`(4) `RecordFieldColumn`(1) `SkuPriceColumns`(1) `editorRegistry`(1)。注：`entityRelations.generated.ts` 的 44 处 custom 是 yml 声明式 B 类复合列（已在 compositeColumns 注册），非技术债。

## 待拍板（用户原话口径）
- "继续"在"无排定开发"下无明确指向；本会话据信号雷达切入 custom 列收敛（MED 信号按方法论汇进待拍板，非 HIGH 自动立案）。是否持续推该迁移、或先 git 落账 89 个未提交改动、或启动 v23 长期目标态（用户此前说"不排期"），由用户定。
- 工作区 89 个未提交改动未动（含本批 3 文件 + 迁移在途文件）；按 git 工作流未提交、未推送。

## 档案类页面统一化（用户纠错 → 设计文档）
- 用户纠错：「基础数据管理本质都是一种，差异是实现方式导致，顶层设计不够通用」。核实结论：**product 是"没迁过去"的迁移债，非本质不同**——supplier/customer 已用 `ArchiveSlotHost`，product 停在旧壳 `ArchiveListPage`+`entityCellSpecs`+1300 行 `ProductEditDialog`。
- 真结构差异仅一处：product 是**复合体**（product→brand→spec→unit→price 跨 5 表），列表是**宽表**（一行=SKU，非一行=实体）。框架已具备 cascade(品牌→规格→单位)/matrix/自定义 槽；缺口 = ① 列表行模型 ② 价格矩阵复用槽。
- 设计文档：`档案类页面统一化设计.md`（已写）。目标态：product 迁 ArchiveSlotHost + 新增 **一个 product 专用 `composite` 槽**（SKU 价格矩阵），其余字段全转标准槽；**不建通用分组引擎**（过度抽象）。分阶段 Phase 0–4，每阶段可验证可回滚。
- 已登记 Task 5（开发规划.md + 台账 P1 指针），**待用户审 §5 未决项后开工**：① UX 取舍（候选 A 一行一产品+展开 vs 候选 B 宽表分组保留现 UX，推荐 A）② 宽表聚合位置（前端聚合推荐，零 DB 风险）。单据类（CostVerify 等）不在范围，按引擎边界代码驱动。
- 设计文档含给非技术用户的 UX 说明（§0/§2/§5），关键决策归用户。

## 档案类统一化·实施启动（用户已拍板：候选A+前端聚合）
- **Phase 1 框架扩展已落地并验证**（前端 typecheck + lint 绿）：
  - `archiveSlotTypes.ts` 新增 `ArchiveCompositeSlot<T>`（kind:'composite'；getSubRows + renderSubTable），加入槽联合。
  - `ArchiveSlotHost.tsx`：列推导加 composite 计数列（"N 个 SKU"）；宿主按 composite 槽计算 `expandable`（expandedRowRender=slot.renderSubTable，rowExpandable=getSubRows 非空）并透传给 `ArchiveListPage`。
  - `ArchiveListPage` 已通过 `...tableProps` 透传 `UnifiedTableProps.expandable`（search 0 命中，因 expandable 不在 omit 清单，原生转发）——**无需改 ArchiveListPage**。
  - 现有 supplier/customer 无 composite 槽 → expandable=undefined，行为不变（additive）。
- **关键校正**：设计文档里"从 ProductManage 抽 createSkuPriceColumns/useSkuPriceState 到 shared"其实已完成——二者已在 `shared/components/cells/SkuPriceColumns.tsx` 与 `shared/hooks/useSkuPriceState.ts`，且泛型 `<T>` + `getRowData(record:T)` 映射，可直接复用到 composite 子表。
- **剩余 Phase 1b–4（本会话未做，风险高需分步验证）**：productDef 前端聚合 searchProducts→一产品一行；product 专用 ArchiveSkuPriceSlot 子表组件（包 useSkuPriceState+createSkuPriceColumns+品牌/规格列）；ProductManage 换壳；品牌/规格/单位转 cascade 槽、价格转 matrix 槽（需把 ProductEditDialog 1300 行的 UnitSection/SpecListPanel/BrandImages 抽成槽兼容编辑器——最大风险点）；删旧壳。
- **下一步阻塞点**：编辑弹窗是 1300 行 ProductEditDialog（自带 DsDialog），与 ArchiveSlotHost 自带 DsDialog 会双套弹窗；需在 Phase 2/3 决定 bridge 方式（def.renderEditDialog 逃逸 or 把对话内部抽到 cascade/matrix 槽）。

## v23 长期目标态·启动（用户本日明确启动 ①③ 联锁）
- 用户裁决范式定调：**产品名=全局字典 `product_name`（name 全局唯一）**；同名即同一产品，规格挂产品下归一起；分类改多对多关系表 `product_category`（isPrimary+sortOrder），`product` 不再直连 categoryId。参照 brand/unit 范式。
- **实证（只读 SELECT）**：`product` 290 行、DISTINCT name 290、分类 6 个；跨分类同名=0。→ 回填**零合并风险**（每条 product 1:1 抽 product_name；product_category 每条 isPrimary=true）。用户原假设"跨分类同名正常不该出现"被证实。
- P0 实施口径已写入 `docs-coverage.md` v23 段（范式定调/实证/三层目标态/迁移路 P0–P5/DB闸门）。两条台账状态 ⏳→🕐。
- 节奏：用户选「先写文档(P0)审完再动」→ 等用户确认文档后做 P1（schema/meta/生成，不动 DB）。DB 执行(P4)无论如何需用户单独点确认（硬纪律）。
- 全栈触点清单（子代理清点 48+）：后端 saveProduct/catalog/searchNormalized/dictMerge/documentLineService/resolveSnapshots/controllers；前端 ProductManage/ProductEditDialog/ProductPicker/buildSaveProductInput/baseDataApi。召回字段名 productName/categoryName 保持不变→打分层零改。

## 两套"五层"正交关系澄清（用户问 P1 前先要此分析）
- **架构蓝图 L1–L5（纵向·资产归属轴）**：L1 业务元描述(yml/md)→L2 运行时配置(generated.ts)→L3 平台内核(shared/**)→L4 业务胶水(apps/*/pages/*)→L5 数据(MySQL)。回答"代码归哪层"。
- **表格 UI L1–L5（横向·表格关切纵深轴）**：L1 骨架装配→L2 行槽位→L3 表格主体(grid/matrix)→L4 单元格(display×editEntry×valueState)→L5 确认层。回答"表格内部从上到下怎么拆"。
- **二者正交**：表格 UI 的 L1+L2 = 蓝图的 L1(声明)+L2(生成配置)；表格 UI 的 L3+L4+L5 = 蓝图的 L3(平台内核实现)。不是两套平行架构，是"表格关切"在蓝图纵向栈上被切开后每层内部的再分解。
- **对 P1 含义**：P1"补 resources/pages"纯属蓝图 L1 登记动作，产出 L2 被既有的 L3 引擎消费，**不写任何表格 UI 引擎代码**。必须区分两个正交指标：① 元模型覆盖率(配置边界=登记 resources/pages) ② 表格 UI 层覆盖率(引擎采用率=页面是否路由到 L3/L4/L5 引擎而非手写 custom)。登记 pages ≠ 页面跑通（消费是另一道工序）。
- **P1 真实范围纠正**：用户说"剩余 26 个"与代码不符。实测 resources 19/19 适用实体已覆盖 + 8 只读视图(7 report_*+supplier_payable)按两条边界裁决正确免登=引擎天花板；pages 仅 5/27（supplier/category/brand/unit/price_type）。真实缺口是 **pages 覆盖率**，非 resources。建议 P1=为"应有列表/编辑页"的实体补 pages 段。

## P1 实施结果（本轮）：补 pages.customer，pages 5→6
- **关键取证**：`pages` 段只被 `pageAssembler.assembleSlots` 消费（档案类 `ArchiveSlotHost` 装配）；单据类页面（AuditLogs/InventoryManage/BackorderManage/PurchaseInbound/AuthCodes/AccessRequests/InboundManage/SupplierPayableManage/AdminUsers 等）全部走 `columns`→`entityCellSpecs`→`cellSpecsWithEditorsToColumns`，**根本不消费 `pages`**。所以"单据补 pages"=死配置，正确做法是**不重复登记**（单据已通过 `columns` 配置驱动覆盖）。`pages` 段现有 5 条中，实际被 `assembleSlots` 消费的仅 supplier；category/brand/unit/price_type 有条目但无 assembleSlots 消费者（预存孤儿，非本任务范围）。
- **真实档案类 pages 缺口 = 仅 customer**（`CustomerManage` 用 `ArchiveSlotHost` 但 `def` 硬编码、未接 `pages.customer`）。product 在旧壳 `ArchiveListPage`+`entityCellSpecs`=Task 5（待 §5 拍板）。
- **改动**：① `data-source/entity-meta.yml` 加 `pages.customer`（6 槽：name/customerType/contacts/invoices/addresses/note，与现有 5 同构）；② `CustomerManage.tsx` 引入 `assembleSlots`、用 `assembleSlots('customer', byKey)` 重排 `def.slots`（镜像 SupplierManage）；③ 跑 `gen-entity-meta.mjs` 重出生成物（pages.customer 已进 entityMeta.generated.ts line 435）；④ 回写台账 §七 P1 行 pages 5→6；⑤ 跑 `gen-boss-view`（完成度 95% 35/37）。
- **门禁**：`verify:static` 8/9 过；S1 fe-typecheck 红，**64 错全在 `ProductSkuSubTable.tsx`**（会话开始即未跟踪，产品统一化 Task 5 在途文件，非本任务引入；本次改动文件 CustomerManage 0 诊断）。该红属外部在途工作，未动。
- **结论**："剩余 26"是类别错误——把"单据列配置"误并入"pages"。真实可补的档案 pages 缺口仅 customer，已补。单据类列配置已在 `columns` 覆盖，无需也不应再补 pages。

## ProductSkuSubTable.tsx 修复（本轮，用户 q-0 决策"现在就修"）
- **现象**：`verify:static` 的 S1 红 64 错全在该文件。根因：它是上一会话 Task 5 Phase 1 留的空壳，基于**不存在的 API 表面**写——`SalePriceItem/PurchasePriceItem` 误从 `baseDataApi` 导入（应在 `UnitPriceExpandPanel`）、`../../../../shared/utils/unitGen.js` 模块不存在、`SkuSearchRow` 误用 `specUnitId/skuId/salePriceItems` 等不存在字段、`buildSaveProductInput(row.productId)` 误传 string（应吃 `ProductView`）、`createSkuPriceColumns` 缺必填 `onPriceTypesChange` 且 `actions` 传错（真实用 `useSkuPriceState` 返回的 `{states}` 无 `actions`，需另构 `SkuPriceRowActions`）、`COL_WIDTHS.TEXT` 不存在（用 `NAME_M`）、`UnifiedTable` 无 `size` 属性、误传不存在的 `inSubTable` 选项。
- **修法**：忠实移植 `ProductManage.tsx` 已验证的 SSOT 接线（`loadOptions` 调 `getSkuOptions` + 转 `SalePriceItem/PurchasePriceItem`；`saveRowPricesCtx` 调 `getProduct→buildSaveProductInput→saveProduct` 做 brandIdx/unitIdx 映射；`actions` 由 `skuPrice` 各方法组装）。**省略 `unitManage`**——`createSkuPriceColumns` 无 `unitManage` 时自动回退纯单位下拉分支（SkuPriceColumns.tsx:174），省去 5 个复杂 handler，不伤编译与价格编辑。文件仍是孤儿（无 import），修复只清门禁、不波及线上。
- **结果**：`npx tsc -p tsconfig.app.json --noEmit` → 0 错；`verify:static` 7/7 PASS（S0/S0b/S1/S2/S3/S3b/S3c），S4/S5 因仅改前端被智能子集跳过。**P1 现可正式交付**。单据页统一到 `pages` 按用户 q-1 决策留作未来独立立项，不归本次 P1。

## v23 P1 / P2a / P2b 进度（本轮「继续」）
- **P1（schema+meta 生成，不动 DB）✅**：`schema.prisma` 新增 `product_name`(Int 主键,name 全局唯一)/`product_category`(productId+categoryId,isPrimary+sortOrder,@@unique([productId,categoryId])) 两模型；`product` 加 `productNameId Int?` + `productName`/`productCategories` 关系（保留 `name`/`categoryId` 至 P4）。`prisma generate` + `gen-entity-meta` 全绿；改动面实测：后端 29 文件含 categoryId、6 含 product.name；前端 27 含 categoryId。
- **工作区清场（用户裁决 A）**：89 个未提交改动打包为本地归档提交 `868d106`（不推送）；随后 P2a `4f75f6b`、P2b `eebc53c` 均独立提交（feat 逻辑隔离）。
- **P2a（product_name 字典基础设施）✅**：`entity-meta.yml` 三处加 `product_name`（entities 展示层 globalDict / resources CRUD 层复用 `product_manage` 权限避权限矩阵手术 / fieldDefs 字典引用）；`registry.ts` 加 `PRODUCT_NAME_REGISTRY = registryFrom('product_name')`（gen 自动产出）。gen + 前后端 tsc 全绿。
- **P2b（后端写路径改写）✅ 仅 typecheck 绿**：`saveProduct.ts` 加 `ensureGlobalProductName`+`syncProductCategory` 辅助；`saveProduct`/`quickCreateProduct` 唯一键从 `categoryId_name` 升为 `productNameId` 全局唯一，写 `productNameId` 并维持 `name` 写透（P4 移除），写完后 upsert `product_category`(isPrimary 主分类,旧主分类降级)。后端 tsc `BE_TS_EXIT=0`。
- **关键依赖**：P2b 代码依赖 **P4 DB 迁移**（建 product_name/product_category 表 + 回填 productNameId/category 关系）才能 runtime 生效；当前仅静态类型绿。P4 是硬闸门（用户单独点确认才跑 migrate deploy + 回填 SQL）。
- **待续**：P2c 前端（ProductEditDialog/ProductManage/ProductPicker/buildSaveProductInput/baseDataApi 接 product_name 字典 + 分类多选）/ P2d meta 的 product 实体接线（name→dictKind:product_name、categoryId→product_category）/ P3 读路径（catalog/searchNormalized/dictMerge/documentLineService/resolveSnapshots）/ P4 DB 迁移闸门。

## 根因分析与预防措施文档（本轮·产品范式化重析）
- 用户反思：标准 3NF 已满足业务目标，却额外造了宽表 `product_sku_search` + 产品名字典，属"局部优化晋升结构真理 + 真相源不唯一 + 层边界穿越"。
- 诊断：蓝图 L5=3NF、§3.3=派生须显式 `@escape` 规则**已有**，缺的是"设计期闸门 + 机器守卫"。预防措施 4 条全部挂靠现有机制：A 数据建模升维清单（设计期硬闸门）/ B L1 真相源唯一守卫（同实体双定义拦截）/ C 层边界 arch-lint（UI 禁依赖宽表结构）/ D 主动信号前置（设计评审列"3NF 已提供却另搞的结构"先消再写码）。
- 文档：`根因分析与预防措施.md`（项目根）。用户选"先出文档审后再落地"。
- ⚠️ **关键矛盾待用户裁决**：本文档把"产品名字典"列为冗余弯路（=本轮用户立场"产品名不需要字典"），但**同日 v23 决策**曾裁定"产品名=全局字典 `product_name`"，且 P1(schema.product_name 模型)/P2a(entity-meta product_name)/P2b(saveProduct 全局唯一键) 已 typecheck 绿、仅 P4 DB 未执行。二者方向**相反**。若确认"无字典"→ 需回退 v23 P1/P2a/P2b（product_name 模型 + 实体登记 + saveProduct 改写）；若保留 product_name 字典 → 文档前提需反转（product_name 字典是 intended 范式设计，非弯路）。待用户审文档时一并裁决。

## v23 回退落地（用户确认"无字典"后执行）
- 用户裁决：确认"产品名不需要字典" → **回退 v23 ①③ 方向**（产品名升全局字典 + 分类改关系表一并作废）。外科手术式摘除，**保留复合体框架** `ArchiveSlotHost`/`archiveSlotTypes`/`ProductSkuSubTable`（属 product 统一化正确方向）。
- 执行：
  - P2b (`eebc53c`，只动 `saveProduct.ts`)：`git revert --no-commit` 精确反转（不提交，留审）。
  - P2a (混杂提交)：手动摘 `entity-meta.yml` 的 `product_name` 全局字典实体(71-84) + fieldDefs 引用(400-401) + resources 段(491-507)；`registry.ts` 删 `PRODUCT_NAME_REGISTRY`。
  - P1 (schema 在 `868d106` 混杂提交)：手动摘 `schema.prisma` 的 `product_name` 模型 + `product_category` 模型 + `product.productNameId` 关系(401-406) + `category.productCategories` 反向关系(363-364)。**教训**：v23 在 `category` 模型也加了反向关系，漏摘会致 `prisma generate` 报 P1012，须全量扫残留。
  - `node tools/gen-entity-meta.mjs` + `npx prisma generate`（不动 DB）重生成；全仓已无 `product_name`/`PRODUCT_NAME_REGISTRY`/`productNameId` 任何引用。
  - 验证：backend `tsc -b` 全绿、frontend `tsc -p tsconfig.app.json && tsc -p tsconfig.node.json` 全绿。
  - `docs-coverage.md` v23 台账 + P0–P5 状态标"已回退/无需"；`MEMORY.md` v23 条目标作废并加"过度抽象红线"偏好。
- DB 未动（P4 从未执行，故无回滚 SQL）。改动**未提交**（按 git 工作流留用户审后提交）。
- 文档 §5 的两条新守卫（B 生成器同实体双定义拦截 / C 层边界 arch-lint 禁依赖宽表结构）**尚未实施**，列为后续；A/D 为方法论动作已随本流程体现。

## 双向架构收口（根因定稿 + 蓝图 v3 + 名全局唯一收口，2026-09-05 晚间）
- **裁决补齐**：v23 撤字典后，用户另选「A 撤字典 · 名全局唯一」——唯一键从 [categoryId,name] 收紧为 `@@unique([name])`（不建字典、product_category 多分类暂不实施）。蓝图升版双向架构。
- **文档定稿**：`根因分析与预防措施.md` 定稿（双向不对称认识：自下而上机制强制清晰/自上而下只有软纪律缺推导链；1:1 拆表与 290/0 实证；预防 A–D 挂靠蓝图 v3 推导链；落地动作表标状态）。`架构蓝图.md` v3：上半部推导链五步 + 范式判据表（0.1–0.4），下半部保留 L1–L5/红线/门禁，§5 工作流加第 0 步推导链、§7 红线加第 9 条、§4.2 结案注记、§9 决策记录。
- **代码收口**：schema `@@unique([name])` + 注释；saveProduct.ts 编辑/新建/quickCreate 3 处、catalog.ts create/update 2 处查重改 `findUnique({ name })`（提示"已存在同名产品（产品名全局唯一）"）；迁移 `20260905120000_product_name_global_unique`（DROP `product_categoryId_name_key` → ADD `product_name_key`）用户确认后 deploy 成功。
- **验证**：运行时只读校验（product 索引实测含 product_name_key、findUnique 命中/未命中正常，无 500）；`npm run verify` 完整 10/10 全绿（含 S6 e2e 冒烟 61.7s）。注意：verify 有 fingerprint 缓存复用，删 verify-report.json 才强制含 S6 全量重跑。
- **机器守卫 B/C 已落地（2026-09-05 收尾，见下节）**：原"遗留"项已实施并验证；git 仍未提交（含 v23 回退 + 名全局唯一 + 守卫改动），按逻辑隔离待用户点头后分批提交、不 push；ProductSkuSubTable.tsx（Task 5 在途）同在未提交集。

## 机器守卫 B/C 落地（2026-09-05 收尾 · 根因文档 §5 行5）
- **B（L1 真相源唯一守卫）= `tools/gen-entity-meta.mjs` 校验段 `validateL1SingleSource()`**：仅实现可机器判定的 **B1 = `entities` 段内同键双定义拦截**（js-yaml 默认静默覆盖最后一份，此处显式 exit 1，防"旧 flat product + subject product 并存"）。挂在生成器，S0 阶段（generate 与 `--check` 都跑）即生效。
- **C（层边界 arch-lint）= `tools/check-arch.mjs` A3 规则**：**剥离注释后**扫描 frontend/backend 源码，禁用名清单 `product_sku_search`（已删派生宽表）不得在非注释代码出现（防作结构真相重新引入）；entity-meta.yml 亦不得再登记。注释里的历史说明不算违规（否则 10 处注释引用会误报阻断）。挂在 S0b arch-lint。
- **关键教训（B3 不可行）**：曾试"1:1 过度归一化字典"自动判定（globalDict 仅被 1 实体引用即报错），但 `entity-meta.yml` 的 `relations` 只登记了部分跨实体引用（spec 对 brand/unit/price_type 的关系没登记）→ 引用计数不可靠，把 brand/unit/price_type 误判为"1:1/孤儿"。**结论：1:1 字典过度归一化无法机器可靠判定，归入 §4-A 设计期推导链人类闸门**（蓝图 v3 上半部），不进机器守卫。
- **验证**：`node tools/gen-entity-meta.mjs --check` 与 `node tools/check-arch.mjs` 均绿；`npm run verify:static` 7/7 PASS（S4/S5 因改动仅涉 frontend/tools 被智能跳过，非静默丢弃）。根因文档 §4-B/C 与 §5 行5 同步标"✅ 已落地"。

## 档案类统一化 v2（通用 displayLevel + 运行时切换，2026-09-05）
- 用户裁决推翻 v1「product 专用 composite 后门」方案：框架要**通用、按层级配置**，不能给 product 开后门；且要给用户**运行时视图切换**（分组/平铺）；SKU 量级按**生产级几百万~上千万**设计，聚合走**后端索引**，禁止前端全量聚合。
- 用户批评我的方法论失误：① 把"成本最低"（前端聚合零风险）当"推荐"推给他——违反「长期架构方向>短期低成本，反对最小可行优先」；② 把 product 当特例开"后门"。
- 设计文档 `档案类页面统一化设计.md` 已重写 v2（§0/§2/§3/§4/§5/§6）：通用 `displayLevel`('entity'|'parent') + `childLevel`（声明式子级展开）+ `flatView`（运行时平铺 def）+ 后端产品级索引查询（GROUP BY productId 复用现有 FULLTEXT 召回）+ 运行时切换开关。
- **Phase 1 框架通用化已落地并验证**（`verify:static` 7/7 绿）：`archiveSlotTypes.ts` 原 `ArchiveCompositeSlot`→通用 `ArchiveChildLevel<T,C>`（childApi/childRowKey/childColumns/summary 全索引有界）；`ArchiveEntityDef` 加 `displayLevel`/`childLevel`/`flatView`；`ArchiveSlotHost` 改名 `ArchiveSlotHostInner` + 新增默认导出包装（displayLevel==='parent' 且 flatView 时渲染 分组/平铺 开关、切换 activeDef），composite 列分支→通用 childLevel 摘要列，展开→异步 `ChildTable`（childApi 有界查询）。supplier/customer 因 displayLevel 默认 entity + 无 childLevel/flatView，**行为零变化**。
- 剩余 Phase 2（后端产品级索引查询 groupBy='product'）/ 3（建 productDef）/ 4（ProductManage 换壳）/ 5（槽化删旧壳）待续；运行时切换平铺模式需 product 在 Phase 3 提供 `flatView` def。
- 改动未提交（按 git 工作流待用户点头后分批提交、不 push）。

## Phase 2 后端产品级索引查询已落地（2026-09-05）
- 新增 `searchProductsGrouped(params)`：`search.ts` 复用现有 `searchProducts` 索引召回（候选集 LIMIT 500，FULLTEXT+范式表）拉满，再 `groupSkuRowsToProducts` 纯函数 `GROUP BY productId` 落产品级一行（带 skuCount/brandCount），做产品级分页。前端绝不做全量聚合（生产级量级）。
- 纯函数 `groupSkuRowsToProducts` 放 `search-scoring.ts`（零 DB 依赖，可单测），由 `search.ts` 与单测共用，避免测试引 prisma。
- 配套：controller `searchProductsGroupedHandler`、路由 `/staff/products/search/grouped` + 公开 `/products/search/grouped`、前端 api `searchProductsGrouped`（`baseDataApi.ts`）。
- 单测：search-scoring.test.ts 加 4 例（同 productId 聚合/skuCount/brandCount 去重/updateTime 降序）。
- **验证**：`npm run verify:static` 9/9 全绿（S4 后端单测、S5 后端类型均过）。
- 设计文档 §4 Phase 1/2 标 ✅ 已落地。Phase 3（建 productDef：displayLevel='parent'+childLevel+flatView）待续。

## 失败模式沉淀入方法论（形状≠身份 / 规模预算 / 推荐资格过滤）
- 用户审《档案类页面统一化设计.md》v1 指出两错（v1 已由并行会话重写为 v2）：①「product 唯一例外、专用 composite 槽」——抽象维度错：把实体身份当形状；②「前端聚合零风险（推荐）」——代理准则：违反长期方向优先与千万级 SKU 量级。归类＝同型失败第二次（此前"低成本/最小可行"已被纠正一次）。
- 落点：扩展方法论 `文档可视化/data-source/methodology/items/know-loop.yml`（接到需求先归类·L0）——新增两判据表（参数化测试：形状≠身份 / 规模预算进抽象）+ rules 三条（形状≠身份 / 规模预算进抽象 / 推荐先资格过滤），并把三条移到 rules 前 5 位使 AGENTS 摘要可见。`node tools/gen-docs.mjs` 通过（42 篇），AGENTS know-loop 段已含三新纪律。
- 坑：YAML 标量不能以 ASCII 双引号开头（`- - "唯一例外"...` 触发解析错），全文统一「」引号；AGENTS 指令派生 = `page.rules.slice(0,5)` + 首条含 自检/验收/禁止/违规/没反应 的规则作自检行——想让新纪律上 AGENTS 摘要必须放 rules 前段。
- 状态：methodology 真相源已改、生成物已同步；git 未提交。

## 文档之家裁决 A（2026-09-05 晚间 · 文档归位）
- 用户发现：架构蓝图/根因分析/档案统一化设计 等系统决策文档直接丢仓库根，"文档可视化地位没明确"。裁决 A：**文档可视化＝全仓人读文档的内容之家**；仓库根只留工具加载层（AGENTS/docs-coverage/开发规划/掌控台等）。
- 落地：① know-layout 加「文档家族归属」表（工具加载层=根 / 方法论条目=data-source/methodology/** / 系统决策与设计=项目文档/** / 业务归档）＋新文档归属自检（给谁读/谁加载/真相源是谁）；② 三个决策文档 `mv` 入 `文档可视化/项目文档/`；③ AGENTS 顶注加"文档之家"段 + 动手前先读路径改为 项目文档/架构蓝图.md；④ gen-boss-view/check-arch 引用文案同步为新路径；⑤ gen-docs + gen-boss-view 已重跑通过。
- 注意：docs-coverage/开发规划/migration 等对这三文档仍用裸文件名文字引用（同文件名字、家已变），未逐处改路径；`废弃的可参考文档/` 与 `用户项目开发文档` 删除态为并行会话作业，未纳入本次归位。git 未提交。

## P0-1 verify 假绿洞修复（2026-09-05 晚间 · 工具小修）
- 现象：全量 `npm run verify` 复用了 `--skip-smoke` 的静态报告，静默跳过 S6 冒烟仍判 PASS（删 verify-report.json 才逼出真跑）。
- 根因：`report.scope` 按「本次运行自己的 STAGES」算——静态跑时 S6 根本没进 STAGES，9/9 全跑即标 scope=full；且并发锁复用路径只比 fingerprint。两个洞都允许 partial 顶 full。
- 修复（tools/verify.mjs，最正确方案非最省事）：报告新增 `ranIds`（真实跑过的 stage）+ `wantedIds`；新增 `covers()` 超集覆盖判据，复用条件=ranIds ⊇ 本次 needIds 且全绿且指纹一致；scope 仅作展示不再作判据；报告写盘改为静态跑 scope='static' 等准确值。并发锁等待路径同步改用 covers。
- 验证：负向——先 verify:static 后全量 verify 拒绝复用、真跑含 S6 10/10（68.9s）；正向——同状态全量二次运行复用 0ms exit 0。git 未提交。

## 架构校准三件套落地（2026-09-05 深夜 · "刹车校准"）
- 用户真实意图：项目一路写代码堆功能没有"刹车校准"；进度看得见、架构腐化（范式偏离/平台后门/文档代码割裂）是隐性的，今天不纠偏就成千万级时代的毁灭性历史债。触发=每批交付后（非固定周期）。
- 落地：① `tools/arch-review.mjs`（新）聚合 verify 红绿 / signal HIGH·MED / 台账❌🕐⏳ / 规划在途 → `arch-review.json`；② 掌控台新增「架构校准」区（gen-boss-view 刷新自动跑校准器：该刹车了/校准态 + 发现口令 + 深度体检口令「架构校准（上帝视角）」六视角）；③ AGENTS 一·七 交付门禁加"verify 全绿后跑 arch-review，架构级新发现先立案/进待拍板再标交付"。
- 实测：`node tools/arch-review.mjs` 输出摘要；gen-boss-view 重生成后掌控台.md 出现「架构校准」区（P2×3：MED14 / 台账❌2🕐1⏳2 / 规划在途）。arch-review.json 加入 .gitignore（生成物）。git 未提交。
