# 采购清单手写单据式录入体验优化 任务清单

## 核心重构

- [ ] **任务 1: 修复虚拟滚动机制**
  - [ ] 1.1 检查 antd Table 的 `virtual` prop 是否正确启用
  - [ ] 1.2 验证 `scroll={{ y: tableHeight }}` 配置是否正确计算
  - [ ] 1.3 使用浏览器开发者工具检查 DOM 中 `<tr>` 节点数是否 ≤ 30
  - [ ] 1.4 滚动时观察节点是否动态替换（而非移动）
  - [ ] 1.5 验证虚拟滚动与分页共存时的行为（翻页后滚动重置到顶部）

- [ ] **任务 2: 修复样式一致性问题**
  - [ ] 2.1 检查 `CELL_SHARED_STYLE` 是否包含 `boxSizing: 'border-box'`
  - [ ] 2.2 检查 `CELL_TEXT_STYLE` 和 `CELL_INPUT_STYLE` 是否完全继承字体度量
  - [ ] 2.3 验证文本态 `<div>` 和编辑态 `<input>` 的 computed style 完全一致
  - [ ] 2.4 确保焦点态仅添加 `box-shadow`，不改变 padding/border/margin
  - [ ] 2.5 点击单元格时验证无视觉跳动（无位移、无闪烁、无大小变化）

- [ ] **任务 3: 实现渲染优化**
  - [ ] 3.1 使用 `useMemo` 缓存 `mergedColumns`，依赖项包括 `columns`、`editingCell`、`internalRows`
  - [ ] 3.2 使用 `React.memo` 优化行渲染，确保未变化的行不重渲染
  - [ ] 3.3 验证编辑态切换仅触发目标单元格重渲染
  - [ ] 3.4 使用 React DevTools 验证重渲染范围

## 验证与测试

- [ ] **任务 4: 性能验证**
  - [ ] 4.1 验证 50 行数据：DOM 节点数 ≤ 30，滚动流畅，FPS ≥ 55
  - [ ] 4.2 验证 100 行数据：DOM 节点数 ≤ 30，滚动流畅，FPS ≥ 55
  - [ ] 4.3 验证 200 行数据：DOM 节点数 ≤ 30，滚动流畅，FPS ≥ 55
  - [ ] 4.4 验证 500 行数据：DOM 节点数 ≤ 30，滚动流畅，FPS ≥ 55
  - [ ] 4.5 验证点击编辑响应时间 < 50ms

- [ ] **任务 5: 视觉验证**
  - [ ] 5.1 验证点击单元格时文字无位移
  - [ ] 5.2 验证点击单元格时文字无闪烁
  - [ ] 5.3 验证点击单元格时文字无大小变化
  - [ ] 5.4 验证仅底部出现 brand 色细线（焦点指示）
  - [ ] 5.5 验证失焦后切换回文本态无视觉跳动

- [ ] **任务 6: 功能验证**
  - [ ] 6.1 验证空订单：第 1 页显示 13 个空行，点击任意单元格可录入
  - [ ] 6.2 验证数据行 + 空行：5 行数据 + 8 个空行，点击空行可录入
  - [ ] 6.3 验证翻页：翻到第 2 页显示空行，可录入
  - [ ] 6.4 验证调整每页行数：切换为 20 行/页，重新分页
  - [ ] 6.5 验证键盘导航：Enter/Tab/Escape/ArrowDown/ArrowUp 行为正确

## 代码质量

- [ ] **任务 7: TypeScript 编译检查**
  - [ ] 7.1 运行 `npx tsc --noEmit` 确保零错误
  - [ ] 7.2 检查是否有未使用的导入或变量
  - [ ] 7.3 检查类型定义是否完整

- [ ] **任务 8: 代码审查**
  - [ ] 8.1 检查是否有冗余的 state 或 ref
  - [ ] 8.2 检查是否有不必要的 useEffect 或事件监听
  - [ ] 8.3 检查是否有内存泄漏风险（未清理的定时器、监听器等）

## 文档更新

- [ ] **任务 9: 更新交互范式规范**
  - [ ] 9.1 更新 `用户项目开发文档/架构原则/交互范式规范.md`
  - [ ] 9.2 说明虚拟滚动的正确配置方式
  - [ ] 9.3 说明样式一致性的关键要素
  - [ ] 9.4 说明渲染优化的最佳实践
