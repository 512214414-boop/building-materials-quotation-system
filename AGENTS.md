# AI 执行卡 · 建材报价系统

> 本卡由 tools/gen-docs.mjs 从真相源 `文档可视化/data-source/methodology.yml` 生成（GEN 节）+ 手写总纲。
> **改任何方法论只改真相源一处，跑 `node tools/gen-docs.mjs`，站点与本卡自动同步；禁止手改 GEN 节与 js/data/gen/**。

## 一、对话分流（任务类对话的第 0 步）

轻量对话（纯澄清、概念追问、闲聊）只定性、直接回答，不输出分流仪式。
要干活的对话，走完五步再动手：

1. **判类型**：需求整理 / 变更调整 / 纠错反馈 / 澄清问答 / 闲聊。按用户原话形态定，不猜意图。
2. **判产物**（先查 `docs-coverage.md` 台账，不靠记性）：
   - 文档没写 + 讲清了新要求 → **文档任务**：先补文档，再排代码
   - 台账显示已规定未实现 → **代码任务**：禁止重复生成文档
   - 文档与代码矛盾 → **双任务**：先按裁决链改文档定对错，代码跟文档
   - 纯实现细节要调 → **代码任务**：先读对应方法论再动
3. **定文档分层**（要补文档时）：指导思想层（业务规则变）/ 规格层（字段、交互、确认策略变）/ 代码层（纯实现）。多数日常调整只动规格层，不必每次惊动指导思想。
4. **查方法论**（见技能索引）：命中就声明「走哪条、第几步」，先读对应篇再动手；查不到且属重复信号 → 走沉淀。
5. **报验收**：干完必须给出可操作的行为验收清单（打开哪页、点什么、应看到什么），从文档规则反推；复杂改动跑 e2e 截图；回写台账。

## 一·五、输出必须可见（用户要能确认你按体系工作）

每轮任务对话：回复**开头**给出分流卡，结束给出验收清单。

```
【分流】类型：<需求整理/变更调整/纠错反馈/澄清问答> ｜ 产物：<文档任务/代码任务/双任务/无任务> ｜ 方法论：<篇名 · 第几步>
【验收】1. … 2. …（打开哪页、点什么、应看到什么）
```

**理由**：分流若只发生在 AI 内部，用户无从判断 AI 是按体系走还是凭感觉走——
**不可见＝不可验收＝体系形同虚设**。轻量对话（纯澄清/追问/闲聊）不需要。

## 一·六、操作不中断（任务一口气做完）

- **该做的全部做完**：不允许做完一段停下来问"要不要继续"——中断式提问浪费用户回合
- **需要用户决策的**：先记下、继续做完其余，最后用 **IDE 的选项提问面板（ask_followup_question）** 一次性给出，让用户点选——不占对话回合、不中断流程
- **能自己决定的不要问**：有依据就按裁决链（用户原话 > 现场 > 文档 > 代码）自行决定并在验收清单里说明

## 二、裁决链（冲突时从高到低）

**用户原话意图 > 现场事实 > 文档可视化 > 代码现状。**

冲突时摆出各方说法、引用裁决链，让用户做确认题。用户是最终裁决者。

## 三、技能索引（触发条件 → 先读哪篇 → 篇章文件）

> 本表**由 `tools/gen-docs.mjs` 从真相源生成，禁止手改**（GEN 节）。
> 改触发词 = 改真相源条目的 `index.hear`；加一篇 = 真相源加条目。改完跑 `node tools/gen-docs.mjs`。
> 手写索引必然漂移——2026-08-30 事故：路径写成不存在的 `04-why/`、9 个编号错 7 个、20 章只登记 9 章。

<!-- GEN:INDEX:BEGIN -->
| 听到什么 | 先读哪篇 | 文件（文档可视化/js/data/gen/） |
|---|---|---|
| 识别到表功能（存数据/列清单/挂关系/留痕迹） | 表功能方法论 | `01-know-table.js` |
| 要新增一个表功能、要描述字段有哪些维度、要填登记表 | 表功能元模型 | `02-know-meta.js` |
| 本章定位 | 本章定位 | `03-why-scope.js` |
| 销售开单 | 销售开单 | `04-why-sales.js` |
| 配货履约 | 配货履约 | `05-why-fulfill.js` |
| 收付款往来 | 收付款往来 | `06-why-money.js` |
| 售后 | 售后 | `07-why-after.js` |
| 公共能力 | 公共能力 | `08-why-shared.js` |
| 总纲领 | 总纲领 | `09-why-canon.js` |
| 进货管货 | 进货管货 | `10-why-inbound.js` |
| 货和人怎么存在 | 货和人怎么存在 | `11-why-objects.js` |
| 单怎么过手 | 单怎么过手 | `12-why-flow.js` |
| 人习惯怎么干 | 人习惯怎么干 | `13-why-habit.js` |
| 对话要沉淀成指导思想、切一类活 | 对话 → 指导思想 | `14-know-method.js` |
| 想看方法做通的实样 | 对照本项目 | `15-know-cause.js` |
| 新项目下发文档 | 下发文档范本 | `16-know-brief.js` |
| 接到任意功能需求 | 接到需求先归类 | `17-know-loop.js` |
| 要填登记表（entity-meta.yml）、要新增一个表功能 | 登记表填写口径 | `18-know-metaschema.js` |
| 建/改文档站点、往里加一章 | 文档项目怎么分层 | `19-know-layout.js` |
| 文档改动记账 / 出问题回滚 | 版本记录与恢复 | `20-know-recover.js` |
| 每轮任务对话开工前 / 判完类型判产物后 | 对话分流 | `21-know-route.js` |
| 设计/评审格子点击交互、接到『点一下要几层』『这个按钮要不要』类问题 | 格子点击 → 确认层 | `22-cell-gate-path.js` |
| 同类问题第二次出现 / 高代价教训 | 怎么沉淀方法论 | `23-know-precipitate.js` |
| 采购入库、囤货补货、不绑订单的进货 | 采购入库 | `24-order-inbound-purchase.js` |
| 待入库管理、超额调货待入、一键确认入库 | 待入库管理 | `25-order-inbound-pending.js` |
| 库存台账、查库存、期初建档、盘点调整 | 库存台账 | `26-order-inbound-inventory.js` |
| 欠库台账、库存缺口、挂欠库、补货清单 | 欠库台账 | `27-order-backorder.js` |
| 供应商应付、欠供应商多少钱、结算、对账单、账龄 | 供应商应付 | `28-order-payable.js` |
| 经营报表、看数、毛利、周转、账龄、绩效 | 经营报表 | `29-ops-report.js` |
| 角色权限、配谁能看谁能改、三档矩阵 | 角色权限 | `30-sys-role.js` |
| 审计日志、谁干了什么、留痕、操作记录 | 审计日志 | `31-sys-audit.js` |
| 授权码、客户准入、发码、吊销 | 授权码 | `32-sys-auth-code.js` |
| 访问申请、客户申请准入、审核通过驳回 | 访问申请 | `33-sys-access-request.js` |
| 用户管理、员工账号、停用启用、重置密码 | 用户管理 | `34-sys-user.js` |
| 设计/评审一个表格页面、接到『这个页面由哪几行拼』『这格点下去发生什么』类问题 | 表格 UI 分层 | `35-ui-layer-model.js` |
| 该不该重构、架构级调整、以前的做法限制了、历史包袱、推倒重来、架构升级、识别重构信号 | 架构重构信号 | `36-refactor-signals.js` |
| 元模型驱动全栈、登记表生成前后端、资源引擎、页面装配、操作守卫声明化、宽表与检索演进、范式多路召回 | 元模型运行时 | `37-meta-runtime.js` |
| 画布、缩放、行盒子、设计令牌、视觉稳定性、移动端适配、浮层载体、信息密度 | 视觉与画布 | `38-visual-canvas.js` |
| 新建文件、类型迁移、同名 js、改了没生效、构建校验、模块引用后缀 | 工程纪律 | `39-eng-discipline.js` |
| 导航、路由、菜单声明、位置记忆、视图保活、底部操作栏、返回前进 | 导航与位置记忆 | `40-app-navigation.js` |
| 前后端职责切分、算力放前端还是后端、性能预算、谁做校验 | 前后端职责 | `41-fe-be-duties.js` |
<!-- GEN:INDEX:END -->

本卡的上游全文：对话分流见 `js/data/gen/19-know-route.js`（序号由真相源顺序决定，重跑后以本表为准）。

## 四、硬纪律（随时生效）

> **总纲 · 先升维再动手**：拿到任何问题，先做升维提问——① 归类到类型（这属于哪一类问题？）② 查该类的通用最优范式（不只看本项目）③ 定位本项目现状版本（雏形/低配/误用/正确），方案 = 范式 − 差距。**没说出「这属于哪一类问题」就动手 = 没升维。** 所有「就近/将就/复用成将就」的事故，根子都是跳过这一步。更优解靠升维，不靠用户提醒。

- 改前先查同类；共性走槽，差异 override 并登记
- **确认层范式**：一切可编辑单元格＝点值→确认层→应用（ArchiveFieldCell / PickerNameCell / PickerNumCell），空行新增走 ArchiveEmptyFieldCell / PickerEmptyName，**禁止常驻输入框**（全局已取消）。空行确认＝无条件晋升为记录行并自动补新空行（useMatrixRecords 范式，供应商联系/地址矩阵是范本），**禁止确认后被守卫静默丢弃——零反馈即违规**
- **门禁用提示不用静默**：前置条件未满足的格子（备注挂规格名、售价挂单位等依赖顺序）保持正常视觉——hover / 分割线 / 行高与可编辑格一致，点击给提示「请先 X」（DisplayCell 的 rejectReason）。禁止默默不可点、禁止置灰消失——视觉不一致就是又一处各写各的
- **查同类先查资产清单**：形态→共用组件的对照表在「表功能方法论」（文档可视化 #know-table）。动手前先查表命中组件，再读范本实现（供应商联系矩阵 ArchiveContactMatrixEditor），邻近实现只能校验、不能当参照物。事故实例：要求对齐供应商矩阵，却参照同弹窗单位区选了低一层的 EntityPanel
- **矩阵走共用组件**：多记录矩阵一律 MatrixTable（C29，内含 EntityPanel），单元格一律 ArchiveFieldCell / ArchiveEmptyFieldCell（供应商联系信息矩阵是范本），禁止弹窗内第二套矩阵
- **涉及用户能感知的形态，先摆选项再做**：交互/导航/布局/视觉类改动，先给 2–3 个候选 + 各自适用条件 + 推荐，让用户选；纯技术实现（数据结构/接口/算法）才直接给最优解并说明理由。**禁止替用户决定他看得见的东西**
- **复用前适配度三问**：沿用现有组件/模式前必须答：① 它当初为解决什么问题设计？（顶部 tab：5 段短篇）② 我的场景和它的前提差在哪？（10+ 节长文）③ 差异超阈值就换方案——**复用不是将就**
- **先保住体感再定问题**：用户的抱怨原句（划很久/找不到/分不清）是最高优先级信号。翻译成技术任务前先复述一遍体感，**禁止把「体验问题」窄化成「加个组件」**——问题定义窄了，解空间就窄了
- **长文导航范式**：3–6 节顶部 tab 切换；7 节以上侧边目录+滚动联动（指示器，内容不切碎）；20 节以上加分组折叠与搜索
- **编辑矩阵形态由层级+表类型推导**：父实体切换=按钮标签；挂载子表=表格行+确认层+空行晋升；行级数=行内确认层格；跨记录数=展开/弹层面板；全局字典=检索+快建。同一形态槽全站同组件，新增表先画进关系图定层级，形态自动确定
- **查同类查到交互层**：改任何表格/矩阵，先逐格对照同场景成熟实现（如产品编辑弹窗对照单位区）——骨架和单元格交互都要比对，只对骨架=没查
- 快照与标注分离；演进不改历史；统计基于 ID
- 先文档后代码；文档没规定的先补文档

## 五、沉淀（方法论库的维护）

- 信号：同一教训第二次 / 同一问题跨功能第二次 / 同一偏好讲第二遍 / 规则多处被违反 / AI 踩坑后找到稳做法 / **高代价教训一次也立即沉淀**
- 流程：留证据 → 提炼（触发条件+步骤+否决项）→ 去项目化 → 可证伪 → **登记进真相源** → 回写旧篇
- 登记：`文档可视化/data-source/methodology.yml` 加条目 → 跑 `node tools/gen-docs.mjs`，站点/侧栏/执行卡自动同步。**没进真相源=不存在。**

## 六、台账

`docs-coverage.md`：文档↔代码覆盖账。判产物前先查；任何文档/代码改动后回写状态。

## 七、访问地址与命令中心

全项目所有能打开的界面，**单一来源是生成物，禁止手写维护**：

- 总表：`访问地址.md` —— `node tools/gen-access.mjs` 从 dev.sh 等代码自动提取 + 实时探测在线状态
- 中文命令中心：`~/.bmq/开工.sh` —— 手机远程（UU 命令行）敲「开工」出菜单；直接敲中文命令亦可
  （启动项目 / 刷新项目 / 重启项目 / 停止项目 / 项目状态 / 建公网 / 文档站 / 看进度 / 对话 / 接回对话 / 全部地址）
- 总表里「用中文命令启动」列与命令中心一一对应：**改命令必须同步改总表，反之亦然**——两边不一致就是第二套实现
- 改端口只改来源（dev.sh / 各服务脚本），再重跑生成器；在总表上手改 = 违规
- CLI 与 IDE 同源：两边都读本卡与 `docs-coverage.md`，工具链只有一套，不因接入方式不同而分叉

<!-- GEN:BEGIN -->
## 方法论指令（由 tools/gen-docs.mjs 生成，禁止手改 · 真相源 文档可视化/data-source/methodology.yml）

### 表功能方法论（L0）
- 触发：识别到表功能（要存数据/列清单/挂关系/留痕迹）
- 先升维再动手：归类到类型 → 查该类通用最优 → 定位现状版本（雏形/低配/误用/正确）
- 画关系图（实体+字段+关系+场景），图没确认不建表不写组件
- 按图推集合体与分层；集合体、分层是推论，不由实现顺手定
- 共性插槽自动产出五面，差异 override 登记；禁止视图手写第二套列
- 变更回图：任何表变更先改图/登记表，快照层新字段只对新落单生效
- 自检：有没有关系图？集合体和分层是不是从图推出的？有没有第二套手写列/第二套确认？

### 表功能元模型（L0）
- 触发：要新增一个表功能、要描述字段有哪些维度、要填登记表
- 先升维再动手：归类到类型 → 查该类通用最优 → 定位现状版本（雏形/低配/误用/正确）
- 画关系图（实体+字段+关系+场景），图没确认不填登记表
- 按九组视角填登记表（身份/语义/来源/关系/呈现/行为/检索/历史/权限），值来源与关系方向不能空
- 推导五面：列表/编辑/检索/快建/确认从同一份声明产出；差异走 override 并登记
- 变更回表：加字段改关系先改登记表，五面自动跟
- 自检：有没有第二套手写列/第二套确认？九组视角填全了吗？值来源和关系方向标了吗？

### 本章定位（L1）
- 触发：先看门店怎么干活，再设计系统
- 阅读顺序：定位 → 五类活 → 货和人怎么存在 → 单怎么过手、谁在用、事后看什么 → 人习惯怎么干 → 公共能力 → 总纲领。写法见「方法论库 · 对话 → 指导思想」。
- 本项目的一类活：销售开单、配货履约、进货管货、收付款往来、售后。这是这家门店切出来的，不是通用目录。
- 作业之外必须写：货和人怎么存在、单怎么过手、谁在用、事后看什么、人习惯怎么干。缺一条，实现就没有源头。
- 每环怎么写：四块写法和效果精度见「方法论库 · 对话 → 指导思想」。本章只填这家店的现场和效果，写到能约束实现。
- 公共能力从业务来：几类活碰到同一类现实问题才抽成一套。不是为了少写代码。

### 销售开单（L1）
- 触发：柜台前那几十秒：说到哪写到哪，用这行字去探、去打电话
- 禁止：开单拆成多个输入框、切看法清字、要求格式规整、对不上就不让保存、把整串口语当产品名建档、按词把有效结果禁掉、把渠道名单当成商品名来搜、没有客户档案就不让开单、造一份假的临时客户去凑名单。
- 开单和管理不是同一套填法：开单混写；档案平时分列维护。匹配规则要同一套，填法不要合成一个框。管理页筛档案、开单格探货，是两套框。
- 找人问是看法，逛名册是只读：开单切到找人问：用这一行字列出谁可能供，插入仍是货。另有一处只读逛「谁还可能供」，不占开单这一格、不改插入。
- 自检：禁止开单拆成多个输入框、切看法清字、要求格式规整、对不上就不让保存、把整串口语当产品名建档、按词把有效结果禁掉、把渠道名单当成商品名来搜、没有客户档案就不让开单、造一份假的临时客户去凑名单。

### 配货履约（L1）
- 触发：货怎么到客户手里：店里有从哪出，没有走刚问到的渠道
- 店里有还是没有，是两种出法 → 内部仓和外部渠道分开。有货从仓出，没货从渠道走。
- 送到哪是开单时问清楚的 → 地址当时抄在单上。档案里后来改的地址，不自动改已经在走的单。
- 货不够 → 能出的先出。内部仓出不够，系统当场记下欠着；货进仓时冲掉欠。不能假装已经出齐。
- 仓里出的这笔成本 → 按仓里当时的均价算，不拿今天档案进价去改。
- 外面调刚好够这笔单的 → 当时问到的进价算进这笔订单成本。

### 收付款往来（L1）
- 触发：对着当时那张单：跟客户收，跟供应商付
- 收的是当时报给客户的价 → 往来对着单上当时抄下的金额。档案改价不改已收、已对的数。
- 付给供应商的是当时问到的价 → 应付钉在当时进的那笔货、当时谈的价上，不拿后来的档案进价去改。
- 定金和尾款会拆开收 → 允许开单当时就收一笔，货到再收。不强迫先配完货才能记收款。

### 售后（L1）
- 触发：对着柜台上的货去对已经卖掉的行；认全了才能回仓
- 柜台上的货顺序是乱的 → 单据检索和已卖行检索是两个独立格子，不要塞进同一个框。先对单据（宽松打头上所有字，或切精准）。点格进确认层；空词按当前日窗口翻清单（默认当天，上一天下一天、上月下月，也可以点日期跳），一次不要灌很多。退货周期可能隔半年、一年，必须能翻到当时那张单，禁止只列出当天。打字检索不锁日期。行上能预览确认是不是这张。勾上的单当数据源。再对着这些单里已经卖掉的行打名称。对上了才落到那张或那些原单。顺序跟柜台上的货走，不跟原单行号走。禁止必须先打开一张已知的单再顺着行号找。
- 一次可能退好几张单 → 这一趟可以对上多张当时的单。每一件仍钉在它当时那一行。禁止逼人先做完第一张再打开第二张。
- 他可能在几家买过 → 只能退本店当时确实卖掉的数量。没对上已经卖掉的行，不能按嘴里说的数量退。
- 卖价很活，这个人一个价 → 退的价是当时卖给这个人、写在那一行上的价。不是名册现在的标准价。档案改价不改已卖掉的那行。
- 散客也会退 → 水电常买多用剩。没建档的和建档的都能退。开单时没记人档，不挡这一趟售后。
- 自检：禁止必须先打开一张已知的单再顺着行号找；一次只许退一张单；按名册现在的价退；没对上本店卖掉的行就按嘴里说的数量退；散客不许退；造一份假的临时客户才准退。

### 公共能力（L1）
- 触发：几类活碰到同一类现实问题，才抽成一套

### 总纲领（L1）
- 触发：方案两难时对照这里，不要对照按钮长什么样
- 系统适配人：贴近手写：残缺也能先记下，跟得上嘴。不让人先填齐表再干活。
- 一段字是线索：混在一行里是正常经营。换看法不改这行字。字在名称里或名称在字里都算碰上。找不到档案不能停业务。
- 没货就找人问：列出可能供的渠道，留得住电话。进价滞后不等于不供。出货时仓和渠道不是一份名单。
- 当时抄死：名称、价、地址、往来按当时单上的走。改数量不刷名称。档案变更不改历史。结清只读。退货按当时卖给这个人的价，不按名册现价。
- 三种出法三种成本：仓出按当时均价；外面刚好够这笔的算进订单成本；外面多调的不算订单成本、货进库存。

### 进货管货（L1）
- 触发：货进店：点进哪一仓，仓里还有多少，多调的先进库存
- 货到了要点进仓 → 能收进某一仓，库存跟着变。
- 还没到的记着 → 待入的货能看见、能确认进仓。
- 仓里还有多少 → 按仓、按货能看见现在有多少。
- 多调的 → 进库存。不算这笔客户单的成本。口径见履约。

### 货和人怎么存在（L1）
- 触发：东西在世界上怎么分层，开单那行糊字才能打到
- 本页：货/人/地/渠道在世界上怎么分层，效果硬到能约束改点位、建档。
- 档案五段：出发点指回哪一类活；要支持到才把经营事实推成必须拆的层。不要在五段重写本页。

### 单怎么过手（L1）
- 触发：一张单怎么过阶段、谁在用、事后看什么

### 人习惯怎么干（L1）
- 触发：人是对着一张表往下写的，系统就按这个习惯做

### 对话 → 指导思想（L0）
- 触发：对话要沉淀成指导思想
- 先切后写：一类活的名字来自这个现场，不来自上一份文档。
- 六条线一起交提纲：不要先写完一类活再「另写」。对象/过程/角色/看数/工具是指导思想的正文，不是附录。
- 目录不是流水线：阅读可以有顺序。现场可以交错。
- 公共能力后置：各摊写完再抽。抽的判据是同一类现实问题。
- 总纲领最后：开篇只放这个现场共用的现实。不要一上来口号。

### 对照本项目（L1）
- 触发：要看方法做通的实样
- 看这一页：看补完后追源表。那是方法要求的精度。
- 写下一项目：回到「对话 → 指导思想」，按新现场交六条线提纲，不要抄本页的开单/配货/进货。
- 本项目开发：新功能先归到指导思想对应页。效果原句能否决你的实现，才动手。

### 下发文档范本（L0）
- 触发：新项目下发文档
- 先交提纲再动手：让 AI 先交拟切的一类活 + 复合体类型 + 范式提纲 + 不变量清单，确认前禁止画页面、建表。
- 复合体先分叉：一组表先打包定复合体类型（档案/单据），行为分支从这里分。再往下才是单表类型。不要跳过这一层直接到单表。
- 范式层只写怎么写：UI/交互/弹层/字典的统一规则，硬化到能否决一种实现。不写业务对错。
- 不变量层只写写什么对：业务规则带否决条件。不写 UI。范式和复合体都管不到的，全在这里。
- 现状从代码提取：不手写维护现状描述。代码一变文档就脱节，脱节就返工。
- 自检：先交提纲再动手让 AI 先交拟切的一类活 + 复合体类型 + 范式提纲 + 不变量清单，确认前禁止画页面、建表。

### 接到需求先归类（L0）
- 触发：接到功能需求
- 先升维再动手：没说出「这属于哪一类问题」就动手=没升维。归类到类型、查该类通用最优、定位现状版本，三步走完才决定方案。更优解靠升维，不靠用户提醒。
- AI 主动抽象不等用户写好：用户零散说，AI 自己打包复合体、归类、命名，反过来摆给用户看。抽象这件事不该让用户做。
- 归类按现场不套固定名词：复合体类型和单表类型由现场形态定，AI 自己命名。不限于档案 / 单据两种。
- 改前先查同类：改任何功能前先查项目有没有同类公共能力，有就改公共槽位。这是杜绝各改各的根。
- 提炼差异与共性摆给用户：抽象完把差异点和共性摆出来，让用户一眼看清，帮他理清需求。

### 登记表填写口径（L1）
- 触发：要填登记表
- 顺序是配置值：pages.slots 数组顺序 = 列表列顺序，不推导、不在页面里手写第二份。
- 列三路合成：声明列 + 关系推导列 + slot 占位合并（mergeColumns）。页面手写第四路列来源＝违规。
- 反常设计必须登记：有意识冗余、绕开框架的例外，都要留一句「为什么这样是对的」，没登记的反常＝待修 bug。
- 禁止第二套手写列 / 第二套确认：有就是绕过登记表，违规。
- 边界：判定数据能描述为「字段/行数/状态/角色」→ 声明化；依赖多请求时序、多表联合对账 → 例外，代码写 + 登记在案。
- 自检：列三路合成：声明列 + 关系推导列 + slot 占位合并（mergeColumns）。页面手写第四路列来源＝违规。

### 文档项目怎么分层（L0）
- 触发：建/改文档站点
- 导航范式按体量选：先数小节再选范式：3–6 节顶部 tab 切换；7 节以上侧边目录+滚动联动；20 节以上加分组折叠与搜索。拿现成的硬套=把长文切碎。
- 导航是指示器不是控制器：长文导航只指示「你在第几节」，内容始终完整可滚。禁止用切换隐藏把长文切碎——用户要的是定位，不是分页。
- 一章一文件，单文件不过 250 行：粒度标准是一次对话能读完并改写。超了就按成员再分一组，不要硬塞。
- 约定写进项目，不写进对话：分层规范必须落在可视化自己这一页。换谁来、换个什么工具，翻开就知道。
- index.html 是唯一装配清单：所有内容、渲染、样式文件都在那里按序登记。加载顺序就是依赖顺序，别在别处偷偷引。
- 自检：导航是指示器不是控制器长文导航只指示「你在第几节」，内容始终完整可滚。禁止用切换隐藏把长文切碎——用户要的是定位，不是分页。

### 版本记录与恢复（L0）
- 触发：文档改动前后
- 改前先确认工作区干净：开工前 git status 应为空。带着旧改动开新活，出错时说不清是哪次改的，回滚就无从谈起。
- 一条改动一个提交，信息写清：feat / fix / chore 分开提交，信息说明改了什么。这是你回滚时的索引，写得越清越好找。
- 恢复前先给方案，人工确认后执行：凡涉及回退、删提交、删分支，AI 必须先列方案和影响范围，你确认后才动手。不可逆操作不允许直接做。
- reflog 是最后保险：任何操作都留痕。退过头、删错，都能从操作流水翻回来。出了事先查记录，别慌。
- 推送时机由人定：提交留在本地随时可回；推上云端前先看差异，未获指示不推送。

### 对话分流（L1）
- 触发：每轮对话先到这里：判类型 → 判产物 → 查方法论 → 干活 → 报验收
- 任务对话必分流：要干活的对话走完五步才轮到动手。轻对话只定性不表演，但定性不能省。
- 操作不中断：该做的全部做完，禁止做完一段停下来问「要不要继续」。需要用户决策的先记下、继续干，最后用 IDE 选项提问面板一次性给出让用户点选，不占对话回合。能自己决定的按裁决链自行决定并在验收清单说明。
- 判定查台账：「文档有没有规定」先查 docs-coverage.md，不读就断言=分流失败。
- 先文档后代码：文档是唯一规范源。文档没规定的先补文档再排代码；代码任务按文档干。
- 文档已规定不重生成：台账显示已规定未实现就按代码任务走，不重复生成文档。重复生成=分流失败。
- 自检：操作不中断该做的全部做完，禁止做完一段停下来问「要不要继续」。需要用户决策的先记下、继续干，最后用 IDE 选项提问面板一次性给出让用户点选，不占对话回合。能自己决定的按裁决链自行决定并在验收清单说明。

### 格子点击 → 确认层（L0）
- 触发：设计格子交互
- 路径对表不拍脑袋：新格子先在路径表里找到值来源行，按行取路径。同一值来源出现第二种路径=违规。
- 确认层检索默认展开、按钮可收：进入即展开检索下拉（快速检索），收/展按钮全站同形态同位置；禁止只自动展开不给收起（遮确认层其他信息），禁止默认收起（多一步才能检索）。
- 管理能力跟字典走、参数位控制：值指向独立字典表 → 检索下拉带「完整字典」档 + 行内改/删（改走改名/并档预览，删走单条确认）；参数位空 → 不显示任何管理入口。管理能力由参数驱动，格子侧零分支。
- 禁止第二套管理面板：同一确认层里不许并存两个字典管理入口；旧面板与新能力重叠时删旧的。确认层之外的管理面板（多选面板等）要单独说明存在理由，说不出就收。
- 行内操作常驻不悬停：列表行内的改/删按钮常驻渲染，不靠 hover 出现——移动端没有 hover，hover 态格局不稳。
- 自检：路径对表不拍脑袋新格子先在路径表里找到值来源行，按行取路径。同一值来源出现第二种路径=违规。

### 怎么沉淀方法论（L0）
- 触发：什么时候沉淀什么：从重复里长出方法论，登记进真相源才算存在
- 重复才沉淀，重坑例外：一次是案例，两次是模式；但高代价教训出现一次就立即沉淀。没有信号不开新方法论。
- 登记才算存在：篇章+侧栏+执行卡由真相源一处生成；不登记进真相源，AI 用不到，等于没沉淀。
- 触发条件是门牌：每条方法论必须有明确触发条件，写用户的话长什么样。写不出触发条件=还不是方法论。
- 去项目化：方法论剥离本项目专名，换现场通用语。带专名的方法论传到别的项目会被照套。
- 可证伪：每条规则硬到能否决一种实现。删掉它现场会断，才算规则；删掉不断的是废话。
- 自检：先保住体感再定问题用户的抱怨原句（划很久/找不到/分不清）是最高优先级信号。翻译成技术任务前先复述体感，禁止把体验问题窄化成「加个组件」。

### 采购入库（L1）
- 触发：囤货补货

### 待入库管理（L1）
- 触发：配货超额调货

### 库存台账（L1）
- 触发：看库存与成本

### 欠库台账（L1）
- 触发：出库缺口兜底

### 供应商应付（L1）
- 触发：该付供应商多少钱

### 经营报表（L1）
- 触发：看经营数据

### 角色权限（L1）
- 触发：配谁能看谁能改
- 可见性原则：所有页面与功能按钮对所有账号开放显示（代码实证：StaffLayout 废除可见不可见控制）；无权限点击给统一提示。藏按钮＝用户以为功能丢了。
- 三段式执行：前端入口展示（不置灰不禁用）→ 前端点击时按权限集校验提示 → 后端拒绝写（绕过前端直调 API 也必须拒）。前端管体验、后端管底线，缺任何一段＝违规。
- ro 可预览即时算价：只读账号可改数预览即时算价（失焦/防抖不落库），主动写才提示「只能查看」。
- 权限码数以实测为准：权限码清单以导航注册表/生成物实测为准（现 18 个），禁止在文档手写死数字。
- 自检：三段式执行前端入口展示（不置灰不禁用）→ 前端点击时按权限集校验提示 → 后端拒绝写（绕过前端直调 API 也必须拒）。前端管体验、后端管底线，缺任何一段＝违规。

### 审计日志（L1）
- 触发：查谁干了什么

### 授权码（L1）
- 触发：客户要进门

### 访问申请（L1）
- 触发：客户申请进门

### 用户管理（L1）
- 触发：管员工账号

### 表格 UI 分层（L1）
- 触发：设计表格页面
- 上层只装配、下层只参数：上层不许替下层决定形态，下层不许越过上层改装配。新增一种行 = 行槽位数组里插一项，不动骨架代码；新增一种格子形态 = 在 L4 加一个分支，不动页面。
- 不得在 custom 内手写单元格交互：任何可编辑格必须能表达为三维参数组合。无法归类的例外登记在案并说明为什么不能用参数表达——custom 的语义是「无法归类」，用它就是认输。
- 门禁格视觉必须一致：前置未满足的格子保持与可编辑格完全一致的视觉（hover、分割线、行高），点击给提示「请先 X」。参数化之后门禁 = `editEntry: 'confirm' + disabledReason`，视觉由组件统一保证，页面无权决定。
- 待补不静默留白：某表某层暂无实测数据时，页面明写「待补 + 补法」，禁止空白。让人一眼看出是没填还是没有。
- L1 落地决策：改造 ViewFrame：已裁决采用「改造 `ViewFrame` 支持行槽位数组」，而非在其上加一层装配器——理由是避免多一层抽象；代价是需对 9 个工作台视图全量回归。L2 的辅助行是本层唯一需新建的组件。
- 自检：待补不静默留白某表某层暂无实测数据时，页面明写「待补 + 补法」，禁止空白。让人一眼看出是没填还是没有。

### 架构重构信号（L0）
- 触发：历史做法限制当下
- 三次绕行即立案：同一类"别扭"在对话里出现第三次，必须立案评估重构，禁止继续绕。一次是需求，两次是巧合，三次是架构错了。
- 先画目标态，再看迁移路：先按当前业务画理想架构（无视现状），再评估从现状过去的代价。顺序反了就会拿现状当约束，画出个放大版的旧架子。
- 禁止用"以前投入"否决重构：沉没成本不参与架构决策。旧工作的价值体现在"哪些部分可保留"里，不体现在"要不要改"里。
- 开发期从宽，发布期从严：没有外部用户依赖时，重构代价只有自己的回归成本；上线后每拖一天代价都在涨。"现在是开发阶段"是重构的窗口期信号，不是拖延期信号。
- 重构必须连文档一起动：架构变了，方法论、登记表、台账不同步＝没有完成。改完代码回写台账是验收条件，不是可选项。
- 自检：三次绕行即立案同一类"别扭"在对话里出现第三次，必须立案评估重构，禁止继续绕。一次是需求，两次是巧合，三次是架构错了。

### 元模型运行时（L1）
- 触发：一份 yml 驱动全栈
- 一处声明五面产出：改一个字段只动 entity-meta.yml 一处，前端列表、后端校验、文档三处自动跟；check-docs 与前后端构建零报错是验收线。
- 留痕优于审批：审计动作未登记仍记录并告警，不丢事件；登记补齐是事后动作。
- 快照层独立于检索层：单据快照、金额、统计禁止依赖任何检索派生表——派生表可以删，快照不能跟着陪葬。
- 自检：一处声明五面产出改一个字段只动 entity-meta.yml 一处，前端列表、后端校验、文档三处自动跟；check-docs 与前后端构建零报错是验收线。

### 视觉与画布（L1）
- 触发：尺寸缩放令牌
- 浮层承载细则：确认/取消钉 FloatPanel 底栏、面板内禁止横向滚动（内容超出走内部纵滚）、水平锚点在画布内平移；选完/输完自动关闭，没选中保留自由文字。
- 行盒两层嵌套不可回退：外层固定可视窗口 overflow:hidden，内层 overflow-x:auto——单层结构下任意一行被撑宽会让整列变宽、单行滚动失效，禁止合并。
- 内容区水平内边距为 0：行盒自带 padding，父子都加水平 padding 会让内容起点错位、各行宽度不一致（垂直 8px 保留）。
- UI 框架主题走令牌：表格与输入框的行高/字号/内边距通过主题令牌统一配置，不做样式覆盖。
- 自检：浮层承载细则确认/取消钉 FloatPanel 底栏、面板内禁止横向滚动（内容超出走内部纵滚）、水平锚点在画布内平移；选完/输完自动关闭，没选中保留自由文字。

### 工程纪律（L0）
- 触发：src 只许 .ts/.tsx
- 工程配置文件同名只留一种：同名 .js/.ts 只留一种、以 .ts 为准。
- 四条反模式即断链判定：复制式迁移（断唯一权威）、只改 .ts 不删 .js（断变更纪律）、裸构建不校验（断校验闸门）、变更记录先写代码后改文档（断同步）。命中任何一条＝返工。
- 后端同守唯一权威：本组纪律为跨功能通用规则，后端 TypeScript 工程同样遵守。

### 导航与位置记忆（L1）
- 触发：目录即配置
- 导航层不做整页缓存：保活靠视图级懒激活+显隐切换，禁止导航层整页缓存。
- 三级及 deeper 自动插入：三级及更深导航自动插在二级导航与内容区之间，由布局渲染——塞进内容区当工具条＝违规。
- 多文档标签行不塞返回按钮：标签行管文档切换，返回走底栏浏览栈——两套职责不混。
- 自检：导航层不做整页缓存保活靠视图级懒激活+显隐切换，禁止导航层整页缓存。

### 前后端职责（L1）
- 触发：算力往前放
- 禁止服务端实时算价：算价在前端即时完成，后端只存结果并做最终校验——每按键开事务＝违规。
- 禁止大范围重算：改一处只重算受影响面，禁止全表/全单重算。
- 单店规模假设写死：按「单店 50 人在线、笔记本跑得动」设计；为多门店高并发做的架构＝过度设计。
- 自检：禁止服务端实时算价算价在前端即时完成，后端只存结果并做最终校验——每按键开事务＝违规。

### 组件资产清单（L1 项目级 · 查同类先查这里）

| 形态 / 需求 | 共用组件 · 路径（徽标） · 关键参数 |
|---|---|
| 多记录矩阵（行 + 空行 + 操作列） | `MatrixTable` · shared/components/MatrixTable.tsx（C29）· rows(MatrixRowConfig[]) / addNameCell / addMidCells / addPriceCell / showDefault / showAddButton / template / selectedRowKey+onRowSelect |
| 更底层网格骨架（仅表头+行，无操作/默认列） | `EntityPanel` · shared/components/EntityPanel.tsx（C65）· 新矩阵优先用 MatrixTable，不要直接用它。template/header/rows/addRow/footer |
| 可编辑单元格（点值→确认层） | `ArchiveFieldCell` · shared/components/product-picker/PickerInlineCells.tsx（-）· value/title/bullets/onApply/disabledReason（门禁） |
| 空行单元格（点值→确认→晋升） | `ArchiveEmptyFieldCell` · shared/components/product-picker/PickerInlineCells.tsx（-）· placeholder/title/onApply/disabledReason |
| 字典引用单元格 | `PickerNameCell / PickerNumCell` · shared/components/product-picker/PickerInlineCells.tsx（-）· kind/scope/fromId/onApply/onApplyGlobal |
| 只读 / 门禁占位格 | `DisplayCell` · shared/components/product-picker/PickerInlineCells.tsx（-）· text/placeholder/rejectReason（门禁提示） |
| 多记录状态机（空行晋升/追加/默认互斥） | `useMatrixRecords` · shared/hooks/useMatrixRecords.ts（-）· value/isDataRow/blank/normalize/onDirty |
| 枚举记录矩阵（单位 + 换算率，列表侧） | `UnitManagePanel` · shared/components/UnitManagePanel.tsx（C19）· units/conversions/extensions{showBase,onSetBase,priceColumns}（编辑矩阵内的单位区已改用 MatrixTable，本组件用于列表侧独立单位管理） |
| 集合编辑矩阵（集合体编辑弹窗统一形态：§A 根实体信息 → 中间层切换行 → 叶子挂载子表矩阵 → 价格展开面板） | `集合编辑矩阵（实例：ProductEditDialog）` · apps/staff/pages/product-manage/ProductEditDialog.tsx（-）· 描述时加集合体前缀（产品集合编辑矩阵 / 供应商集合编辑矩阵）；组装式：CascadeSwitchRow（中间层）+ MatrixTable（叶子挂载子表）+ ArchiveFieldCell（确认层格）+ UnitPriceExpandPanel（价格展开面板） |
| 中间层级联切换行（品牌/规格：下挂子记录 → 行切换） | `CascadeSwitchRow` · shared/components/CascadeSwitchRow.tsx（-）· label/options{key,label,active,editCell}/onSelect/addCell/editRow（仅中间层用；叶子挂载子表一律 MatrixTable，禁止误用行切换） |

<!-- GEN:END -->
