# 建材进销存平台：从零启动实施路径 × 现状偏差对标报告

> 视角：SoftwareCompany 专家组（产品 / 架构 / 研发 / 测试 / 交付）
> 优先级基调：**长期生产级稳定性、可维护性优先**，不以短期开发成本最低、快速交付为优先。
> 事实基础：本报告对代码、schema、部署、文档、报告（verify / signal / arch-review / 开发规划 / 掌控台 / 架构蓝图）做了逐物考证，非印象。

---

## 0. 执行摘要（先看这页）

**一句话差异**：项目把大量精力用于"用 AI 高速盖楼 + 建一套约束 AI 不漂移的元框架"，但**地基（稳定的领域边界与数据模型）和机房（生产级基建）是后补且至今未完成的**——而长期的真正命题是"千万级 SKU 生产运行"，需要的是后两者。

**最关键的三个判断**：
1. **"推进缓慢"的根因不是 AI 写得慢，是"人"成了唯一闸门**（待拍板 / 22 个未提交文件 / 领先远端 11 个 / 蓝图自诊"反复返工循环"）。AI 把"盖楼"提速了，但每次改动都要人审、人落账、人裁决，吞吐量被单人带宽卡死。
2. **数据模型 30+ 次大版本重构（v2→v31）、SQLite↔MySQL 反复**，是"编码前未冻结地基"的典型症状；这部分工作本应在第 0 周一次性做对。
3. **生产级基建几乎空白**（无 Docker Compose / k8s / Redis / 搜索引擎 / CI / staging / 备份 DR / 可观测），部署手册已被弃用。当前是"单店笔记本 + cpolar"。这与"千万级 SKU 生产"目标之间存在**结构性缺口**，而非细节缺口。

**好消息（应保留的资产）**：应用层已具备相当生产化的设计直觉——v11.0 全域解耦（业务表只存 ID+名称快照、无物理外键）、单据快照范式、库存台账加权均价、审计/变更日志、按千万行预规划的复合索引。这些"正确性"层面的决策是扎实的，从零路径也会保留。**需要补的是"运行时规模化"与"决策/交付节律"**。

---

## 1. 业务场景与长期目标（确认理解）

- **业务**：建材门店的**进销存 + 报价协同**平台。覆盖基础档案（产品/供应商/客户/库房）、单据（采购报价→配货→交付→成本→退款→归档）、进货管货、欠库台账、收付款往来、经营分析（7 类报表）、系统管理（RBAC/审计/授权码）、客户端（产品中心/采购清单/地址）。
- **长期目标（用户原话）**：**支撑千万级 SKU 生产运行**的建材进销存平台。
- **对"千万级 SKU"的工程含义（必须被显式定义，现状缺失）**：
  - 数据量：SKU 主数据千万级；单据/库存流水/审计日志**十亿级**行。
  - 并发：多门店/多业务员并发录入与查询；大促/盘点期写峰值。
  - 一致性：库存扣减、成本核算、应付账款必须**强一致且不超卖**。
  - 可用性与耐久：数据不能丢（RPO/RTO 有界）、服务可水平扩展、可观测、可回滚。
  - 多主体：既是"门店系统"又是"平台"——**多店 / 多租户隔离**是隐含前提，现状未实现。

---

## 2. 项目当前实现现状（代码取证）

### 2.1 技术栈与运行形态
| 项 | 现状 | 来源 |
|---|---|---|
| 前端 | React 19 + antd 6 + zustand + Vite 8 + Vitest 5 + oxlint | frontend/package.json |
| 后端 | Node(tsx) + Express 4 + Prisma 5.22 + JWT + ws + sharp + zod | backend/package.json |
| 数据库 | **MySQL 8**（`.env` 指向 `mysql://…/bm_quotation`），但 `prisma/dev.db`（SQLite）仍残留 | .env / schema.prisma / 目录残留 |
| 运行 | 单 Node 进程；`dev.sh` 一键拉起 3000+8080+8081+cpolar 公网穿透 | 部署运维手册（已废弃） |
| 迁移 | `prisma/migrations/` **仅 1 条**（v22）；日常靠 `prisma db push` / 重置 | 目录实证 |
| 门禁 | `npm run verify` 9 道，**当前 8 通过 / 1 失败（S1 fe-typecheck 红）** | verify-report.json |
| 信号 | 14 处 MED「列逃进 custom」迁移债（框架未完全落地） | signal-report.json |
| 完成度 | 掌控台自报 35/37（95%），剩余为 v23 两项 ❌（产品名全局字典 / 分类改关系表，已裁决撤销） | 项目掌控台.md |

### 2.2 数据模型成熟度（**亮点，应保留**）
- **v11.0 全域解耦**：业务交易表对基础档案只保留 `BigInt` ID + 名称快照、**无物理外键**；档案物理删除不影响历史。这是十亿级流水表的正确选择。
- **单据双区 + 快照范式**：`document_lines` 写时落 `productName/brandName/unitName` 快照，历史打印/对账脱离档案当前态。正确。
- **库存台账 + 流水加权均价**（`inventory` / `inventory_ledger`），分层成本（internal/external_agreed/external_excess）。正确。
- **索引按规模预规划**：`documents` 复合索引 `[status, sales_archived_at, created_at]` 覆盖"过滤+排序"避免 filesort；审计/应付按 `created_at` 分页。意图正确。
- **敢于回退反模式**：曾建 `product_sku_search` 宽表，实证会陈旧/留孤儿/规模代价高后**物理删除**，改回范式多路召回。工程判断力成熟。

### 2.3 方法论成熟度（**亮点，但方向偏**）
- 架构蓝图 v3（双向架构：自上而下推导链 + L1–L5 层栈）、三次原则、显式逃逸登记、verify 门禁、登记表驱动代码生成（entity-meta.yml→generated）、Meta Studio 向导。**代码组织纪律是行业水准的**。
- 代价：这套方法论的**全部重心是"约束 AI 不漂移"**（蓝图 §0.0 明言痛点是"AI 不会写代码吗？不，是反复返工"），而非"面向生产规模与可运维性"。

### 2.4 现状缺口（信号汇总）
- 质量门 **S1 红**、**14 MED 逃逸**、**v23 两项 ❌ 未决**；
- 基建 0：无 Compose/k8s/Redis/搜索/CI/staging/备份 DR/可观测；
- 数据演进不可控：仅 1 条迁移、SQLite 残留、长期 `db push`；
- 单库单店，无多租户；无独立搜索引擎；无异步队列；ID 用应用层 `P+时间戳+随机` 字符串（绕 autoincrement 的妥协）。

---

## 3. 从零启动的完整实施路径（SoftwareCompany 方法论）

> 原则：**先冻结地基与机房，再垂直切片交付，最后按需规模化**。不做"先建横向元框架"。

### 3.1 总体哲学与取舍
- 稳定性/可维护性 > 短期速度。允许前 2–4 周"慢"（设计 + 基建），换后期不返工。
- **垂直切片 > 横向平台**：先用真实业务流把端到端跑通并测试，第 3 个同类才抽象（沿用三次原则，但作用于*产品代码*，不是去造代码生成平台）。
- **决策与验收下放**：用 ADR + 角色授权 + CI，避免"人卡一切"。

### 3.2 阶段 0｜需求与领域稳定化（产品 PM，第 0–1 周）
- **产出物**：业务能力地图（进销存限界上下文：商品 / 采购 / 库存 / 销售 / 财务 / 客户 / 供应商 / 报表）、事件风暴产物、*非功能需求表（NFR）*。
- **NFR 必须显式量化**（现状缺失）：SKU 规模、峰值 TPS、查询 P99 延迟、可用性目标、RPO/RTO、多店/租户要求、合规。
- **需求处理**：Spec-Driven——每条需求带「目的 / 成功标准（机器可验优先）/ 边界 / NFR 影响」。规格由**人类 PM 拥有**，不为满足 AI 而由 AI 生成。

### 3.3 阶段 1｜架构与数据模型先设计（架构师，第 1–3 周，*关键*）
- **先逻辑模型（ERM），后物理模型**；绝不在写功能时顺手定表。
- **设计期就定下规模化骨架**（现状最缺）：
  - **ID 策略**：snowflake / ULID / UUIDv7（时间有序、可排序、跨分片友好）。**否决**应用层 `P+时间戳+随机` 字符串。
  - **分区/分片键**：库存、流水、审计按 `warehouse_id` / 时间范围分区；为将来分片留键。
  - **多租户/多店**：`tenant_id` / `store_id` 行级隔离从 Day 1 进入模型（哪怕先单租户）。
  - **读写分离 + 读模型**：报表/商品检索走独立读模型（CQRS-lite）；**商品检索用独立搜索引擎**（ES/OpenSearch/Meilisearch），不用 DB FULLTEXT ngram 扛千万级。
  - **异步边界**：库存流水、成本核算、欠库冲抵、报表聚合走消息队列（BullMQ/Redis Streams/NATS），不阻塞写请求。
- **迁移纪律**：版本化迁移从第一次提交即有；`db push` 仅限本地一次性，**staging/prod 只跑 `migrate deploy`**；回放脚本在 prod 副本验证。
- **保留本项目已验证正确的部分**：快照范式、全域解耦无物理外键、库存加权均价、审计/变更日志——直接继承，不重做。
- **决策记录**：每个重大选择写 **ADR（上下文/选项/后果）**，模型冻结后非经 ADR 不复动。

### 3.4 阶段 2｜生产级基建并行铺设（架构/DevOps，第 2–4 周，与功能并行）
- `docker-compose`：app + MySQL + Redis + 搜索引擎 + nginx；后续 k8s。
- **CI**：PR 触发 lint / typecheck / 单测 / build / 集成测试；门禁非零即拒（取代"AI 自评"）。
- **环境**：dev / staging（验收真相源）/ prod，配置与密钥分离管理。
- **可观测**：结构化日志 + 指标 + 链路追踪；告警。
- **备份/DR**：定时备份 + 演练恢复，RPO/RTO 写进 SLA。

### 3.5 阶段 3｜垂直切片交付（研发，第 3 周起，持续）
- 每个限界上下文先交**一条端到端薄切片**（如：商品档案 → 采购入库 → 库存台账），带单测 + 集成测试 + 关键路径 e2e。
- 不先建"登记表驱动代码生成元框架"。通用能力在第 3 个真实案例出现时再抽（三次原则作用于产品代码）。
- 交付定义（DoD）含测试与 CI 全绿；**验收由独立 QA 判定，不由开发/AI 自认**。

### 3.6 阶段 4｜规模化硬化（按需，第 2 阶段后）
- 读写副本、连接池代理（ProxySQL/PgBouncer）、缓存层、搜索服务、队列消费端、分片/分区上线、多租户隔离落地。
- **不在早期过度投入**（避免 YAGNI），但**架构缝（ID/分区键/租户/读写分离/异步边界）在第 1 周就留好**。

### 3.7 文档规划（精简、规定性）
- **保留**：领域模型、ADR 决策库、API 契约（OpenAPI）、Runbook、NFR 表。
- **自动化**：状态看板（类掌控台）可自动生成，但**不替代方向指引**。
- **禁止**：93KB 级"活台账"式描述性巨文档——它记录"做了什么"，却不回答"为什么/下一步往哪"。

### 3.8 需求处理方式
- Spec-Driven + 人类 PM 所有；规格绑定业务价值与 NFR；验收标准机器可验优先；变更走 ADR，不临时拍脑袋。

### 3.9 协作与交付模式（团队 / QA）
- **真实角色**：PM（规格+验收所有）、架构师（模型+ADR+门禁）、研发（按规格实现）、QA（**独立**拥有 verify 门禁，非 dev/AI）。
- **节奏**：短生命周期分支 + 人类 PR 互审 + CI 强制门禁 + staging 验收。
- **去中心决策**：常规决策下放 ADR/角色授权，**不再由单人充当唯一闸门**——这是解决"推进缓慢"的结构性动作。

---

## 4. 对标：从零路径 vs 实际走过的路

| 维度 | 从零启动（理想路径） | 项目实际路径 | 偏差 |
|---|---|---|---|
| **起点** | 第 0 周冻结领域边界 + NFR | 直接上功能 + AI 代码生成 | 地基未定先盖楼 |
| **数据模型时机** | 设计期一次性稳定（含分区/ID/租户） | 30+ 次大版本（v2→v31）、SQLite↔MySQL 反复 | 持续重构、移动靶 |
| **框架/元层** | 先垂直切片，第 3 次才抽象 | 先建登记表驱动代码生成元框架（Meta Studio/生成器/门禁） | 把"造楼"变成"先造脚手架"，元框架成并行负担 |
| **生产基建** | 与功能并行铺（Compose/CI/Staging/备份/可观测） | 几乎空白；单笔记本+cpolar；部署手册已弃 | 机房缺失 |
| **规模化** | 设计期定缝（读写分离/搜索/异步/分片）按需硬化 | 仅应用层解耦/快照/索引；无搜索/队列/缓存/多租户 | 正确性有、运行时扩展无 |
| **测试/门禁** | 独立 QA + CI 从第一天 | 测试基建最后才建，至今 S1 红、14 MED 逃逸 | 长期无安全网 |
| **文档** | 精简规定性（ADR/契约/Runbook） | 描述性巨量（93KB docs-coverage + 53KB AGENTS + 掌控台） | 记录状态 ≠ 指引方向 |
| **协作/交付** | 真团队角色 + CI + 独立验收 + 决策下放 | 人（业务架构师）为唯一闸门（待拍板/22未提交/11领先） | 单人带宽成瓶颈 |
| **决策节律** | ADR 显式、稳定 | 每功能附带 vX.0 重构 | 反复返工循环 |

---

## 5. 偏差点识别（具体到物）

1. **数据演进不可控**：`prisma/migrations/` 仅 1 条（v22），长期 `db push`/重置；`prisma/dev.db`（SQLite）与 MySQL 声明并存 → 数据层曾反复切换的残留。对千万级是**硬伤**（无版本化迁移=无法安全演进）。
2. **主键策略反范式**：`product.id` 用应用层 `P+时间戳+随机`（因 FK 解耦晚于建表，无法去 autoincrement）。不可排序、跨分片难、索引效率低。
3. **搜索无独立引擎**：SKU 检索从宽表改回范式多路召回（FULLTEXT ngram）。千万级全文检索靠 DB ngram 不经济。
4. **无异步/队列**：库存流水、成本、欠库冲抵疑似同步写入 → 高并发锁竞争、响应劣化。
5. **单库单店、无多租户**：与"平台"定位不符，后期改造代价极高。
6. **元框架半采纳**：signal-report 14 处 custom 列逃逸、cellSpec 死代码 → 框架未完全落地，维护"生成 + 手写"双轨。
7. **质量门未全绿**：S1 fe-typecheck 失败；v23 两项 ❌ 未决（已裁决撤销但未收口）。
8. **运维故事被弃**：`废弃的可参考文档/技术架构/部署运维手册.md` → 生产运维未被持续维护。

---

## 6. 开发推进缓慢的根本原因（分层）

- **A. 最深层（地基未冻结）**：领域边界与数据模型未在编码前稳定，每个功能触发局部重构，累积 30+ 大版本与"反复返工循环"（蓝图自身已诊断）。AI 让盖楼变快，但地基没定，于是快在地基上拆改。
- **B. 方法论层（主线偏移）**：把"防止 AI 漂移"当项目主线，建了以约束 AI 为核心的元框架与巨量文档；优先级压过了"生产级稳定性/规模化"该投的基建，且元框架本身成并行维护负担与单点风险。
- **C. 协作层（最直接瓶颈）**：**人（业务架构师）是唯一决策与验收闸门** → 吞吐量受单人审阅带宽限制（22 未提交 / 11 领先 / 待拍板）。不是 AI 慢，是没人拍板、落账、裁决。"推进缓慢"在此被结构性放大。
- **D. 质量层（安全网缺失）**：测试/CI 最后才补且未全绿 → 长期无回归保护，每次改动有风险，进一步加剧谨慎与对人审的依赖。
- **E. 目标对齐层（方向缺失）**：大量文档记录"做了什么"，但缺把日常任务对齐到"千万级生产"的路线图/NFR → 做了很多却难判偏差，问题晚期才暴露。

---

## 7. 后续改进建议（分优先级，衔接千万级目标）

### P0｜立即（低成本，解锁交付）
- 修 **S1 fe-typecheck 红门** + 14 处 MED「custom 列」逃逸（落地既有框架，或显式 `@escape` 登记）；收口 v23 两项 ❌（正式 ADR 否决或实现）。
- 把 `npm run verify` 接入**真实 CI**，每次 PR 必跑；不再以"AI 自评 / 掌控台"作为验收权威。

### P1｜1–2 周（地基）
- **冻结数据模型**：写 ADR 锁定当前 v31；停止 vX.0 重构；建立版本化迁移（`prisma migrate`），在 **prod 副本**上验证回放。
- **决策并文档化规模化关键项（即使暂不实现，现在极便宜、后期极贵）**：多店/租户模型、ID 策略改 snowflake/ULID、读写分离与分片键、SKU 搜索选型（独立搜索引擎）、异步化边界（库存/成本/报表）。

### P2｜月级（生产级）
- 铺基建：docker-compose（app+MySQL+Redis+搜索+nginx）、CI/CD、staging、**备份/DR（RPO/RTO）**、可观测（log/metric/trace）、密钥管理。
- 引入**独立 QA 角色/流程**，verify 归 QA 而非 dev/AI；用 ADR 下放常规决策，减少人对每决策的卡点。

### P3｜规模化（按需）
- 读写分离、搜索服务、消息队列异步化库存/成本/报表、分区/分片、缓存层、多租户隔离落地。

### Meta｜方法论纠偏
- **文档减肥**：保留 ADR + 领域模型 + API 契约 + Runbook；93KB 活台账降级为自动生成状态看板，不替代方向。
- **元框架二选一**：要么全面落地（清掉 14 逃逸、成唯一路径），要么简化回结构化手写代码。**半采纳最危险**——既没享到框架红利，又背了双轨维护。

---

## 8. 附录：关键证据清单

| 证据 | 位置 |
|---|---|
| 数据模型（v11.0 解耦 / v31 现状） | backend/prisma/schema.prisma |
| 技术栈 | backend/package.json、frontend/package.json |
| DB 真相（MySQL）+ SQLite 残留 | backend/.env、backend/prisma/dev.db |
| 迁移仅 1 条 | backend/prisma/migrations/ |
| 质量门 8/9（S1 红） | verify-report.json |
| 14 MED 逃逸 | signal-report.json |
| 方法论 / 层模型 / 门禁 / 推导链 | 文档可视化/项目文档/架构蓝图.md、AGENTS.md |
| 完成度 / 待拍板 / 未提交 | 项目掌控台.md、开发规划.md |
| 部署形态（单店+cpolar，手册已弃） | 废弃的可参考文档/技术架构/部署运维手册.md、dev.sh |

---

*本报告为专家组从零路径设计与现状对标，供决策参考。所有"实际路径"结论均来自对上述文件的逐物考证。*
