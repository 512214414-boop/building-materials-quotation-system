/**
 * DOC_VIZ.archive
 * 归属：文档可视化 / 内容层
 * 切片自：js/data.js 原 2442-2475 行（已按分层规范拆出，原单文件不再维护）
 *
 * 约定：本文件只承载这一段内容。改这一段，只读/只改本文件，不必读全量。
 */
DOC_VIZ.archive = {
  kicker: "档案框架怎么用 · 和选品同一套格子",
  title: "档案已经落在最后一级。格子用值，点文字即改。",
  lead: "选品要层级展开和插入。档案不要照抄下拉形态，但输入方式必须同一套：点值 → 确认浮层 → 看全文和影响范围。别处用这套档案框架，自动就是点值而不是常驻框。",
  rules: [
    ["列表行是槽", "列序跟数据模型走：分类 → 产品名 → 品牌 → 系列/规格 → 单位 → 售价 → 进价 → 备注。图 / 状态 / 时间是壳，不占这棵树。拆列不拼全名。"],
    ["点值打开确认层", "分类 / 品牌 / 系列/规格直接点文字。字典格打开即检索。输入框后面 ▾ 打开字典管理（增删改）。默认改当前；单位 / 品牌 / 分类 / 售价类型 / 供应商可改全局并档。售价 / 进价维护浮层：类型或渠道 | 面价 | 点位 | 实际价。点位点确认层：确认修改=只改当前这一条规格；改全局=当前品牌+当前分类这一批（售价再加类型，进价再加渠道），已经单独改过的规格不跟着变。列表不另开点位列。"],
    ["备注格内直编", "产品宽表备注：点格就是输入，失焦即存。存 spec.remark。这是产品列表私有，不要和开单行备注混。开单备注走确认层，见「订单中心」。"],
    ["宽表锁列显示", "表头筛走档案框架（关键词 + 条件词 + 表头 HeaderCascadeFilter）。产品私有：标准条件锁的是宽表数据列，同一组只在第一条显示名称；非标条件这一列各种匹配都显示。级联粒度是产品→品牌→规格。"],
    ["产品名进弹窗", "点名称打开产品集合编辑矩阵（ProductEditDialog）。弹窗顺序：分类 / 产品名 / 俗称备注 → 品牌切换行（下面不再挂备注）→ 系列/规格切换行 + 规格备注（执行标准/企标/国标，跟这一条规格走）→ 单位多记录矩阵（全摊开 + 空行追加）+ 价/图。全部 ArchiveDialogField 点值确认（layout=stack）。"],
    ["维护浮层也是值", "单位 / 售价 / 进价点开维护浮层。浮层里名称、换算、面价、点位不再挂常驻输入框，点值才出确认层。售价与进价同一套列：类型或渠道 | 面价 | 点位 | 实际价。点行是切换当前项，勾选才是默认。无 Tab 的单位浮层贴边少留白，常用单位横滑不把面板撑宽。缩放时浮层跟画布等比，不要箭头那截白边。"],
    ["空行点空位新增", "末尾空行左边勾选仍占位。点空名称打开新增确认层，不要每行先挂一个输入框。"],
    ["勾选跨页保留", "TableSelectionStore：keys 全局保留，rowCache 存行快照。翻页 / 搜索只 syncPageRows，不 prune。表头 ⋯ 批量含其他页。clearSelection 或离开页面才清。"],
    ["列表页壳", "ArchiveListPage = ViewFrame + UnifiedTable + selection 契约。defaultStatusHint 与 selectionSummary 自动合并进 statusHint。标量列可跟当前页最长内容撑开。经营范围这种 N 项集合列宽封顶、按钮组合换行，禁止按顿号长串把表撑宽。"],
    ["表头批量入口", "工具栏只放直接动作（新建、批量改价）。停用/启用/删除走表头 ⋯。headerMoreMenuRenderer(selectedRows)。"],
    ["开单表同一套点值", "单据行不再除外。格子是值，点开确认层；确认才 commitCell，取消不写。选用检索挂确认层输入。细则在侧栏「订单中心 · 全局规则」。"],
    ["统一实体关系模型槽位", "带数据表的功能=一份关系图参数，槽位自动产出五面：表格 UI（列序/列宽/对齐/单元格类型/布局从实体关系推导）/检索（pickerTree 派生）/字典快建管理（DictSlot）/确认策略（三档）/场景截断。新增只填这一份，禁止各视图手写第二套列、第二套快建、第二套确认。"],
    ["一层一个真相源", "面板列结构只从实体关系登记表推导（data-source/entity-meta.yml → entityRelations.generated.ts + deriveTableColumns.ts），禁止按字段名手写第二套列。改列序改一处，三视图自动跟。已迁移：采购报价/产品档案(含 skuPrice slot)/库存主表。"],
    ["字典快建=直接建即选", "A 类字典录入/挂载场景 allowCreate=true（边用边建）。按名称确保幂等 → 直接建即选 + reused 可感知提示，无二次确认。列表列筛 allowCreate=false。"],
    ["确认策略三档", "单字段幂等快建=直接建即选；多字段+缺省值+宽表去重+相似匹配=二次确认弹窗（产品特例）；改全局/删除=二次确认+列出影响范围（并档/引用计数）。槽位内置，不各处自写。"],
    ["单位双表由槽位选", "单位是 A+B 混合：非标行（无 specId）走 A 类全局 unit 字典（检索+快建+管理面板）；标准行（有 specId）走 B 类 spec_unit（专用 Picker，管理在产品编辑内）。槽位按上下文自动选，不在 UnitPicker 揉两套。"],
    ["源格高亮", "确认层从某格展开时，源格加 active 状态（边框/底色），视觉上一眼看出确认层从它展开。close 即清。"],
    ["展开钮高亮", "选用检索展开/收起钮、字典管理钮在「展开」态时图标高亮（主色/底色），新用户一眼看出按钮与展开面板的关系，不靠位置记忆。"]
  ],
  snap: "已开单据行保持当时抄下来的内容。档案改了不自动跟；再插入才整份重抄。",
  alignTitle: "两套框架 · 一套核心",
  alignLead: "选品框架管展开和插入。档案框架管最后一级的值和维护浮层。格子、确认层、改全局、字典检索是同一套。不要合成一套 UI。",
  align: [
    ["选品检索主行", "默认名称视图：产品名称字典 + 分类值 + 品牌 N。面板顶栏可切入口层"],
    ["选品展开行", "规格 + 单位 + 换算率 + 售价 + 进价；写入只走插入"],
    ["档案列表行", "同一棵树摊平：已经是规格×品牌。分类/品牌/规格点值；备注格内直编（spec.remark）"],
    ["档案维护浮层", "单位 / 售价 / 进价子集。格子仍是值，点开同一套确认层"]
  ]
};
