# P0 重构安全网 · 执行记录

> 执行时间：2026-09-05 · 主理人：齐活林（SoftwareCompany 交付总监）
> 对应蓝图：`重构蓝图-主交付.md` 的「P0 最小安全网 8 项」与 `重构期质量安全网方案-Edward.md` 第 5 节。
> 本文是**执行结果记录**，不是设计。设计见上面两份蓝图。

---

## 一、本次做了什么（P0 止血，不碰数据库、不碰需你确认的环境）

按「先立安全网、再动业务代码」的原则，本次完成的是**不依赖生产环境、不碰数据库结构**即可落地的门禁与止血项：

| # | 安全网项 | 状态 | 具体落地 |
|---|---|---|---|
| 1 | **S1 fe-typecheck 修绿** | ✅ 已修 | 真有 3 处类型错误（`ProductManage.tsx` 的 `ArchiveSlot` 泛型退化），已定位并修复（`readOnlySlots<T>` 调用点显式传 `T`）。重跑全量类型检查 0 错误。 |
| 2 | **SQLite 残留门禁（S7）** | ✅ 已立 | 新增 `tools/check-sqlite-residual.mjs`：仓库不得含 `dev.db` / `*.sqlite` / `provider="sqlite"`。已删除孤儿 `backend/prisma/dev.db`（0 字节空文件，生产库为 MySQL，零风险），备份在 `/tmp/dev.db.bak.*`。 |
| 3 | **信号门禁（S9）** | ✅ 已立 | 新增 `tools/check-signal-gate.mjs`：把信号雷达变成**会拦合入**的门禁。收窄到「业务页手写 custom 列」17 处（框架内/生成物 52 处属合法扩展点，放行）。 |
| 4 | **契约门禁骨架（S11）** | ⚠️ 骨架就位 | 新增 `tools/check-contract.mjs`：后端启用 Swagger 后自动生效，当前后端**无 Swagger** 故显式跳过（非静默）。这是真实的后端改造项，需后续排期。 |
| 5 | **质量白名单（S9 豁免机制）** | ✅ 已立 | 新增 `质量白名单.json`：把 17 处既有 custom 债务登记为「已知债务 + owner + 回收日期 2026-10-05」，S9 命中后放行但超时自动转红，不卡死推进。 |
| 6 | **verify 集成三道新门禁** | ✅ 已接 | `tools/verify.mjs` stage 列表新增 S7（sqlite）/ S9（signal）/ S11（contract），与其他门禁并行跑、统一判定。 |

> 与历史「假绿」事故的区别：S1 已确认是**真错误**而非增量缓存假绿（tsconfig 早已 `incremental:false`）；所有新门禁退出码非 0 即阻断，无 `|| true` 吞错。

---

## 二、本次**没有**做、且为什么（需你拍板 / 需环境）

以下属 P0 安全网 8 项里**依赖数据库、生产环境或你的确认**的部分，本次未擅自执行：

| # | 项 | 未做原因 | 后续归属 |
|---|---|---|---|
| A | 迁移回放骨架（S8 数据级） | 需要真实 MySQL 实例 + 版本化迁移，涉及数据库结构演进，**按规矩需你本人在电脑上确认** | P2 |
| B | 黄金路径 API e2e（S7 业务流） | 需要测试库 + 测试框架接线，属后续阶段 | P1/P3 |
| C | 性能基线（S9 千万级 SKU） | 需要 staging 有生产量级数据，**当前无 staging** | P6 |
| D | 生产基建（Docker/staging/CI/备份DR） | 需要环境搭建 + 部署改造，可能触及线上 | 蓝图 P0 基建段，需你确认启动 |
| E | 后端启用 Swagger（S11 生效前提） | 纯后端改造、不动库，但需排期写装饰器 | 下一轮小步 |

---

## 三、当前门禁全景（verify:static --full 实测，2026-09-05 末次）

> 末次 `npm run verify:static -- --full` 实测 **通过 12 / 12，总判定 PASS，可以交付**。下方为各门禁实跑结果（非缓存、非静默跳过）。

| 门禁 | 名称 | 结果 | 备注 |
|---|---|---|---|
| S0 | meta-consistency | ✓ PASS | yml ↔ generated 对拍 |
| S0b | arch-lint | ✓ PASS | 架构合规 |
| S1 | fe-typecheck | ✓ PASS | **本次修绿**（3 处类型错误已修，全量 0 错误） |
| S2 | fe-lint | ✓ PASS | 前端 lint |
| S3 | fe-dupe | ✓ PASS | 前端重复检查 |
| S3b | fe-test | ✓ PASS | 前端单测 |
| S3c | cell-layer | ✓ PASS | 单元格层守卫 |
| S4 | be-test | ✓ PASS | 后端单测 |
| S5 | be-lint | ✓ PASS | 后端类型检查 |
| S7 | sqlite-residual | ✓ PASS | **新增**：SQLite 残留门禁（dev.db 已删） |
| S9 | signal-gate | ✓ PASS | **新增**：信号门禁（17 处白名单豁免，非清零） |
| S11 | contract-gate | ✓ PASS | **新增**：契约门禁（后端无 Swagger 故放行不静默） |

> 注：S6 浏览器冒烟未含在 `verify:static` 内（需 8080/3000 在线）；完整 `npm run verify` 含 S6。

---

## 四、17 处业务页 custom 债务清单（S9 已追踪，2026-10-05 前需清零）

| 文件 | 处数 | 类型（初步归类） |
|---|---|---|
| `apps/staff/pages/InventoryManage.tsx` | 3 | 仓库名反查 ×2、状态徽标 ×1 |
| `apps/staff/pages/BackorderManage.tsx` | 2 | 操作按钮（取消欠库）×2 |
| `apps/staff/pages/DocumentList.tsx` | 2 | 状态/类型徽标 |
| `apps/staff/pages/InboundManage.tsx` | 2 | 状态徽标、操作 |
| `apps/staff/pages/OpsReports.tsx` | 2 | 标签/徽标 |
| `apps/staff/pages/SupplierPayableManage.tsx` | 2 | 结算按钮、状态 |
| `apps/staff/pages/product-manage/ProductSkuSubTable.tsx` | 2 | 仓库名反查、主图 |
| `apps/staff/pages/AccessRequests.tsx` | 1 | 状态徽标 |
| `apps/staff/pages/workbench/views/PurchaseQuote.tsx` | 1 | 状态/标签 |

**清理路线**（不动业务功能的前提下）：仓库名反查类 → 归入 L4「关系展示」单一原语（3 个页面重复出现正说明它该是框架扩展点）；状态/标签类 → 归入 L4「枚举/标签」原语；操作按钮类 → 归入 L4「行操作」槽。每页改完需浏览器验证，故安排在 P1/P3 随分层重构一并外科手术式清理。

---

## 五、需要你拍板 / 确认的下一项

1. **是否启动生产基建（D）**：Docker Compose + staging + 每日备份 + CI 卡合入。这是「正常项目都该有」的地基，但涉及部署改造，需你点头并可能在电脑上操作。
2. **是否排期后端 Swagger（E）**：让 S11 契约门禁真正生效，是后续防「静默改坏接口」的关键。纯后端、不动库，风险低。
3. **17 处 custom 的清零节奏**：白名单 2026-10-05 到期。是随 P1 分层重构顺手清，还是单独排一轮？
4. **P2 数据库迁移**（A）：从「db push 演进」改为版本化迁移 + 加 `tenant_id` 等规模化预留，按规矩需你本人在电脑上确认后才执行。

---

## 六、结论

P0「止血型」安全网已就位并接入 verify：S1 真红已修绿、SQLite 残留已清、信号门禁已能拦真债务、契约门禁骨架已埋好。**verify 全绿不等于「重构完成」**——它只是证明「改坏会立刻被发现」。真正的分层重构、目录重整、物理模型演进、生产基建铺设在 P1–P6，需按蓝图逐阶段推进，且其中多项依赖你的环境确认。

> 一句话：地基的「报警器」装好了，房子还没开始按新图纸盖。
