# 硬闸执行手册（DB 结构变更 / 基建 — 需用户在电脑确认）

> 本手册把重构主线中**所有必须由人在电脑上确认执行**的「硬闸」收口成一个可一键复制执行的序列。
> AI 已完成其全部「纯应用层 / 不碰 WIP / 不踩硬闸」的部分并实测门禁全绿；本手册覆盖剩余物理闸口。
>
> ⚠️ **纪律红线**：以下每一步都是真实 DB 结构变更 / 基建动作，AI 不越权执行。请在本机（非手机）逐项确认后运行。
> ⚠️ **执行前必须先做全量备份**（见第 1 节）。每一步后跑第 6 节验证，确认门禁仍全绿再进下一步。

## 0. 当前已就绪状态（AI 侧，无需你执行）
- 后端四道门禁实测全绿：`tsc` 0 错 / `npm test` 114/114 / `node tools/check-arch.mjs` A4+A5 通过 / `prisma validate` 有效。
- P3 写路径 100% 经仓储（`prisma.<model>` 委托 662→0，A5 计数 772→102 只减不增）。
- P4 应用层接缝已落地：事件总线端口 + in-process 实现（7 测）、SearchService 端口（3 测），均可 feature-flag 回退。
- SQL 硬闸文件已全部备齐：`docs/architecture/sql/p2d_drift_cleanup.sql`、`p2b_snowflake_pk.sql`、`p4_outbox.sql`。

## 1. 执行前全量备份（必做，回滚依据）
```bash
cd /Users/mac/Desktop/建材报价系统
# 容器名 bm_mysql / 库名 bm_quotation（见 docker-compose.yml，可用 .env 覆盖）
mkdir -p /tmp/bm_backup
docker compose exec bm_mysql \
  mysqldump -u root -p"$MYSQL_ROOT_PASSWORD" bm_quotation \
  --single-transaction --routines --triggers --events \
  > /tmp/bm_backup/bm_quotation_$(date +%Y%m%d_%H%M%S).sql
ls -lh /tmp/bm_backup/
```
> 记下文件名，第 7 节回滚要用。建议同时 `git stash`/确认无未提交应用层改动混入场。

## 2. Phase A · P2-d 历史 drift 清理
```bash
cd /Users/mac/Desktop/建材报价系统/backend
# 19 删 FK / 5 重命名索引 / 2 补索引 / 2 补 FK / inventory_ledger 补 3 列 等
npx prisma db execute --schema prisma/schema.prisma \
  --file ../docs/architecture/sql/p2d_drift_cleanup.sql
```
**关键决策**：6 个 `ft_*` FULLTEXT 全文索引在 P4 搜索服务就位前**保留**（见 `P2d-迁移方案.md`），本 SQL 已避开删除它们。
**验证**：第 6 节全绿后继续。

## 3. Phase B · P2-b 62 表 PK 切 Snowflake
```bash
cd /Users/mac/Desktop/建材报价系统/backend
# 632 行：DROP FK → MODIFY PK 为 BIGINT UNSIGNED → ADD FK，避开 P3018（无 DROP INDEX）
npx prisma db execute --schema prisma/schema.prisma \
  --file ../docs/architecture/sql/p2b_snowflake_pk.sql
```
**过渡策略（应用层已就绪）**：app 先显式传 Snowflake id 旁路自增（`PrismaRepositoryBase.save()` 已预留回写），待双写稳定后再 `MODIFY` 去自增。详见 `P2b-迁移方案.md`。
**验证**：第 6 节全绿（重点核对 `prisma validate` 仍有效、无类型退化）。

## 4. Phase C · P4 outbox 表（可靠事件投递）
```bash
cd /Users/mac/Desktop/建材报价系统/backend
npx prisma db execute --schema prisma/schema.prisma \
  --file ../docs/architecture/sql/p4_outbox.sql
```
执行后，infrastructure 层可补「DB 实现 `OutboxStore` + relay（outbox→event_bus）」——该补丁为纯应用层，可走 P4 同款 feature-flag 接缝，不在此硬闸内。

## 5. Phase D · P6 分区 + 千万级压测（需 staging，本机不执行）
- `inventory_movement` 按时间分区需 **PK 含分区键**（Snowflake id 时间有序，可借 id 范围分区；或改造 PK 为 `(id, created_at)`，属 PK 重设计，须先与 P2-b 对齐）。
- 千万级 SLA 压测须独立 **staging 实例**，不在开发库跑。
- 本手册不提供一键命令；待 P2-b 落定 + staging 就绪后单独立项。

## 6. 每阶段后验证（门禁护栏）
```bash
cd /Users/mac/Desktop/建材报价系统/backend
/usr/local/bin/node ./node_modules/typescript/bin/tsc --noEmit --incremental false   # 期望 exit 0
npm test                                                                             # 期望 114/114
cd /Users/mac/Desktop/建材报价系统
node tools/check-arch.mjs                                                            # 期望 exit 0（A4+A5）
# 行数核对：A5 prisma. 总行数应 ≤ 102（只减不增）
grep -rEn "prisma\.[A-Za-z_][A-Za-z0-9_]*\." backend/src/services backend/src/controllers backend/src/routes | wc -l
```

## 7. 回滚
```bash
cd /Users/mac/Desktop/建材报价系统
docker compose exec -T bm_mysql mysql -u root -p"$MYSQL_ROOT_PASSWORD" bm_quotation \
  < /tmp/bm_backup/<第1节文件名>.sql
# 回滚后重跑第 6 节验证
```

## 8. 用户确认清单（授权后逐项勾）
- [ ] 已在本机（非手机）操作，已通读本手册
- [ ] 第 1 节全量备份成功，记下文件名
- [ ] Phase A（P2-d）执行 + 第 6 节验证全绿
- [ ] Phase B（P2-b）执行 + 第 6 节验证全绿
- [ ] Phase C（outbox）执行 + 第 6 节验证全绿
- [ ] 如需 P6：已在 staging 单独立项，未在本机跑
- [ ] 全部完成后告知 AI，由 AI 收口文档 / 评审提交（不推送）
