# P2 数据库迁移 · 执行记录

> 日期：2026-09-05 ｜ 执行：专家团（software-company 主理人齐活林协调）
> 授权来源：用户在 P0 安全网全量交付后全权授权专家团；P2 为「待本人确认」项，本次用户明确「点头 P2 数据库迁移」，门禁解除。

## 一、P2 原计划（来自 `docs/architecture/目标架构设计.md`）

- 交付：以当前 schema v31 为 baseline 重新生成**线性**迁移历史（`prisma migrate diff`）；所有表加 `tenant_id NOT NULL` + Snowflake PK（新增表用 Snowflake；存量表双写：旧码→`legacy_code` 只读，新 PK→Snowflake）；**退役 `db push`**。
- 验收：`prisma migrate status` 全绿（无 drift）；部署脚本不含 `db push`；新表 PK 类型为 `BIGINT UNSIGNED`；旧表 `legacy_code` 存在且只读。

## 二、本次实际执行范围（安全可验证核心）

P2 原计划体量约 3–4 周、含存量表 PK 重映射（双写过渡）。一次性对**在线开发库**做全表 PK 切 Snowflake 会触发应用层大面积改造、且易破坏当前可运行系统。按项目纪律「不破坏系统 / verify 必须绿」，本次落地 **P2 的安全可验证核心**，把需要应用双写配合的存量表 PK 切换**分阶段**处理（见第五节）。

| # | 动作 | 结果 |
|---|------|------|
| 1 | 全量备份 dev 库 `bm_quotation`（mysqldump） | `/tmp/p2_backup/bm_quotation_20260905_212519.sql`（63 表 / 875KB） |
| 2 | 退役 `db push`：`backend/docker-compose.yml` 启动命令 `prisma db push` → `prisma migrate deploy` | ✅ 已改 |
| 3 | 迁移历史线性化：40 个混乱迁移文件夹归档至 `backend/_migrations_archive_pre_p2/`，生成单一 `20260905000000_baseline_v31` 并 `migrate resolve --applied` | ✅ `migrate status` 显示 1 个 baseline 已应用 |
| 4 | 全 62 张表注入 `tenantId`（BigInt UNSIGNED NOT NULL DEFAULT 1，多租户预留）+ `legacyCode`（VARCHAR(64) NULL，旧业务编码只读快照） | ✅ 增量迁移 `20260905010000_p2_tenant_id_legacy_code` 已 `migrate deploy` 应用 |
| 5 | 新表 Snowflake PK 约定写进 `schema.prisma` 头部注释；P0 已交付 Snowflake 生成器（前后端均有）作为新表标准 | ✅ 约定落地 |

## 三、验证结果

- `prisma migrate status` → **`2 migrations found` / `Database schema is up to date!`**（绿）。
- 列落地核对：`tenantId` 62 列、`legacyCode` 62 列，类型正确（`tenantId` = `bigint unsigned NOT NULL DEFAULT 1`，`legacyCode` = `varchar(64) NULL`）。
- `_prisma_migrations` 记录：`baseline_v31` + `p2_tenant_id_legacy_code` 两条。
- `prisma validate` 通过；Prisma Client 已重新生成（含新字段类型）。

## 三·五、总门禁收尾（顺手修绿）

P2 自身验收（迁移/列/门禁中的 DB 部分）全绿后，用户选择「顺手修绿」，把项目总门禁 `npm run verify:static -- --full` 拉回全绿。排查结论：

- **S1 前端类型检查**：初次跑出红为**瞬时假红**——门禁与 `prisma generate` / `tsc` / `vitest` / 库还原并发抢占 runner，导致 `.tsbuildinfo` 增量缓存损坏。清理缓存后单独 `npm run typecheck` 退出码 0、0 错误；纯前端改动（slot 框架 WIP）与 P2 无关（P2 只动 `backend/prisma/` + `docker-compose.yml`，git 已确认）。
- **S3b 前端单测**：唯一真红。根因是 `tests/snowflake.test.ts` 的「百万级连续生成」用例跑 `100_000` 次循环 + `200_000` 次 `expect`，冷启动/门禁受限 runner 下易触碰 vitest 默认单测超时（5000ms）。修复：**只给该重用例单独设 `timeout: 30_000`**（其余用例仍走全局 5s 守卫，真实卡死仍会快速失败），不动循环规模（10w 是该用例「百万级唯一性/单调性」的验证意义所在）。

最终门禁：`npm run verify:static -- --full` → **通过 12 / 12 · 总判定 PASS — 可以交付**（退出码 0，报告落 `verify-report.json`）。

## 四、过程中的两个坑（已解决，供复盘）

1. **首次生成的全量 diff 不只加列**：`migrate diff --from-schema-datasource --to-schema-datamodel` 把库里「db push 时代遗留的游离外键 / 索引重命名 / id 类型差异 / inventory_ledger 缺列」也一并算出，导致迁移里混入了 `DROP FOREIGN KEY`、`DROP INDEX`、`MODIFY id` 等语句。直接 apply 时先后撞 MySQL `1553`（删索引时外键仍在）与 `1833`（改 id 被外键挡住）。
2. **处置**：P2 库迁移只做显式、安全的「加列」部分（从 `schema.prisma` 表名直接生成纯 `ADD COLUMN` SQL，零 `MODIFY/DROP/外键/索引` 变更），避开外键排序陷阱；历史 drift 的清理单独立项（见下）。

## 五、分阶段后续（非本次范围，需应用双写配合）

| 阶段 | 内容 | 为什么不在本次 |
|------|------|----------------|
| P2-b | 存量 62 张表 PK 由 `BigInt autoincrement` 切 Snowflake（应用层写号）；`legacy_code` 回填旧码并设为只读 | 需后端写号逻辑 + 前端 ID 消费改造，属「双写过渡」，应用层改动大，一次性做会破坏当前系统 |
| P2-c | 多租户 `TenantContext` 强制注入 `tenant_id`（单测覆盖） | 应用层能力，需独立开发+测试 |
| P2-d | 历史 drift 清理：游离外键删除、索引重命名对齐、id 类型 UNSIGNED 对齐、inventory_ledger 补列 | 触发外键排序问题（见四），需用 `migrate dev` + shadow DB 或人工逐表编排，单独成迁移更稳 |

## 六、回滚

- 任一阶段出问题：`docker exec bm_quotation_db mysql … < /tmp/p2_backup/bm_quotation_20260905_212519.sql` 还原至 P2 前干净态。
- 迁移历史：归档的 40 个原迁移文件夹仍保留在 `backend/_migrations_archive_pre_p2/`，可回溯。

## 七、行为验收（给用户）

1. 打开掌控台 / 任意表格页，系统应照常可打开、可增删改（本次只加列，不改任何现有字段与行为）。
2. 后端重启后 `prisma migrate status` 仍显示「Database schema is up to date!」。
3. 数据库 `bm_quotation` 每张表均存在 `tenantId`、`legacyCode` 两列（Navicat/任意客户端可见）。
4. `docker-compose.yml` 中后端启动已不含 `prisma db push`。
