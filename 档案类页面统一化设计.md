# 档案类页面统一化设计（product → ArchiveSlotHost）

> 生成：2026-09-05。来源：用户纠错「基础数据管理本质都是一种，差异是实现方式导致，顶层设计不够通用」。
> 配套：用户选「先出迁移方案，审完再动手」。本文是设计文档，未动任何代码。
> 用法：用户审 §5 未决项 → 说「按设计开工」→ AI 按 §4 分阶段实施。

---

## 0. 结论（先给判词，给非技术用户看）

- **product 不是"本质不同"，是"没迁过去"。** supplier、customer 早已站在统一壳 `ArchiveSlotHost` 上；product 还停在更早的旧壳（`ArchiveListPage` + 手搓列 + 1300 行 `ProductEditDialog`）。所以表面上"产品管理和其他档案页不一样"，根因是历史没统一，不是业务本质不同。
- **唯一的真结构差异只有一处**：product 是**复合体**（一个产品下挂 品牌→规格→单位→售价/进价，跨 5 张表），而且它的列表是**宽表**（一行显示"产品×品牌×规格"的一个 SKU，不是一行一个产品）。
- **框架其实已具备 product 要的大部分能力**：品牌→规格→单位 的级联槽（`cascade`）、价格类的矩阵槽（`matrix`）、单位/售价/进价的自定义列（`custom`）都已存在。缺的只是两件事：① 列表行模型（宽表 vs 一行一产品）；② 一个能承载"单位×售价×进价矩阵"的**复用槽**。
- **目标态**：把 product 也迁到 `ArchiveSlotHost`，只新增 **一个 product 专用的复用槽（SKU 价格矩阵）**，其余字段 100% 走标准槽。这样档案类三页（product / supplier / customer）共用一套壳，差异只在配置——这才是你说的"组件化、零代码复用"。

---

## 1. 现场事实（全部代码取证，2026-09-05）

### 1.1 已统一的页（supplier / customer）
- `SupplierManage.tsx` → `<ArchiveSlotHost def={assembledDef} />`；列顺序由 `assembleSlots('supplier', …)` 读 `pages.supplier.slots` 配置驱动；联系/地址/经营范围都是 `slots`。
- `CustomerManage.tsx` → 同样 `<ArchiveSlotHost def={def} />`；联系/开票/收货地址都是 `slots`。
- 共同契约：列表列、名称弹窗、N 个矩阵、状态、批量启停/删除、引用计数删除流，全部由 `def.slots` 推导，页面零手搓。

### 1.2 product 现状（`ProductManage.tsx`，1381 行）
- **列表宽表**：`searchProducts` → `SkuSearchRow`（product×brand×spec 拍平）；`dedupeSkusByProductId` 按 productId 去重做批量。
- **列**：7 列由 `entityCellSpecs['product']`（配置驱动）生成 + 3 列手搓工厂 `createSkuPriceColumns`（单位▾ / 售价▾ / 进价▾）。
- **行内改价**：`useSkuPriceState`（每行独立加载该 SKU 的单位/售价/进价，popover 内改，失焦自动补价格类型/供应商后保存）。
- **编辑弹窗**：`ProductEditDialog`（1300 行）→ 内嵌 `UnitSection` / `SpecListPanel` / `BrandImages`。
- **批量**：`BatchAdjustDialog`（批量改价）、`batchDeactivateProducts` / `batchActivateProducts`（批量启停）。
- **检索**：`listSkuSearchFacets`（产品/品牌/规格 三段级联筛选）。

### 1.3 `ArchiveSlotHost` 已具备的能力（缺口对照）
| 能力 | 现状 | product 需要 |
|---|---|---|
| 列表列推导 | name/scalar/enum/matrix/readonly/custom/cascade/toggle | 全部需要，外加"复合明细列" |
| 弹窗 + 矩阵面板 | ✅ | 价格矩阵需要 |
| 级联切换行（brand→spec→unit） | ✅ 仅进弹窗 | 需要（弹窗内） |
| 状态 / 批量启停 / 删除流（引用计数） | ✅ | 需要 |
| **列表行模型** | **强制一行=一个实体** | **product 要一行=一个 SKU（宽表）** ❌ 缺口 |
| **单位×售价×进价矩阵列** | 无现成 | 需新增复用槽 ❌ 缺口 |

> 关键判断：框架差的两件事里，**行模型**是架构不变量（一行一实体，守住了框架才不乱）；**价格矩阵列**是 product 特有业务，应作为"一个合法注册的 product 专用槽"存在，而不是把框架改成上帝配置。

---

## 2. 目标态（两种候选 + 推荐）

### 候选 A：一行=一个产品（推荐）
- `def.list` 返回 `ProductView`（每产品一行，total=产品数）；宽表检索改为"按 productId 聚合、取首 SKU 作代表行"，关键词仍匹配 SKU（`searchProducts` 前端聚合，不动 SQL）。
- **slots**：
  - `name`：产品名（点开编辑弹窗）
  - `scalar`：分类（quickAddCategory + 全局改名）
  - `image`：主图缩略（点进编辑）
  - `scalar`：备注
  - `status`：启用/停用
  - `readonly`：更新时间
  - **`composite`（新槽）`skuPrice`**：列表行内**可展开的子表**，复用 `createSkuPriceColumns` + `useSkuPriceState`，展示该产品下所有 SKU（品牌×规格×单位×售价/进价），行内改价保持不变
  - `cascade`（弹窗）：品牌→规格→单位（复用已有 `CascadeSwitchRow`）
  - `matrix`（弹窗）：价格（把 `UnitSection`/`SpecListPanel` 抽成 `ArchiveSkuPriceMatrixEditor`）
- **检索**：产品/品牌/规格 三段筛选作为 `custom` 槽，复用 `listSkuSearchFacets`。
- **批量/删除**：直接复用 `ArchiveSlotHost` 的 `batchSetStatus` / `deleteFlow`（指向现有 API）。

**给业务的影响（UX 取舍）**：列表从"扁平的一行一个 SKU"变成"一行一个产品，点开看它的所有 SKU 明细"。关键词搜 SKU 仍保留；品牌/规格筛选保留为列表级筛选（定位到产品）。行内改价从"直接在列表行上"移到"展开后的子表里"。**信息没少，只是从平铺变成可展开。**

### 候选 B：宽表分组（保留现 UX，扩展框架）
- `ArchiveSlotHost` 新增 `groupKey` + 分组渲染：列表仍返回 SKU 行，宿主按 productId 分组，组头=产品信息、子行=SKU；选择/批量/启停按组（productId）去重。
- 优点：UX 几乎零变化。缺点：框架要支持分组渲染 + 分组选择，复杂度高；product 是**第一个也是唯一一个**这种 case —— 一次就建通用分组引擎 = 过度抽象（违反"三次原则"：第二次出现才立项）。

### 推荐：候选 A + 把"展开 SKU 明细"作为 `composite` 槽
- 守住"一行一实体"不变量，框架不乱。
- product 的独特性收敛为 **一个 composite 槽**（SKU 价格矩阵），其余 100% 走标准槽 → 真正"组件化复用"。
- 不建通用分组引擎（避免过度抽象），只加一个 product 专用槽（登记理由，合法例外）。
- 业务 UX 损失可控（关键词/筛选保留，改价移到展开子表）。

---

## 3. 复合/嵌套槽扩展设计（`composite` 槽）

### 3.1 槽契约（新增一种 `ArchiveSlot` 子类型）
```ts
interface ArchiveCompositeSlot<T> {
  kind: 'composite';
  key: string;
  label: string;
  list?: boolean;          // 列表内可展开子表
  minWidth?: number;
  defaultExpanded?: boolean;
  // 给定实体行，返回 SKU 明细行（品牌×规格×单位×售价/进价）
  getSubRows: (row: T) => SubRow[];
  // 复用既有 createSkuPriceColumns 产出的列 + useSkuPriceState 行级态
  renderSubTable: (row: T, ctx: ArchiveColumnCtx<T>) => ReactNode;
}
```
> 弹窗内的品牌→规格→单位→价格编辑，不靠 composite 槽，而靠已有的 `cascade` + `matrix` 槽（§2 已列）。composite 只负责"列表行内可展开的子表"。

### 3.2 实现路径（小步，不破坏现有行为）
1. 把 `createSkuPriceColumns` + `useSkuPriceState` 从 `ProductManage.tsx` 抽到 `shared/components/archive/ArchiveSkuPriceSlot.tsx`（SSOT，product 专用）。
2. `ArchiveSlotHost` 的 `columns` 推导加一个 `kind === 'composite'` 分支：渲染可展开行（复用现有 `MatrixTable` 展开能力或新建展开行），调用 `renderSubTable`。
3. product 的 `def` 把 `skuPrice` 标成 composite 槽，其余标标准槽。
4. 编辑弹窗：品牌/规格/单位用 `cascade` 槽（已有 `CascadeSwitchRow`）；价格用 `matrix` 槽（把 `UnitSection`/`SpecListPanel` 抽成 `ArchiveSkuPriceMatrixEditor`，复用现有确认层）。
5. 检索三段筛选：`custom` 槽，复用 `listSkuSearchFacets`。

### 3.3 不变量
- **一行一实体**不变量守住；composite 只是"列表行内可展开子表"，不改变行模型。
- **不建通用分组引擎**（避免上帝配置）；composite 是 product 唯一例外，登记理由。
- 确认层范式（点值→确认层→应用）在 composite 子表内沿用（已有 `PickerEditGate`）。
- 价格回退链复用 `pricing-engine.resolveUnitPriceDisplay`（SSOT），禁止在槽里重写。

---

## 4. 迁移路径（分阶段，每阶段可验证、可回滚）

| Phase | 干什么 | 不动什么 | 验证 |
|---|---|---|---|
| **0 抽组件** | 抽 `ArchiveSkuPriceSlot`（价格列+行级态）、`ArchiveSkuPriceMatrixEditor`（单位/规格/价格编辑）到 `shared` | 不改页面渲染 | `build` 通过；product 页行为不变（只换 import 来源） |
| **1 建 def** | 在 `ProductManage.tsx` 内构建 `productDef: ArchiveEntityDef<ProductView>`（list 聚合到一产品一行；status/batch/deleteFlow 指向现有 API）；`searchProducts` 前端聚合出代表行 | 不改 `ArchiveSlotHost` | 单测/类型通过；def 字段齐全 |
| **2 换壳** | `ProductManage` 渲染 `<ArchiveSlotHost def={productDef} />`，`ProductEditDialog` 等先以 `custom`/`dialogRender` 桥接复用 | 不删旧逻辑 | 列表显示产品行；点编辑仍开旧弹窗；批量启停/删除走 def |
| **3 槽化** | category/image/remark/status 转标准槽；skuPrice 转 composite 槽；品牌/规格/单位转 cascade 槽；价格转 matrix 槽；逐步删 `ProductManage` 手搓列/手写检索 | 不留手搓第二套列 | e2e 截图逐项比对 product 页行为 |
| **4 删旧壳** | 删 `ArchiveListPage` 手搓层 + product 专用 `entityCellSpecs` 列 + 旧 `ProductEditDialog`（若已被槽替代） | — | `verify` 全过 + 冒烟 product 页 |

> 每 Phase 结束回写台账 + 重跑 `node tools/gen-boss-view.mjs`。Phase 0–1 零行为变化，可随时回滚。

---

## 5. 未决项（需你拍板，决定 §4 是否开工）

1. **UX 取舍（最关键）**：候选 A 列表从"扁平 SKU 行"变"产品行 + 展开看 SKU 明细"，是否接受？还是坚持候选 B（保留扁平、扩展框架分组）？—— **推荐 A**，但需你确认（你是非技术方，这条是体验决策，归你）。
2. **宽表聚合位置**：`searchProducts` 加 group 参数（动 SQL）还是前端聚合（不动 SQL）？—— 推荐**前端聚合**（零 DB 风险），除非你要求后端 group。
3. **composite 槽通用化边界**：仅 product 用 → 登记为 product 专用例外；若未来出现第二类复合体，再升级为通用（遵循"三次原则"）。
4. **价格矩阵仍是 product 特有业务**：composite 槽内复用现有 `pricing-engine`，禁止重写。

---

## 6. 成功标准（验收清单）

- `ProductManage.tsx` 从 ~1380 行降到 <200 行（只剩 `def` + 壳）。
- 列 / 弹窗 / 矩阵 / 检索全部由 `slots` 驱动；除 composite 槽内 SSOT 外，`grep` 不到手搓 column render。
- 档案类三页（product / supplier / customer）共用 `ArchiveSlotHost`，差异仅在 `def.slots` 配置。
- 业务行为 e2e 截图（17 页里的 product 页）逐项通过。
- `npm run verify` 全过（含 `npm run verify:static` 8 道门禁）。

---

## 7. 单据类（不在本次范围，说明边界）

CostVerify / PaymentReconcile / Delivery / AllocationView 是**多阶段业务流**（状态机 / 跨表对账 / 金额算法），与档案不同类。按引擎边界，**代码驱动、不进 `ArchiveSlotHost`**（放进配置 = 上帝配置）。本次 unification 范围 = 所有**档案 / 基础数据**页 → 一套 `ArchiveSlotHost`。
