# 档案类页面统一化设计（product → ArchiveSlotHost · 通用 displayLevel）

> 生成：2026-09-05 v2。v1 来源：用户纠错「基础数据管理本质都是一种，差异是实现方式导致，顶层设计不够通用」。
> v2 重写（2026-09-05）：用户裁决「框架要通用、按层级配置，不能给 product 开后门；且要给用户运行时视图切换；SKU 量级按生产级几百万~上千万设计，聚合走后端索引」。
> 本文是设计文档，未动任何代码（本次 v2 重写 §0/§2/§3/§4/§5/§6；§1 现场事实保留）。

---

## 0. 结论（先给判词，给非技术用户看）

- **product 不是"本质不同"，是"没迁过去"。** supplier、customer 早已站在统一壳 `ArchiveSlotHost` 上；product 还停在旧壳（`ArchiveListPage` + 手搓列 + 1300 行 `ProductEditDialog`）。
- **关键纠正（v2）：不能把 product 当特例开"后门"。** v1 方案把"一行一产品 + 展开 SKU"写成 `composite` 槽且注释「product 专用」——这就是后门。正确做法：框架提供**通用 displayLevel（展示层级）能力**，任何实体都能声明列表显示到第几级、是否有可展开的子级；product 只是其中一个配置，框架不认"product"这个名字。
- **运行时视图切换（通用能力，非 product 专利）**：product 页给用户一个「分组（一行一产品）/ 平铺（一行一SKU）」开关。该开关是**任何声明了 childLevel 的实体都能用**的通用视图能力——所以不违反"框架通用"。
- **聚合走后端索引（生产级）**：SKU 量级按几百万~上千万设计，禁止前端全量聚合。产品级列表用后端 `GROUP BY productId` 索引聚合；展开子表用单独索引查询（有界）。

---

## 1. 现场事实（全部代码取证，2026-09-05，v2 保留）

### 1.1 已统一的页（supplier / customer）
- `SupplierManage.tsx` → `<ArchiveSlotHost def={assembledDef} />`；列顺序由 `assembleSlots('supplier', …)` 读 `pages.supplier.slots` 配置驱动；联系/地址/经营范围都是 `slots`。
- `CustomerManage.tsx` → 同样 `<ArchiveSlotHost def={def} />`；联系/开票/收货地址都是 `slots`。
- 共同契约：列表列、名称弹窗、N 个矩阵、状态、批量启停/删除、引用计数删除流，全部由 `def.slots` 推导，页面零手搓。

### 1.2 product 现状（`ProductManage.tsx`，1381 行）
- **列表宽表**：`searchProducts` → `SkuSearchRow`（product×brand×spec 拍平）；`dedupeSkusByProductId` 按 productId 去重做批量。
- **列**：7 列由 `entityCellSpecs['product']`（配置驱动）生成 + 3 列手搓工厂 `createSkuPriceColumns`（单位▾ / 售价▾ / 进价▾）。
- **行内改价**：`useSkuPriceState`（每行独立加载该 SKU 的单位/售价/进价，popover 内改，失焦自动补价格类型/供应商后保存）。
- **编辑弹窗**：`ProductEditDialog`（1300 行）→ 内嵌 `UnitSection` / `SpecListPanel` / `BrandImages`。
- **批量**：`BatchAdjustDialog`（批量改价）、`batchDeactivateProducts` / `batchActivateProducts`（批量启停）。
- **检索**：`listSkuSearchFacets`（产品/品牌/规格 三段级联筛选）。
- **已抽出的复用件**：`ProductSkuSubTable.tsx`（shared，列表行内 SKU 子表，复用 `useSkuPriceState` + `createSkuPriceColumns`）——Phase 0 已完成。

### 1.3 `ArchiveSlotHost` 能力（重评，对照 v1 缺口）
| 能力 | v1 现状 | 通用化后（v2 目标） |
|---|---|---|
| 列表列推导 | name/scalar/enum/matrix/readonly/custom/cascade/toggle | 不变 |
| 弹窗 + 矩阵面板 | ✅ | 不变 |
| 级联切换行（brand→spec→unit） | ✅ 仅进弹窗 | 不变 |
| 状态 / 批量启停 / 删除流 | ✅ | 不变 |
| **整行展开子表** | 有（composite 槽，注释「product 专用」，当前无 def 使用=死代码） | **重构为通用 `childLevel`**，任何实体可声明（✅ Phase 1 已落地） |
| **运行时视图切换** | ❌ | **新增**：`displayLevel==='parent'` 时列表头出现 分组/平铺 开关（✅ Phase 1 框架已落地，product 在 Phase 3 接入后可见） |
| **后端产品级查询** | ❌ | **新增** `searchProductsGrouped`（索引 GROUP BY productId，聚合 sku/品牌数）（✅ Phase 2 已落地） |
| 价格矩阵（弹窗） | `UnitSection`/`SpecListPanel` 手搓 | 抽 `ArchiveSkuPriceMatrixEditor`（shared，Phase 0） |

> 关键判断：框架差的三件事（展开子表 / 运行时切换 / 后端产品级查询）都是**通用能力**，由 `displayLevel` + `childLevel` + `flatView` 声明驱动，不绑 product。product 的独特性（复合体 + 价格矩阵）收敛为「一个 childLevel 配置 + 复用已有 matrix 槽」，而非后门。

---

## 2. 目标态（通用 displayLevel + 运行时切换）

### 2.1 框架层：`ArchiveEntityDef` 新增两个声明
- `displayLevel: 'entity' | 'parent'`（默认 `'entity'`）：列表行停在哪一级。
  - supplier / customer → `'entity'`（扁平，无展开、无切换）。
  - product → `'parent'`（一行一产品）。
- `childLevel?: ArchiveChildLevel<T, C>`：当 `displayLevel==='parent'` 时声明子级（如 product 的 sku）。含：
  - `childApi(parent, query)` → 取该父行子表（索引查询，候选集天然有界，如单产品下最多几百 SKU）。
  - `childRowKey(c)`、`childColumns(ctx)`、`summary(parent)`（如「N 个 SKU」）。
- `flatView?: ArchiveEntityDef<any>`：运行时切换的平铺模式 def（行=子级，扁平无展开）；提供后列表头出现 分组/平铺 开关。

### 2.2 运行时视图切换（通用能力，非 product 专利）
- 当 `displayLevel==='parent'`，列表头出现开关：
  - **分组**：行 = 父级（product），每行可展开看子级（sku）子表。后端走 `searchProductsGrouped`。
  - **平铺**：行 = 子级（sku），扁平。后端走现有 sku 级 `searchProducts`。
  - 关键词 / 筛选在两种模式都生效（复用同一套 facet / 索引召回）。
- 开关是通用 UI：任何 `childLevel` 实体都有；不绑 product。这化解了 v1「候选 B=为 product 建通用分组引擎=过度抽象」的顾虑——分组渲染是 `displayLevel==='parent'` 的**通用渲染分支**，将来 supplier 要也能用。

### 2.3 候选 A / B 的处理
- 候选 A（一行一产品 + 展开）= 本方案「分组」模式；候选 B（保留平铺一行一SKU）= 本方案「平铺」模式。**两者通过运行时开关并存，框架不偏向任一、不为任一建特例。** 用户要的"切换"= 这个开关。

---

## 3. 通用 childLevel / 展开（取代 composite 后门）

### 3.1 槽/声明契约
```ts
// archiveSlotTypes.ts：原 ArchiveCompositeSlot 重构为通用 childLevel 声明（✅ Phase 1 已落地）
interface ArchiveChildLevel<T, C extends Record<string, any>> {
  key: string;            // 子级标识（如 'sku'）
  label: string;          // 展开列标题（如「SKU」）
  minWidth?: number;
  // 给定父行，返回子表明细（索引查询，有界）
  childApi: (parent: T, query: { keyword?: string; page: number; size: number }) => Promise<{ list: C[]; total: number }>;
  childRowKey: (c: C) => string;
  childColumns: (ctx: ArchiveColumnCtx<T>) => UnifiedTableColumn<C>[];
  summary?: (parent: T) => ReactNode;   // 父行展示「N 个 SKU」
}
```
> 弹窗内的品牌→规格→单位→价格编辑用已有 `cascade` + `matrix` 槽；`childLevel` 只负责"列表行模型 + 展开子表 + 运行时切换"，不改变行模型语义。

### 3.2 实现路径（小步，不破坏现有行为，✅ Phase 1 已落地）
1. `archiveSlotTypes.ts`：`ArchiveCompositeSlot` → `ArchiveChildLevel<T,C>`；`ArchiveEntityDef` 加 `displayLevel` / `childLevel` / `flatView`。
2. `ArchiveSlotHost` 渲染层：去掉「product 专用」注释，改为按 `def.childLevel` 通用渲染展开行 + 列表头「分组/平铺」开关（`displayLevel==='parent'` 且 `flatView` 时出现）。原 `ArchiveSlotHost` 改名 `ArchiveSlotHostInner`，新增默认导出包装切换 `activeDef`。
3. product 的 `def`：`displayLevel='parent'`、`childLevel={ key:'sku', childApi:getSkuOptions/新品级查询, childColumns:复用 createSkuPriceColumns, summary: N 个 SKU }`；`flatView` 提供平铺 sku def；其余字段标标准槽。

### 3.3 不变量
- **框架不认"product"**：行为完全由 `displayLevel` / `childLevel` / `flatView` 三声明驱动；任何实体声明即生效，框架零改动 = 通用性证明。
- 确认层范式（点值→确认层→应用）在子表内沿用（已有 `PickerEditGate`）。
- 价格回退链复用 `pricing-engine.resolveUnitPriceDisplay`（SSOT），禁止在槽里重写。
- 后端产品级查询必须索引驱动（`searchProductsGrouped` 复用 FULLTEXT 召回 + `groupSkuRowsToProducts`），候选集 LIMIT 控制；禁止前端全量聚合。

---

## 4. 迁移路径（分阶段，每阶段可验证、可回滚）

| Phase | 干什么 | 不动什么 | 验证 |
|---|---|---|---|
| **0 抽组件** | `ProductSkuSubTable`（已落 shared）；抽 `ArchiveSkuPriceMatrixEditor`（单位/规格/价格编辑）到 shared | 不改页面渲染 | `build` 通过；product 页行为不变 |
| **1 框架通用化** | `archiveSlotTypes` 加 `displayLevel`/`childLevel`/`flatView`；`ArchiveSlotHost` 渲染通用展开 + 运行时切换开关；删 composite 的 product 专用标注 | 不改 supplier/customer（`displayLevel` 默认 `entity`，行为不变） | ✅ 已落地（verify:static 9/9 绿，supplier/customer 零变化） |
| **2 后端产品级查询** | 新增 `searchProductsGrouped`（复用索引召回候选集 ≤500，`groupSkuRowsToProducts` 纯函数 `GROUP BY productId`，聚合 sku 数/品牌数）；`childLevel.childApi` 取子表走 `getSkuOptions` 系 | 不改 sku 级 `searchProducts` | ✅ 已落地（searchProductsGrouped + groupSkuRowsToProducts 纯函数单测绿，verify:static 9/9） |
| **3 建 product def** | `ProductManage` 构建 `productDef`（`displayLevel='parent'`、`childLevel=sku`、`flatView=sku 平铺`、标准槽 `category`/`image`/`remark`/`status`） | 不改 `ArchiveSlotHost` | 类型通过；def 字段齐全 |
| **4 换壳** | `ProductManage` 渲染 `<ArchiveSlotHost def={productDef} />`；旧弹窗先 `custom`/`dialogRender` 桥接复用 | 不删旧逻辑 | 列表显示产品行；开关切 分组/平铺；点编辑仍开旧弹窗；批量启停/删除走 def |
| **5 槽化 + 删旧壳** | `category`/`image`/`remark`/`status` 转标准槽；品牌/规格/单位转 `cascade`；价格转 `matrix`；删 `ArchiveListPage` 手搓层 + 旧 `ProductEditDialog` | — | `verify` 全过 + 冒烟 product 页 |

> 每 Phase 结束回写台账 + 重跑 `node tools/gen-boss-view.mjs`。Phase 0–2 零行为变化，可随时回滚。

---

## 5. 已决项（用户 2026-09-05 拍板，原 §5 未决项已闭环）

1. **方向**：通用 `displayLevel`（框架按层级配置，非 product 后门）。✅
2. **聚合**：后端索引（生产级 几百万~上千万 SKU，禁止前端聚合）。✅
3. **运行时切换**：要（分组/平铺开关，通用能力，任何 childLevel 实体可用）。✅
4. **composite → 通用 childLevel**；价格矩阵复用 `pricing-engine`，禁止重写。✅

---

## 6. 成功标准（验收清单）

- `ProductManage.tsx` 从 ~1380 行降到 <200 行（只剩 `def` + 壳）。
- **分组 / 平铺 两种模式 e2e 均通过**；切换开关在 product 页可见可用，关键词/筛选两模式都生效。
- supplier / customer 页行为**零变化**（`displayLevel` 默认 `entity`）。
- **通用性证明**：任何实体声明 `childLevel` 即获展开 + 切换，框架零改动（无 product 专属分支）。
- 除 `childLevel` 内 SSOT 外，`grep` 不到 product 手搓 column render。
- `npm run verify` 全过（含 `npm run verify:static` 8 道门禁 + S6 冒烟）。

---

## 7. 单据类（不在本次范围，说明边界）

CostVerify / PaymentReconcile / Delivery / AllocationView 是**多阶段业务流**（状态机 / 跨表对账 / 金额算法），与档案不同类。按引擎边界，**代码驱动、不进 `ArchiveSlotHost`**。本次 unification 范围 = 所有**档案 / 基础数据**页 → 一套 `ArchiveSlotHost` + 通用 `displayLevel`/`childLevel`。
